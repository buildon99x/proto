# ORIGIN DUST

웹툰 프로젝트. **이 디렉터리가 프로젝트 루트다.**

앱을 구현하는 프로젝트가 아니므로 `projects/` 아래로 가지 않는다. `projects/*`는 런처가 빌드해 배포하는
정적 아티팩트 전용이라 `app/` 빌드를 강제하고(`scripts/build-project.ts`), `project.json`이 없으면
`pnpm sync:registry`가 ENOENT로 죽는다(`scripts/project-utils.ts`의 `readAllProjects`가 모든 하위
디렉터리에서 `project.json`을 읽는다). 둘 다 `pnpm build:vercel`을 깨뜨린다.

## 문서

| 파일 | 내용 |
|---|---|
| [`concept-v2.md`](concept-v2.md) | **정본.** F1~F5를 닫은 개정본 — 작업은 이 문서를 본다 |
| [`concept.md`](concept.md) | 컨셉 안 **보관 원본.** 받은 파일 그대로(바이트 동일), 고치지 않는다 |
| [`concept-review.md`](concept-review.md) | 컨셉 안 리뷰 — 결함 10건(P0 2 · P1 3 · P2 5)과 처리 순서 |
| [`concept-revision-p0.md`](concept-revision-p0.md) | P0 수정안 — F1·F2·F5, v2에 반영된 패치 11개(before/after) |
| [`concept-revision-p1.md`](concept-revision-p1.md) | P1 수정안 — F3·F4, v2에 반영된 패치 9개(before/after) |
| [`eval/00-rubric.md`](eval/00-rubric.md) | **채점 도구.** 공통 서사 10축 + 매체 4축(소설/웹툰), 게이트, 채점 전 측정 3종, 수렴 규칙 |
| [`eval/01-goldset-novel.md`](eval/01-goldset-novel.md) | 소설 예시 — good 8(공개도메인 verbatim) · bad 4 |
| [`eval/02-goldset-webtoon.md`](eval/02-goldset-webtoon.md) | 웹툰 콘티 예시 — good 4 · bad 4 (전부 창작) |
| [`eval/03-example-novel-blind.md`](eval/03-example-novel-blind.md) | 소설 블라인드 채점 시연 — 샘플 5개, 게이트 발동 기록 포함 |
| [`eval/04-example-webtoon-blind.md`](eval/04-example-webtoon-blind.md) | 웹툰 블라인드 채점 시연 — 샘플 3개, 같은 소재로 4.57 대 3.07 |
| [`eval/05-loop-ledger.md`](eval/05-loop-ledger.md) | 패스 기록 |
| [`eval/06-axis7-rejudgement.md`](eval/06-axis7-rejudgement.md) | 축7 재판정 — 독립 레인 2곳, 각각 2점 |
| [`eval/07-pass4-reports.md`](eval/07-pass4-reports.md) | **패스 4 채점 보고** — 축소본 세 원고, 독립 레인 3곳의 보고서 전문 |
| [`episodes/`](episodes/) | 콘티 — 프롤로그(20컷) · 1화(79컷) · 2화(79컷). 각 파일에 패스별 채점 기록이 붙어 있다 |

**정본은 `concept-v2.md`다.** `concept.md`는 받은 파일 그대로(바이트 동일) 보관하며 고치지 않는다 —
원본과 개정본이 나란히 있어야 무엇이 왜 바뀌었는지 추적된다. 요약·대조·파생 설정은 별도 파일로 쓴다.

## 컨셉 안이 정한 것

완성된 세계관 문서다. 컨셉 안이 스스로 요구한 18항목의 답이 전부 들어 있다 —
제목(ORIGIN DUST), 로그라인, 시놉시스, 세계관 4구역, 지구 멸망 사건(ZERO FALL과 그 뒤의 진실),
유물이 비싼 여섯 가지 이유, 등급 체계(D/C/B/A/S/X), 세력 7개, 주인공 레이 한, 동료 4명,
우주선 MOTH-13, 메인 미스터리(THE LOST 11 HOURS), 시즌1 에피소드 12개, 1화 상세, 대표 유물 20개,
주제("진짜란 무엇인가"), 차별점 5가지, 회차 공식 9비트.

채점에 직접 쓰는 것은 `eval/00-rubric.md` §8에 옮겼다 — 약속 한 문장, 시즌1의 질문, 회차 9비트 공식,
축7이 검사할 네 질문의 답. **§8의 입력은 모두 확정됐다** — 매체·독자 진입·회차 단위(60~80컷).

v2는 여기에 더해 **11시간의 충격을 떠받치는 공식 역사**(18시 이후 생존자 없음),
**레이의 나이 공백이 의도라는 선언**, 그리고 **ECHO → 유물 경제 → JANUS를 한 줄로 꿴 인과**를 갖는다.
콘티는 v2 기준으로 작업한다.

## 평가 자산의 성격

`00-rubric.md`는 ORIGIN DUST를 첫 소비처로 삼지만 작품 설정에 묶여 있지 않다. 한국어 서사 원고면 무엇이든 잰다.
다른 작품이 생겨 공유할 필요가 서면 그때 `docs/kb/`로 올린다 — 쓰는 곳이 하나인 동안은 여기 둔다.

컨셉 단계를 다루는 [`../kb/concept-planning/`](../kb/concept-planning/)이 "이 기획이 될 물건인가"를 묻는다면,
이 자산은 "이 원고가 독자를 다음 화로 데려가는가"를 묻는다.

## 상속과 차이

상위 자산은 `buildon99x/omc-ws` → `.claude/skills/max-writing/`이다(설득문 14축 루브릭·한국어 문장 위생·골드셋 12종).
거기서 **문장 위생 축과 블라인드 채점 절차를 그대로 가져왔고**, 설득문 전용 축인 `제목·소제목의 힘`과 `스캔성`은 버렸다.

버린 근거는 max-writing 자신의 실측이다. 그 저장소의 `examples/12-fiction-opening-draft/review-pass1.md`에서
현진건 「운수 좋은 날」은 평균 4.00, 김유정 「동백꽃」은 3.86을 받아 AI 초안(4.29)에 밀렸다.
소제목이 없고 문단 분절이 없다는 이유였다. **스캔되는 소설은 실패한 소설이다.**
같은 텍스트가 `eval/00-rubric.md`에서는 4.67·4.50을 받고, 같은 AI 초안은 4.21로 미수렴 판정을 받는다
(`eval/03-example-novel-blind.md` §8).

## 운용 규칙

- 평가 도구는 `eval/00-rubric.md` 하나다. 다른 기준으로 원고를 판정하지 않는다.
- **작성 ≠ 리뷰.** 자기가 쓴 원고를 자기가 채점하지 않는다. 결정 점수는 원고를 쓰지 않은 별도 컨텍스트에서 나온다.
- 채점 전에 §2의 M1(회수 문장)·M2(이탈 표시)·M3(화자 가리기)를 먼저 측정한다. 점수보다 이 셋이 먼저다.
- 판단할 근거가 원고에 없는 축은 점수 대신 `–`를 쓰고 평균에서 뺀다. 짐작으로 3점을 주면 평균이 거짓말을 한다.
- 고치는 순서는 구조(축1·3·4·6) → 연출(축11~14) → 문장(축9·12)이다. 뒤집으면 다듬은 문장을 버리게 된다.
- 모든 패스를 `eval/05-loop-ledger.md`에 기록한다.

## 예시를 늘릴 때

- 소설 good 예시는 **공개도메인 원문 verbatim**만 쓴다. 출처 URL과 저작권 상태를 반드시 적는다.
- 연재 중인 웹툰·웹소설의 본문을 나쁜 예로 인용하지 않는다. 웹툰 예시는 창작 콘티로 쓰고 그 사실을 명시한다.
- 예시를 추가하면 어느 축을 시연하는지 태그를 달고, 짝이 되는 bad 예시를 같이 넣는다.

## 다음에 할 일

1. **F11~F13을 닫는다** — 축7 재판정(독립 레인 2곳, 각각 2점)이 찾은 P0 셋.
   [`concept-review.md`](concept-review.md) 참조. F11·F12는 F3·F4 패치가 스스로 만든 결함이고,
   F13은 시즌 클라이맥스의 데우스 엑스 마키나다. 닫은 뒤 축7을 다시 잰다.
2. **`concept-v2.md` 머리말을 옮긴다** — 1~7행의 결함 번호·패치 요약이 판정 레인의 눈에 들어간다.
   정본 본문에서 떼어 별도 파일이나 문서 말미로 옮긴다.
3. **골드셋 교체** — `eval/02-goldset-webtoon.md`의 가상 설정 예시(먼지 채굴·세이렌 가문)를 실제 설정
   (레이 한·MOTH-13·SEOUL BASIN·빨간 녹음기) 기준 good/bad 쌍으로 바꾼다. 이제 근거가 있다.
4. **1화 채점** — `concept-v2.md` §17의 1화 구성을 `eval/00-rubric.md`로 재 본다. 다만 그것은 콘티가 아니라
   산문 트리트먼트라 축11~13(컷 분할·스크롤 연출·그림-글 분업)은 `–`가 된다. 콘티로 옮긴 뒤 다시 잰다.
5. **완료 (2026-09-22)** — 회차 컷 수를 60~80컷으로 확정했다. 프롤로그·외전은 제외한다.

## 참고할 만한 사내 자산

`projects/relic-king`(유물왕)이 유물의 **등급·감정·경매·희소성**을 이미 데이터로 다뤘다
(`notes/artifacts-dataset.md`, 메트로폴리탄 미술관 CC0 메타데이터 기반 1,902종).
`concept-v2.md` §2의 유물 가치 체계와 §7의 등급 시스템을 설계할 때 대조해 볼 값이 있다. 장르는 다르지만 경제 구조가 겹친다.
