# Evaluation

- `pnpm --filter msw-inc build`가 통과한다.
- `pnpm build:vercel` 후 런처에서 `/runs/msw-inc/index.html`이 열린다.
- 구현이 들어오면: 첫 10분 안에 첫 진화 결정이 나오고, 8시간 뒤 출근 리포트가 뜨며, 방치 중 어떤 수치도 줄지 않는다(P5). 기준 수치는 컨셉 `prototype/pacing-check.js`와 대조한다.
