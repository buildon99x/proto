# Evaluation — 유물왕

> v0.1 검증 결과. 측정은 헤드리스 시뮬(`pnpm --filter relic-king sim`)과
> 실시간 스모크(`node tests/e2e/smoke.mjs`)로 한다. 마지막 측정: 2026-09-18
> (리뷰 2회차 A1 대응 — `notes/decisions.md` G39가 `engine.ts`의 `PENDING_CAP`
> 오버플로 자동매각을 제거해 §2 아래 수치가 갱신됐다. 엔딩이 빨라지고 자산이
> 오른 것은 회귀가 아니다 — 이전에는 일부 유물이 큐 오버플로로 헐값에
> 자동 처분되고 있었다).
>
> **문서 정합 라운드(B5~B13, `notes/decisions.md` G51 이후)**: 이 실행은
> 실측 대신 **문서 간 정합성**을 닫았다 — 삭제된 시스템(거래소·보험·체납)의
> 잔재를 레포 전체에서 grep으로 찾아 정리했고, §9·§10의 자기채점을 실제
> 문서 상태에 맞춰 정직하게 다시 매겼다(대다수 ✅였던 항목이 사실은 거짓이었고,
> 이번 라운드가 그 근거를 실제로 고쳐 다시 ✅로 만들었다 — #9만 ⚠로 남는다).
> `notes/world-map.md`의 9거점 `eras`(층별 시대 라벨)도 이번에 실제로 채웠다.
>
> **v0.2 구현 1단계(엔진 코어, `notes/decisions.md` G51) — 2026-09-18 재측정**:
> 이번 실행이 처음으로 **코드**를 v0.2로 움직였다(그 전까지는 전부 문서
> 정합성 작업이었다). §2 아래 표가 이번에 실제로 갱신됐고, **엔딩이 크게
> 느려졌다**(29분 36초 → 168시간 방치해도 미달성) — 척추 점검 보고 대상이다.
> 원인은 spec.md §9.5(드랍 간격 하한 + 심층 티어 가중 재역산)를 작업 지시가
> 명시적으로 요구해 **지금** `balance.ts`에 반영했기 때문이다. spec.md §9.5
> 자신은 "480종·12거점 파이프라인과 함께 적용, 지금 `balance.ts`에는
> 반영하지 않는다"고 못박아 뒀는데, 이번 작업 지시가 그 유보를 무시하고
> 구현을 요구해 그대로 따르고 **여기 보고한다**(정정 대상이 아니라 보고
> 대상 — 지시대로 구현했다). 상세 원인·수치는 §2 아래 표와
> `notes/decisions.md` G51 참조.

## 1. 자동 검증

| 항목 | 방법 | 결과 |
| --- | --- | --- |
| 타입체크 + 빌드 | `pnpm --filter relic-king build` | ✅ 무오류 |
| 앱이 뜨고 방치로 진행된다 | `node tests/e2e/smoke.mjs --seconds 45` | ✅ 45초 방치 후 도감 2점 |
| 네 탭이 모두 렌더된다 | 스모크 3~5단계 | ✅ 소장고·세계·도감 |
| 콘솔/페이지 오류 0건 | 스모크 CDP 로그 | ✅ 0건 |
| 세계 원장 보존 | 시뮬 종료 시 전수 검사 | ✅ `획득 합 + 잔여 == 총량`, T4 중복 소유 0 |
| 오프라인 적분이 스텝 크기와 무관 | 1초 스텝 vs 10초 스텝, 1시간 | ✅ 드랍 횟수·층·진척 완전 일치 |

진척 적분은 결정론이므로 **드랍 횟수와 층**을 비교한다. 어떤 유물이 걸리느냐는 난수라
비교 대상이 아니다 — 처음엔 자산으로 비교했다가 52% 차이가 나 측정 자체가 틀렸음을 알았다.

- [x] 세이브 왕복(export → 초기화 → import) 자동 검증 — `qa_migration.ts`의
      "다시 직렬화→역직렬화해도 동일하다" 항목이 이 경로를 직접 돈다
      (export/import는 base64 왕복만 더할 뿐 같은 `serialize`/`deserialize`다).
- [x] 마이그레이션 체인 — v1→v2, `app/src/sim/qa_migration.ts`
      (`pnpm --filter relic-king exec tsx src/sim/qa_migration.ts`)로 17개 항목
      전부 통과 확인(아래 §2.1). v1 JSON을 손으로 만들어 넣고 `version`·
      `vault[].condition/displayed`·`stats.firstT4Finds`·`seasonState`가
      안전한 기본값으로 채워지는지, 기존 v1 데이터(자금·층·pending·codex 등)가
      무손실로 보존되는지 필드 단위로 확인했다.

## 2. 밸런스 (클릭 0회, 방치 기준선)

> **재측정(2026-09-18, v0.2 구현 1단계)** — spec.md §9.5(드랍 간격 하한
> `DROP_INTERVAL_FLOOR_SECONDS`=20초 + 심층 티어 가중 재역산)를 `balance.ts`에
> 반영한 뒤의 수치다. **엔딩이 크게 느려졌다**(29분 36초 → 168시간 방치해도
> 미달성) — "단 드랍 간격 하한은 의도된 변경이다"라는 지시에도 불구하고
> 이 정도 규모의 변화는 보고 대상이라 아래에 명시한다.
>
> 근본 원인은 하한 자체가 아니라 **가중 재역산**(`LAYER_BASE_WEIGHTS_8_9`·
> `_10_12`)이다 — T3 가중이 1→0.01(약 100배 감소), T4가 0.1→0.005(20배
> 감소)로 떨어져 롤 1회당 T3·T4를 뽑을 확률 자체가 극히 낮아졌다. 여기에
> **60종·3거점이라는 현재 데이터셋 규모**가 겹친다 — 이 가중치는 spec.md
> §9.5 자신이 "`D≈5,209`(480종·12거점 정상상태)" 기준으로 재역산한 값인데,
> 지금 데이터셋은 티어당 종 수가 훨씬 적어(예: 이집트 T2가 몇 종뿐) 그 적은
> 종의 유한 재고(T2 종당 60점)가 플레이어+라이벌 6인의 병렬 채굴로 4시간
> 안에 소진된다(`qa` 목적의 디버그 계측으로 직접 확인 — assets가 4시간
> 시점부터 168시간까지 정확히 동일하게 고정됨). 재고가 바닥나면
> `candidates()`가 그 티어에서 빈 풀을 반환해 즉시 하위 티어로 캐스케이드되고,
> 이후 모든 드랍은 T0·T1로 영구히 수렴한다 — 이 시즌 안에는 회복 경로가
> 없다(리젠 메커니즘은 이번 단계 범위 밖). **드랍 간격 하한 자체는 오히려
> 보호 방향으로 작동한다** — D가 커질수록 초당 드랍 횟수가 무제한으로
> 치솟던 v0.1 구식을 ~20초당 1회로 고정해, 재고 소진 자체를 오히려
> "느리게" 만든다. 세계 원장 보존·T4 중복 소유 0·오프라인 T3·T4 상실 0건은
> 168시간 재확인에서도 전부 유지된다 — 회귀는 속도뿐, 불변식은 안 깨졌다.
> 상세는 `notes/decisions.md` G51.

| 기준 | 목표 | 측정(2026-09-18 이전) | 측정(v0.2 1단계 이후) |
| --- | --- | --- | --- |
| 첫 유물 드랍 | 40초 이내 | 12초 | **20초** |
| 초기 20분 평균 드랍 간격 | 300초 이하 | 1.1초 | **20.5초**(하한 20초가 거의 그대로 관측됨 — 의도한 동작) |
| 2권역(이집트) 해금 | — | 11분 42초 | 38분 30초 |
| 3권역(로마) 해금 | — | 29분 36초 | **미달성**(24h 기준 최고 보유 자금 9,444만 ₩, 비용 3억 ₩) |
| 첫 국보(T3) | — | 17분 56초 | 54분 30초 |
| 첫 유일(T4) | — | (미측정) | 1시간 22분 |
| 엔딩(자산 1위 + 도감 75%) | 1~2시간 | 29분 36초 | **미달성**(168시간 방치 재확인, 위 설명 참조) |
| 클릭 최대 활용 시 진행 속도 | ×1.35 이하 | ×1.35 | ×1.35(변화 없음) |
| 추격 계수 | ×2.0 이하 | 최대 ×1.35 관측 | 최대 ×1.28 관측(24h) |
| 세계 원장 보존(72h·168h 재확인) | 항상 ✅ | ✅ | ✅ |
| 오프라인 T3·T4 상실(12h) | 0건 | 0건 | 0건 |

### 2.1 v0.2 1단계 신규 검증

| 항목 | 방법 | 결과 |
| --- | --- | --- |
| `balance.ts` v0.2 상수 전량 선언(spec.md §14) | 코드 리뷰 + `pnpm --filter relic-king build` | ✅ 전부 선언, 각 절 번호 주석 포함 |
| 드랍 간격 하한 실동작 | 시뮬 로그 — 20분 평균 간격 20.5초 | ✅ 하한이 정확히 걸린다 |
| 자동매각 T3·T4 하드 예외 + 종당 1점 보존 | `engine.ts` `autoSellEligible()` 코드 리뷰(단위 테스트는 후속 단계에서 `vitest` 도입 시 추가 권장) | ✅ 로직 반영, 아직 자동화 테스트는 없음(⚠) |
| 미감정 큐 오버플로 자동매각 제거 확인 | `engine.ts`에 그 경로 자체가 없음(grep) | ✅ 제거됨(이번 세션 이전 G39에서 이미 제거, 이번 단계는 유지만 확인) |
| CodexState 5종 전이(드랍→owned_unidentified→owned, 완전 매각→discovered_not_owned) | 코드 리뷰 + 24h 시뮬에서 `소장`/`소실` 카운트가 기대대로 움직임 | ✅ |
| 3축 순위 함수(assetScore/codexScore/fameScore/rankScore) | `pnpm --filter relic-king sim`의 "v0.2 3축 순위" 출력 | ✅ 계산됨. **60종·3거점 데이터셋에서는 `ASSET_SCORE_REF`(480종·12거점 기준 375억 ₩)가 지나치게 커서 자산 축이 항상 1.0 근방으로 포화된다**(보고 대상, `notes/decisions.md` G51) |
| 세이브 v1→v2 마이그레이션 | `pnpm --filter relic-king exec tsx src/sim/qa_migration.ts` | ✅ 17개 항목 전부 통과 |
| 시즌 롤오버 함수 | `pnpm --filter relic-king exec tsx src/sim/qa_season_rollover.ts` | ✅ 17개 항목 전부 통과(vault 헌정·funds 환전+소각·시설/거점 리셋·`PersistentRecord` 적립·상한 클램프) |

클릭 이득은 **자산비가 아니라 진척 속도**로 잰다. 방치형은 성장이 지수적이라 진척 1.35배가
자산비로는 2배 넘게 부풀려 보인다 — 처음 측정에서 ×2.07이 나와 기준을 잘못 잡았음을 알았다.

## 3. 상실 규칙 (설계 원칙의 방어선)

| 기준 | 결과 |
| --- | --- |
| 오프라인 12시간 시뮬에서 T3·T4 상실 | ✅ **0건** |
| 유일 유물은 제보 레이스 밖에서 라이벌에게 넘어가지 않는다 | ✅ 엔진에서 티어 상한으로 강제 |
| 제보를 무시해도 흔함~진귀는 완주 가능하다 | ✅ 제보는 드랍 가중일 뿐 진행 조건이 아니다 |

- [ ] 제보 대응 시 승률이 미대응 대비 유의하게 높은지 — 통계 측정 미구현.
      (현재는 대응 시 롤당 28%, 라이벌 10%로 구조상 우위가 보장된다)

## 4. 데이터 검증 (실존성) — v0.2 3단계로 대폭 진전(**여전히 미완**)

전제가 "실존 유물"이므로 이 항목이 틀리면 게임의 근거가 무너진다.
v0.1~2단계는 개발 세션 자체가 네트워크 차단이라 검증이 불가능했다. **3단계(2026-09-18)
세션은 실제로 `WebSearch`에 접근할 수 있었다**(`notes/decisions.md` G53.1 — 일반
아웃바운드 HTTPS는 여전히 막혀 있지만 WebSearch/WebFetch 툴은 별도 경로로 동작했다)
— 그래서 이번엔 실제 1차/공식 자료 대조가 가능했다. 최신 상태는 §13 참조.

- [x] 280종(12거점) 중 182종을 이번 세션에 WebSearch로 실제 대조해 `sourceStatus:
      "verified"`로 표시했다. 나머지 98종은 실존은 확신하지만 개별 대조를 못 해
      `"pending"`으로 남겼다(드랍 풀 제외, G-B8) — §13 표 참조.
- [x] **T4(유일) 12점 전부**(거점당 1종, 12거점 전부 확보)가 서로 다른 발행주체
      출처 2개 이상으로 실제 대조됐다(`qa_artifacts.ts`가 이 규칙을 강제한다).
- [x] 훈민정음 해례본·백제 금동대향로는 경주 T3/T4 쿼터 초과로 이번 시즌
      로스터 밖으로 **보류**했다(거짓 하향 대신 — `notes/decisions.md` G53.3).
- [x] 하트셉수트 좌상·파우누스 청동상은 개체 특정성이 낮아 T3→T2로 내렸다(같은 이유).
- [x] 반환 논쟁 유물에 `disputed` 표기 — 로제타 석·코이누르 다이아몬드·에레크테이온
      카리아티드·라이몬디 석비 등에 적용.
- [x] 도감 하단에 "평가액은 가상 단위" 문구 상시 노출(v0.1부터 유지).
- [x] 연대와 권역·층 구성 점검 — `qa_artifacts.ts`가 era↔eras 키워드 겹침을
      경고로 낸다(자유 문장 대조라 신호비가 낮다, `notes/decisions.md` G53.8 참조.
      실제 시대 착오 방지는 "거점별 독립 수집"이라는 구조적 제약이 담당한다).
- [ ] 나머지 98개 pending 종의 실제 검증(후속 세션 — 네트워크 가능 여부 재확인 후).
- [ ] 남은 200종(480−280)의 추가 수집(4단계 이후).

## 5. 수동 체크리스트

- [x] 방치만으로 진행되고, 클릭은 가속으로만 작동한다.
- [x] 지층 단면이 진척에 따라 내려가고 층마다 흙 색과 연대가 바뀐다.
- [x] 유물을 팔면 자금이 오르고 순위가 내려간다(자산 = 소장 유물 평가액 합).
- [x] 미감정 매각이 추정가의 70%로 동작하고, 자금 부족 시 감정 대기를 UI가 알린다.
- [x] 제보 배너가 카운트다운과 이동 버튼을 띄운다.
- [x] 도감이 미발견(실루엣) / 소장(컬러) / 소실(회색 + 소유자)을 구분한다.
- [x] 복귀 요약이 라이벌 활동을 포함해 손실을 숨기지 않는다.
- [x] 세계 탭에서 확률표·추격 계수 공식·오프라인 규칙을 볼 수 있다.
- [x] 티어를 색만으로 구분하지 않는다(테두리 두께 + 배지 기호).
- [ ] 실제 모바일 기기에서 4탭과 지층 화면 확인 — 헤드리스 뷰포트만 확인했다.

## 6. 재미 가설 (플레이테스트 미실시)

설계가 목표한 세 문장이 실제로 발생하는지는 **사람이 플레이해야 알 수 있다.** 미검증.

- [ ] T4 획득 직후 소장고·도감을 다시 여는가.
- [ ] 제보 배너가 뜨면 방치를 깨고 개입하는가. 개입률 30% 미만이면 제보 설계 실패.
- [ ] 유물 내력을 끝까지 읽는가.
- [ ] 팔지 말지 망설이는 순간이 세션당 1회 이상 나오는가.

## 7. 알려진 한계

- 라이벌은 NPC 시뮬이다. "사람과 겨룬다"는 감각은 약하다.
- v0.3(거래) 이전까지 영구 상실은 회복 경로가 없다. 그래서 상실 경로를 레이스 하나로 좁혔다.
- 도트 스프라이트는 절차 생성이라 실물 재현도가 낮다. 실존성은 텍스트가 담보한다.
- 레포 공용 `pnpm playtest`는 puppeteer가 크롬을 내려받아야 해서 이 환경에서 쓸 수 없다.
  대신 미리 깔린 크로미움을 CDP로 직접 모는 `tests/e2e/smoke.mjs`를 썼다.

---

# v0.2 — 신규 측정 기준

> `prompts/v0.2-deepening.md` §6·§8 6단계의 산출물. v0.2 시스템은 아직 구현 전이라
> 아래 목표치는 전부 **설계 목표**다 — 상태 열은 실측이 아니라 "구현 후 측정 방법이
> 명시돼 있는가"를 가리킨다. 전제는 `notes/decisions.md`, `notes/economy.md`,
> `notes/world-map.md`, `notes/staff.md`, `notes/ux-v02.md`.

## 8. v0.2 측정 기준과 목표치

| # | 기준 | 목표치 | 측정 방법 | 상태 |
| --- | --- | --- | --- | --- |
| 1 | 24시간 통화 **초과**성장률(개정 G22/A8) | `(M(t+24)−M(t))/M(t) − 24/t ∈ [-0.10, 0.15]`, `MATURE_PHASE_START_HOUR = 48` 이후, **7일 이동평균 `M̄(t)`로 스무딩**(`notes/economy.md` §5) | `pnpm --filter relic-king sim --hours 336 --sell-profile aggressive\|neutral\|hoarding`(3프로필). **`--sell-profile` 플래그와 `M̄(t)` 로그는 아직 구현 안 됨.** 단, v0.1 엔진(σ=1 정책)을 직접 실측해 기존 절대율 캡(0.12)이 자기 수식으로 위반됨을 확인하고 초과율로 재정의했다(`notes/decisions.md` G21·G22) — 그 실측이 이 개정의 근거다 | ⏳ v0.2 미구현 / **기반 v0.1 실측 완료(G21·G22)** |
| 2 | 유한재고 매각 비중(지표 교체 — G23/A9) | `(시즌 누적 T1~T4 매각액)/(시즌 누적 전체 매각액) ≥ FINITE_STOCK_REALIZATION_SHARE_MIN = 0.15` — 기존 `ARTIFACT_TO_CURRENCY_STEADY_RATIO`(절대 통화량 대 고정 스톡, 차원 불일치)는 폐기(`notes/economy.md` §6.2) | 시즌 시뮬 종료 시점까지 티어별 누적 매각액을 로그하고 비율 계산 | ⏳ 미구현·미측정 |
| 3 | 박물관 순수익 캡(기준 전역화 — G24/B1) | 임의 시점 순수익 ≤ `MUSEUM_NET_INCOME_CAP = 0.30` × **플레이어 전체 발굴단 `D` 합** 기준 잠재화폐창출률(1시간 이동평균) — 기존 "그 거점 D" 기준은 박물관 위치로 캡을 3.33배까지 우회할 수 있었다 | 시뮬에 박물관 보유 상태를 강제로 만들고(관장 스탯 100 고정) 매 시간 순수익/**전체** 발굴소득 비율을 로그, 최댓값이 0.30을 넘는지 확인. 티켓가 20,000₩ 기준 예시 계산은 `spec.md` §10.3 참조(회수기간 **204.4h** — 정정, B13: 442h(1회차 원안) → 177.0h(오기) → 189.5h(급여 스탯배율 정정, G49/B4) → 204.4h(스텝 스탯 4→2 축소 반영, G50/C#5, 최종) | ⏳ 미구현·미측정 |
| 4 | 스텝 총 급여 비율(급여 모델 개정 — G29/B7) | `STAFF_TOTAL_SALARY_SHARE_TARGET = 0.08`(8%) 근접 — 급여가 잠재소득이 아니라 **실현 매각액 원천징수** 기준으로 바뀌었으므로 분모도 실현 매각 총액이다 | 시뮬에서 (단장+관장+경매관장 원천징수 합) / 실현 매각 총액 비율을 로그(`notes/staff.md` §5) | ⏳ 미구현·미측정 |
| 5 | 도난율(값 확정 — G28/B6, **보험은 G50/C#1로 전량 삭제**) | `THEFT_RATE_BASE = 0.0014/시간`(독립 설계값, 하루 약 3.4% — G50/C#1로 보험이 삭제되며 "화폐 복제 방지 상한 ≤0.00167"이라는 원래 근거도 함께 폐기됐다. `THEFT_RATE_BASE` 자체는 도난이라는 리스크 게임성으로서 유지된다) | 전시 중 T0~T3 유물의 시간당 도난 발생 횟수를 시뮬에서 집계해 `THEFT_RATE_BASE`와 일치하는지 확인. **정정**: 보험금-회수권 상호배타 확인 항목은 삭제됐다(보험 자체가 없다) — 72시간 회수 창 + 관장 `SECURITY_SENSE` 회수 성공률 + 암시장 장물 재발견 셋만 확인한다 | ⏳ 미구현·미측정 |
| 6 | 원정 실패(미스헵) 확률 상한 | `EXPEDITION_MISHAP_CHANCE_CAP = 0.25` | 파견 시점 판정값을 전량 로그, 거리별 분포가 `spec.md` §8.3 공식과 일치하는지 대조 | ⏳ 미구현·미측정 |
| 7 | 지역 차익 상한(값 재조정 — G26/B3) | `ARBITRAGE_MAX_SPREAD_RATIO = REGIONAL_PRICE_MULT_MAX/MIN = 1.40/0.60 ≈ 2.33`(기존 1.40) | 임의 시각·유물 종에서 12거점 `LOCAL_PRICE_MULT` 최대/최소 비율을 계산해 ≈2.33을 넘지 않는지 전수 검사(결정론적 함수라 시뮬 없이도 단위 테스트로 가능). 보유 3거점 국한 시 개입 이득이 ≥20%인지도 함께 확인(목표치, G26 유도 근거) | ⏳ 미구현·미측정 |
| 8 | 관여 예산 — 손대지 않았을 때 손실 0(구조적으로 보장 — G29/B7) | 루틴 켠 발굴단은 무한 방치해도 자금이 음수로 가지 않는다(`notes/decisions.md` G3). 급여가 판매 시점 원천징수라 **체납이라는 상태 자체가 존재하지 않는다**(기존엔 유예 밴드로 방어했다) | 오프라인 시뮬(현재 `OFFLINE_CAP_SECONDS`보다 긴 구간)에서 `funds`가 0 미만으로 내려가지 않는지 확인. 급여 관련 상태 페널티가 발생하지 않음을 확인(체납 상태 자체가 삭제됨) | ⏳ 미구현·미측정 |
| 9 | 관여 예산 — 개입 시 이득 | 개입(즉시 최적 채널 선택·거점별 최고가 매각 등) 시 방치 대비 자산 증가 `ENGAGEMENT_UPSIDE_MIN~MAX = 0.20~0.40`(20~40%) | v0.1의 클릭 이득 측정 방식(자산비가 아니라 진척 속도 비교, §2 참조)을 재사용해 "개입 시나리오 스크립트" vs "루틴만 켠 방치"를 같은 시간 동안 비교. 세계지도 로컬 시세 개입은 G26/B3로 재조정된 값(0.60/1.40) 기준 ≥20% 목표 | ⏳ 미구현·미측정 |
| 1b | **[신설 G21/A6] 발굴단 재투자 폭주 방지** | σ=1(전량 즉시매각) 정책으로 168시간 방치해도 `D`가 `MAX_GEAR_LEVEL=16` 상한 근방(≈32만/s)에서 자기제동해야 한다(기존 무상한 시 1,446만/s까지 폭주 실측) | `app/src/sim/run.ts`의 매각 정책을 σ=1로 바꾼 임시 스크립트로 실측(레포에는 없음, `notes/decisions.md` G21 재현 가능). `pnpm --filter relic-king sim`(기본 σ<1 정책)이 수정 전후 바이트 단위로 동일함도 함께 확인 | ✅ **실측 완료**(G21, 아래 결과 표) |
| 10 | 첫 세션 마일스톤 | `MILESTONE_FIRST_SESSION_MINUTES = 20`분 안에 거점선택→첫원정→첫감정→첫전시 | 스모크 시나리오 확장: `tests/e2e/smoke.mjs --scenario first-session`(신규 플래그, 미구현)이 4개 체크포인트 도달 시각을 기록 | ⏳ 미구현·미측정 |
| 11 | D1/D7/D30 마일스톤 | `MILESTONE_D1_MINUTES=30`, `MILESTONE_D7_HOURS=3`(박물관 1관), `MILESTONE_D30_HOURS=10`(거점 이전 또는 2관) | `pnpm --filter relic-king sim --hours 720`(30일) 실행 후 위 각 시점의 상태(박물관 보유 수·거점 수) 로그 대조 | ⏳ 미구현·미측정 |
| 12 | 480종 데이터 무결성 | 거점별 `SPECIES_PER_SITE_BY_TIER = [25,8,4,2,1]` 목표(정확한 일치는 요구하지 않는다 — 실존성 우선, 미달은 정직하게 보고), 전량 `source` 필드 보유(T4는 2개 이상) | `pnpm --filter relic-king qa:artifacts`(`app/src/sim/qa_artifacts.ts`) | ✅ **3단계 실측 완료**(§13) — 280/480종(58%), T4는 12거점 전부 정확히 1종 |
| 13 | 주요 액션 조작 단계 수 | 18종 전부 3단계 이내(`notes/ux-v02.md` §2 — 리뷰 2회차/A13로 재계상, 거래소 2종 제거 + 온보딩·탐색·규칙·세이브 4종 신설, 구 16종에서 18종으로) | 설계 문서 검토로 이미 확인됨(수치 설계 단계) — 구현 후에는 각 액션을 실제 클릭 수로 재확인 | ✅ 설계 검토 완료 / ⏳ 구현 후 재확인 |

### 8.1 리뷰 1회차 수치 정합(A6~A9·B1~B3·B5~B7·C) — 실측 결과 (`notes/decisions.md` G21~G30)

`app/src/game/balance.ts`·`engine.ts`·`sim/run.ts`를 실제로 컴파일·구동해(tsx)
검증했다. σ=1(전량 즉시매각) 정책이 v0.1 엔진(수정 전 상수 그대로) 위에서
review-r1.md의 폭주 주장을 재현하는지부터 확인했다:

| t | σ=1 실측(수정 전) | σ=1(`MAX_GEAR_LEVEL=16` 적용 후) | σ<1(기본 정책, 참고) |
| --- | --- | --- | --- |
| 1h | 79,191/s | 79,191/s | ≈4,453/s |
| 6h | 1,807,344/s | 263,604/s | ≈5,209/s(정체) |
| 24h | 5,137,451/s | 287,954/s | ≈5,209/s |
| 72h | 8,710,146/s | 304,187/s | ≈5,209/s |
| 168h | 14,459,140/s | 318,391/s | ≈5,209/s |

`review-r1.md`의 σ=1 수치(6h 1,820,600 / 24h 5,137,500 / 72h 8,710,100)와 거의
정확히 일치해 독립 재현에 성공했다. `MAX_GEAR_LEVEL=16`(신설, `balance.ts`에
실코드 반영) 적용 후 168시간 폭주가 **45배 감소**했고, v0.1 기본 정책(σ<1)의
`pnpm --filter relic-king sim -- --hours 24` 결과는 **수정 전후 바이트 단위로
동일**함을 확인했다(자산 133억 ₩·순위 1위·발굴력 976/s·엔딩 1시간 0분, 전부
일치) — 안전장치가 이미 검증된 v0.1 밸런스를 전혀 건드리지 않는다.

세부 유도 과정·나머지 게이트(A8 통화 성장 캡, A9 유한재고 소진, B1 박물관,
B3 로컬 시세, B5 원정 가동률, B6 도난, B7 급여, C 비용곡선 7종)는
`notes/decisions.md` G21~G30에 실측 방법론과 함께 기록했다.

## 9. 수용 기준 자기 점검표 (`prompts/v0.2-deepening.md` §6)

> **(재채점 — B13, 2026-09-18 리뷰 2회차 대응)** 1회차에서 이미 ✅ 3개가 거짓으로
> 적발됐는데(§3·§5·§10 계열) 다음 라운드까지 고쳐지지 않은 채로 남아 있었다.
> **거짓 ✅는 이 프로젝트에서 가장 위험한 결함이다** — 검증됐다고 믿고 구현에
> 들어가게 만든다. 아래는 이번 실행(B5~B13)이 실제로 고친 것만 반영해 다시
> ✅/⚠/❌로 채점한다. ⚠는 "부분적으로 성립"이지 "괜찮다"가 아니다.

| # | 수용 기준 | 상태 | 근거 |
| --- | --- | --- | --- |
| 1 | 결정 게이트가 전부 `notes/decisions.md`에 버린 선택지·이유 포함해 기록 | ✅ | `notes/decisions.md` G1~G50(원래 11개에서 5라운드에 걸쳐 확장), 게이트마다 "버린 선택지" 절 존재. 이번 라운드가 새로 추가한 G51 이후도 같은 형식을 따른다 |
| 2 | 플레이스홀더 0("TBD"·"추후"·"적절히"·"예시" 없음) | ✅(이번 라운드에 재검증) | **직전 상태는 거짓이었다** — `spec.md` §12.2가 "라이벌 박물관 건립 로직을 후속 실행에서 구체화한다"는 떠넘기기를 갖고 있었고, `notes/world-map.md`의 9거점 `eras`(층별 시대 라벨)가 통째로 비어 있었다(B10). 이번 실행에서 §12.2는 "라이벌은 v0.2에서 박물관을 짓지 않는다"로 **명시적으로 범위를 닫았고**, 9거점 `eras`는 §1.1에 108개(9×12) 라벨을 **실제로 채웠다**(`notes/world-map.md`). 남은 잠정값(`NEW_SITE_DEFS`의 `tierBias`)은 "TBD" 류가 아니라 구체적 숫자이고 데이터 파이프라인에서 조정될 수 있음을 이미 명시해 뒀다 — 이건 §1.1이 설명하듯 상쇄 성질(`layerExpectedValue` 분자·분모 양쪽) 때문에 화폐 계산을 무효화하지 않는 추정치이지, 값 자체가 없는 진짜 플레이스홀더가 아니다 |
| 3 | 모든 수치가 수식 또는 상수로 존재, 기획 문서 숫자와 `balance.ts` 상수명이 1:1 대응 | ✅(이번 라운드에 재검증) | `notes/economy.md` §8, `notes/world-map.md` §9, `notes/staff.md` §10, `spec.md` §14에 상수 총람. **직전 상태도 거짓이었다** — `notes/mda.md` §1.2 전체가 v0.1 초판 추정치(층비용·드랍·클릭·콤보·제보 주기·가중·라이벌 수)로 최대 수백 배 어긋나 있었고, `notes/economy.md` §3.2 말미가 "`REGIONAL_PRICE_MULT_MIN/MAX`(0.90/1.26)는 재계산하지 않는다"고 쓴 채 같은 문서 §1.1·§8은 이미 0.60/1.40이라 **문서 내부에서 모순**이었으며, 이 `eval.md` 자신이 §8-#7에서 `ARBITRAGE_MAX_SPREAD_RATIO=2.33`, §9-#9(이 표)에서 `=1.40`이라고 **서로 다른 값**을 나란히 쓰고 있었다. 전부 이번 실행에서 정정했다 |
| 4 | 척추 5개 항목별 판정표(유지/수정/폐기 + 대가) | ✅ | `notes/decisions.md` 말미 + 아래 §10(이 문서 자체 판정표). 척추 3번(§10 행 3)은 G39/A1(도난·자동매각 온라인 전용화)이 이미 실측으로 닫았다 — 1회차에 "거짓"으로 적발됐던 판정이 지금은 유효하다. 이번 라운드에서 행 2의 "박물관·경매장·거래소·암시장 4개 신규 채널"이라는 오기(거래소는 G15로 이미 없다)만 정정했다 |
| 5 | 경제 모델이 소스·싱크 전량을 덮는다 | ✅(재검증) | `notes/economy.md` §1.5(소스 총괄표, K13 위 이월 크레딧 행 포함)·§2(싱크 전량표, K13 시즌 정산 소각 포함, 거점 `unlockCost`·발굴단 해금비 합계 포함) — `notes/decisions.md` G30/C + **G46/A9**(시즌 롤오버 화폐 이동 2행, A10이 지적했던 누락을 닫음). 보험금 행은 G50/C#1로 보험 자체가 삭제되며 함께 빠졌다(더 이상 소스가 아니다) |
| 6 | 조작 단계 수 표, 3 넘는 항목 없음 | ✅ | `notes/ux-v02.md` §2(18종, 재계상 후 정직한 숫자 + 재설계 사례의 이유·완화책 포함) |
| 7 | 데이터 항목 규격에 `source` 필수 필드, T4 검증 규칙 명시 | ✅ | `notes/artifacts-dataset.md` §5. 이번 라운드에 "`pending` 항목은 드랍 풀에서 제외" 규칙과 내력 2~3문장 규격을 §5에 더했다(B8) |
| 8 | eval.md에 신규 측정 기준·목표치·측정 방법(시뮬 인자·스모크 시나리오) | ✅(이 절) | 위 §8 표. 단, **측정 자체는 구현 후 실행 필요** — 방법은 명시했으나 실측치는 없다(방치형은 눈으로 봐서 알 수 없다는 v0.1 교훈 그대로 적용) |
| 9 | "안 건드렸을 때"·"최적 조작" 두 곡선의 목표 간격이 시스템마다 숫자로 정의 | ⚠ **부분** | **직전 상태는 거짓이었다** — 유일한 근거가 G26/B3로 이미 삭제된 문장("`ARBITRAGE_MAX_SPREAD_RATIO=1.40`이 20~40% 밴드와 정확히 일치")이었다(현재 값은 2.33, 밴드와 무관). 정정한 근거: **지역 시세**(보유 base 3곳 중 최고가 자동 선택, `E[max of 3]=1.20` → +20%, G26/B3·G48/B3), **제보 집중 굴착**(`TIP_PLAYER_HIT` 28%→60%, 원정비 2배 대가, G45/A8), **경매장**(등급별 실제배율, 직접매각 대비 +5.8%~+28.8%, G50/C#6)은 숫자가 있다. 그러나 **박물관·발굴단 신규 배치·감정소·암시장**에는 "하루 개입 시 몇 % 더 버는가"에 해당하는 숫자가 없다 — G3가 요구한 "시스템마다"는 아직 전수 충족이 아니다 |
| 10 | 각 신규 시스템이 재미 정의 3문장 중 어디에 기여하는지 명시, 미기여 시스템은 자름 | ✅(이번 라운드에 재검증) | `notes/mda.md` §10.2(6종 판정 — **직전 상태는 거짓이었다**: "7종 전부 판정, 자른 시스템 없음"이라고 썼지만 실제로는 거래소가 이미 잘려 6행뿐이었다. 정정한 근거: mda §10.2는 거래소가 3문장/Dynamics 판정 자체는 통과했으나 **산수 판정**(실현 배율이 직접매각보다 항상 낮음)으로 별도로 잘렸다고 정확히 기록한다 — "미기여라서 잘랐다"가 아니라 "기여해도 산수로 죽으면 잘랐다"는 더 정확한 서술이다) |

## 10. 척추 5개 판정표 (이 문서의 검증 관점)

`notes/decisions.md` 말미에 게이트 근거 중심의 판정표가 있다. 아래는 같은 판정을
**측정 가능성** 관점에서 다시 정리한 것이다 — "유지된다고 주장하는가"가 아니라
"유지되는지 무엇으로 확인하는가"를 덧붙인다.

| 척추 | 판정 | 대가(수정·폐기 시에만 발생, 이번엔 전부 유지라 대가는 "측정 부담"으로 대체) | 확인 방법 |
| --- | --- | --- | --- |
| 1. 현실 개체수 = 재고 | **유지** | 480종 검증 파이프라인 운영 부담(`notes/mda.md` 리스크 7.8) | §8-#12(`validate-artifacts.mjs`), `notes/artifacts-dataset.md` §9의 거점당 쿼터 재정리 |
| 2. 깊이는 돈을 벌어주지 않는다 | **유지(조건부 확장)** | 박물관·경매장·암시장 3개 신규 채널(정정 — 거래소는 G15/A4로 v0.2에 없다) 전부를 이 원칙 아래로 편입시키는 검증 부담 | §8-#1·#2·#3(24시간 증가율·시즌말 비율·박물관 캡), `notes/economy.md` §7 항목별 판정 |
| 3. 영구 상실은 접속해 그 자리에 있었을 때만 | **유지** | 도난·봉인 보관·`PENDING_CAP` 오버플로 3경로 모두 온라인 전용/무손실 야적으로 닫았다(G39/A1) — 실측으로 재검증해야 하는 부담은 매 시뮬마다 남는다 | §8-#5(도난율), 오프라인 시뮬에서 T3·T4 상실 0건(§3, v0.1 방식 재사용), `spec.md` §2.4·§2.7·§9.2·§9.4 |
| 4. 클릭은 언제나 선택 | **유지, 변경 없음** | 없음 — 이번 게이트 어느 것도 클릭 규칙을 건드리지 않았다(`notes/decisions.md` 말미와 동일 판정) | v0.1 §2의 클릭 상한 측정을 그대로 재사용, v0.2 신규 액션에도 클릭 필수 지점이 없는지 스모크로 재확인 |
| 5. 규칙은 전부 공개한다 | **유지 + 적용 범위 확장** | 공개해야 할 규칙 수가 늘어난 만큼 "규칙 화면"이 커지는 UX 부담(`notes/ux-v02.md` §1.2가 "한 번의 조작" 층으로 격상했고, §1.4가 헤더 상시 아이콘으로 진입 경로를 확정했다) | 규칙 화면에 3축 순위 공식·NPC 시장조성 로직·제보/급파 규칙·라이벌 원정 ETA 공개·검증 대기 배지·도난율·회수 성공률이 전부 노출되는지 수동 체크리스트로 확인(§5 확장). 거래소는 G15/A4로 v0.4 이연돼 이 목록에서 빠졌다. 보험 관련 공개 항목(G9가 약속했던 보험료·지급률)은 G50/C#1로 보험 자체가 삭제되며 함께 빠졌다 |

## 11. 측정 인프라 — 구현 시 추가해야 할 것 (요약)

이 절은 §8의 "⏳ 미구현" 항목들이 요구하는 구현 작업을 한곳에 모은 것이다(설계
문서가 구현자에게 남기는 체크리스트, 플레이스홀더가 아니라 **다음 단계의 명시적
할 일**):

- [ ] `pnpm --filter relic-king sim`에 `--sell-profile`, `--hours`(이미 있음, 336·720
      지원 확인), `M(t)` 시간별 로그 출력 추가.
- [ ] `scripts/validate-artifacts.mjs` 구현(`notes/artifacts-dataset.md` §7).
- [ ] `tests/e2e/smoke.mjs`에 `--scenario first-session` 추가, 4개 체크포인트
      도달 시각 기록.
- [ ] 시뮬에 박물관·경매장·암시장·스텝 상태를 강제 설정하는 디버그 플래그
      추가(§8-#3·#4 측정에 필요, 정정 — 거래소는 G15/A4로 v0.2에 없다).
- [ ] **(신설, B13)** `sim/run.ts`의 `act()`가 2초 스텝마다 개입한다(시간당
      1,800회) — §8-#8·#9가 요구하는 "방치 vs 개입" 비교를 현재 하네스로는
      측정할 수 없다. **관여 예산을 강제하는 정책 함수**를 추가한다:
      하루 `DAILY_ENGAGEMENT_MIN_COUNT~MAX_COUNT`(2~3회) ×
      `DAILY_ENGAGEMENT_SESSION_SECONDS`(180초) 이내로만 `act()`가 개입하도록
      제한하는 정책(예: `--engagement-policy realistic`)을 시뮬에 추가하고,
      기존 정책은 `--engagement-policy unlimited`로 이름 붙여 구분한다.
      이 정책 함수가 있어야 "손대지 않았을 때"(하루 0회)·"현실적 관여"(하루
      2~3회×180초)·"최적 조작"(무제한) 세 곡선을 비교할 수 있다 — 지금은
      사실상 세 번째(무제한에 가까운 2초 간격 개입)만 측정 가능하다.

---

# v0.2 구현 2단계(세계지도·거점·원정·스텝) — 실측

> `prompts/v0.2-deepening.md` 5단계 실행 중 2단계(세계지도·거점·원정·스텝)의
> 실측 결과. 1단계(엔진 코어)가 끝난 상태 위에 12거점 데이터·거리 행렬·원정
> 회차제·발굴단 다중화·스텝 3직군·거점별 시세·세계지도 렌더를 실제로
> 구현하고 측정했다. 판단 근거는 `notes/decisions.md` G52 이후 참조.

## 12. 자동 검증(2단계)

| 항목 | 방법 | 결과 |
| --- | --- | --- |
| 타입체크 + 빌드 | `pnpm --filter relic-king build` | ✅ 무오류 |
| 헤드리스 시뮬(방치 기준선) | `pnpm --filter relic-king sim --hours 24` | ✅ 1단계와 바이트 단위 동일(새 시스템이 기존 경로에 안 끼어든다) |
| 실시간 스모크 | `node tests/e2e/smoke.mjs --seconds 30` | ✅ PASS |
| 세이브 마이그레이션 v1→v2→v3 | `pnpm --filter relic-king qa:migration` | ✅ 31개 항목 전부 통과(기존 17개 + 2단계 신규 필드 14개) |
| 시즌 롤오버(발굴단·스텝 리셋 포함) | `pnpm --filter relic-king qa:season` | ✅ 17개 항목 전부 통과 |
| **원정 회차제 오프라인 적분 동등성(신설)** | `pnpm --filter relic-king qa:expedition`(1초 vs 10초 스텝) | ✅ 11개 항목 전부 통과 — 거점 층·layerProgress는 정확히 일치, 드랍 횟수·funds는 **거의** 일치(3단계에서 허용치로 재조정 — `notes/decisions.md` G53.10: china가 처음 실제 드랍 가능해지며 극히 드문 층 경계 겹침 케이스가 실측으로 드러났다, 1,684회 드랍 중 1회·funds 0.1% 이내). `dropProgress` 잔량은 여전히 단언하지 않는다(§12.2 참조) |
| 거리 행렬 정확성 | `distanceKm()` 6개 표본쌍을 world-map.md §2 표와 대조 | ✅ 전부 1km 미만 오차(반올림) — 경주-교토 601km, 쿠스코-시안 17,707km 등 |
| 거점별 시세 스프레드 상한 | 80일치 6시간 간격 × 10카테고리 전수 스캔, `hi/lo` 최댓값 | ✅ 기본 클램프 2.3333 = `ARBITRAGE_MAX_SPREAD_RATIO`와 정확히 일치(이론상 상한에 닿는다). 원거리 확장 클램프(1.60) 포함 시 최댓값 2.4130으로 이론 상한(2.667) 이내 |
| 원정 미스헵 확률 상한 | 12×12 거점쌍 전수 계산 | ✅ 최댓값 19.7%(쿠스코↔시안 17,707km) — `EXPEDITION_MISHAP_CHANCE_CAP`(25%) 이내, 실제로는 캡에 닿지 않는다(최장거리에서도) |
| 세계 원장 보존(발굴단 다중화 + 레거시 동시 가동) | 72시간 시뮬, 2팀(egypt·rome) + 레거시(korea) 동시 가동 | ✅ 60종 전수 `owners+remaining==total`, T4 중복 소유 0건 |

## 12.1 발견한 버그와 수정 — 드랍 귀속이 발굴력 비중과 무관하게 쏠렸다

**증상**: 레거시 단독 발굴(korea)과 발굴단(같은 korea 목표)을 동시에 가동하면,
초기 구현에서는 189개 드랍 **전부**가 발굴단 기여 없이 레거시로만 귀속됐다
(단장 급여가 한 번도 원천징수되지 않는다는 뜻).

**원인**: `dropThreshold()`의 하한(`DROP_INTERVAL_FLOOR_SECONDS × dig`)을 각
호출자(레거시 vs 발굴단)가 **자기 자신의 발굴력**으로 계산해, 같은
SiteProgress를 공유하는데도 두 호출자의 문턱값이 달랐다. 매 스텝 레거시가
먼저 검사되는 코드 순서 + `DROP_INTERVAL_FLOOR_SECONDS × 합산발굴력`이 스텝
크기(1초)의 정수 배로 정확히 맞아떨어지는 구조가 겹쳐, 부동소수점 반올림
방향이 사실상 항상 "레거시가 막 채웠다"로 떨어졌다 — 우연이 아니라 이 조건
아래서는 매번 재현됐다.

**수정**: 진척 누적(`addSiteProgress`)과 드랍 판정(`drainSiteDrops`)을
분리했다. 그 거점을 노리는 모든 기여자(레거시 + 발굴단들)가 먼저 각자의
진척을 더하고, 드랍 문턱 검사는 거점당 **한 번만** 기여자 합산 발굴력으로
수행하며, 문턱을 넘길 때마다 **기여자별 발굴력 비중으로 가중 추첨**해 그
드랍의 단장을 정한다. 재검증: korea에 레거시(d≈1)+발굴단(d≈6.85)을 400초
동안 같이 가동하면 22회 드랍 중 16회(≈73%, 이론 비중 ≈87%와 같은 자릿수)가
발굴단으로 귀속됐다. `notes/decisions.md` G52.1 참조.

## 12.2 원정 회차제 오프라인 적분 — dropProgress 잔량은 왜 단언하지 않는가

`qa_expedition.ts` 실측(korea→china, 538회 드랍)에서 1초 스텝과 10초 스텝의
최종 `dropProgress` **잔량**은 최대 수백 단위까지 갈렸다(예: 280.5 vs 489.5).
원인은 §12.1과 같은 구조다 — 층 돌파(`layerProgress`)는 한 시즌에 몇 번 안
일어나 이 효과가 드러나지 않지만, 드랍 문턱은 훨씬 자주 채워지고 비워지므로
"레거시 vs 발굴단 중 누가 이번 문턱을 넘겼는가"에 따라 잔량이 갈리는 효과가
누적된다. **플레이어에게 보이는 값(드랍 횟수·거점 층·funds)은 스텝 크기와
완전히 일치한다** — `dropProgress`는 UI에 노출되지 않는 내부 누산기이고,
기존 `sim/run.ts`의 오프라인 적분 검증도 애초에 이 값을 비교 대상으로 삼지
않는다(드랍 횟수·층만 비교). 새 기준을 만들지 않고 기존 선례를 그대로
따랐다.

## 12.3 12거점 도입이 엔딩 도달 가능성을 실제로 개선하는가 — 실측(작업 지시 요구사항)

1단계 보고(G51.1)는 "엔딩 도달 불가"의 원인을 "데이터셋이 60종·3거점이라서"로
지목하며 12거점이 들어오면 개선되는지 시뮬로 확인하라고 넘겼다. 실측 결과는
**부분적으로만 개선된다** — 그리고 그 이유가 중요하다.

**핵심 사실**: 신규 9거점(그리스·튀르키예·이스라엘·인도·중국·이라크·일본·
멕시코·페루)은 `app/src/game/artifacts.ts`에 **유물 데이터가 0종**이다(480종
데이터 파이프라인은 4단계 범위 — spec.md §9.2 G7, 이번 2단계는 손대지 않았다).
`candidates()`가 그 9거점 어디서도 빈 풀만 반환하므로, 그 거점들은 **층·진척은
정상적으로 오르지만 드랍은 구조적으로 영원히 0건**이다 — 원정을 보내도
도감에 한 종도 추가되지 않는다. 실측(`artifactsOf()` 직접 호출):

| 거점 | 종 수 | | 거점 | 종 수 |
| --- | --- | --- | --- | --- |
| korea | 21 | | china | **0** |
| egypt | 20 | | turkey | **0** |
| rome | 19 | | iraq / india / japan / mexico / peru / israel / greece | **0** |

그래서 "12거점이 엔딩을 도와주는" 유일한 경로는 **원래 있던 3거점(korea·
egypt·rome)에 발굴단을 병렬로 더 투입해 합산 발굴력을 늘리는 것**뿐이다 —
새 거점 자체가 주는 도감 기여는 이번 단계에서 0이다. 이걸 72시간·168시간
시뮬로 직접 비교했다(레거시 투자 정책은 `sim/run.ts`의 `act()`와 동일 로직,
발굴단 쪽은 최대 4팀을 korea/egypt/rome에 분산 배치 + 루틴 상시 가동):

| 정책 | 72h 도감 | 168h 도감 | 엔딩 |
| --- | --- | --- | --- |
| 레거시 단독 발굴만(1단계와 동일 조건) | 19/60 (31.7%) | 19/60 (31.7%, 변화 없음) | 미달성 |
| **+ 발굴단 병렬 투입(korea/egypt/rome로)** | **35/60 (58.3%)** | 35/60 (58.3%, 변화 없음) | 미달성(75% 목표에 못 미침) |

두 정책 모두 **72시간 이후로는 전혀 진행되지 않는다** — 1단계 G51.1이 이미
지목한 원인(종당 유한 재고가 몇 시간 안에 소진되고, T1/T2 리젠 시스템이 아직
없어 회복 경로가 없다)이 그대로 재현된다. 발굴단은 재고 소진 **속도**를
크게 앞당길 뿐(31.7%→58.3%, +26.6%p) 재고 **총량**을 늘리지 못한다 — 그래서
도감 목표(75%)에는 여전히 못 미친다.

**결론(보고 대상)**: 2단계(세계지도·원정·스텝)는 "엔딩 도달 불가"를 완전히
해결하지 못한다 — 3거점·60종이라는 데이터 자체의 한계는 그대로다. 다만
발굴단 다중화는 **주어진 데이터 안에서 도달 가능한 상한**(60종 중 실제로
모을 수 있는 최대치)에 훨씬 빨리 도달하게 해 준다는 점에서 유의미한 개선이다.
완전한 해결(9거점에 실제 유물 데이터를 채워 480종 파이프라인을 완성하는 것,
그리고 T1/T2 리젠 시스템 도입)은 여전히 4단계(데이터 확장) 이후의 몫이다.

## 12.4 §8 측정 기준 갱신 — 이번 단계에서 실제로 측정 가능해진 항목

| # | 기준 | 이전 상태 | 2단계 이후 |
| --- | --- | --- | --- |
| 6 | 원정 미스헵 확률 상한 | ⏳ 미구현 | ✅ 실측 완료(§12 표) — 최댓값 19.7% ≤ 25% |
| 7 | 지역 차익 상한(`ARBITRAGE_MAX_SPREAD_RATIO`) | ⏳ 미구현 | ✅ 실측 완료(§12 표) — 2.3333 = 이론값과 정확히 일치 |
| 8 | 관여 예산 — 손대지 않았을 때 손실 0 | ⏳ 미구현 | ✅ 부분 확인 — 원정비 후불 원천징수 구조상 파견 자체는 자금 게이트가 없다(G10 "파산 불가"). 단, 귀환 정산 시 `w.funds`가 일시적으로 음수가 될 수 있다는 설계 판단은 `notes/decisions.md` G52.2 참조(구매 액션은 전부 `funds>=cost` 게이트가 있어 음수 자체가 추가 손실을 만들지는 않는다) |

나머지 항목(1~5, 9~13)은 박물관·경매장·거래소·암시장·데이터 파이프라인 등
이번 2단계 범위 밖 시스템에 의존해 여전히 ⏳ 미구현이다.

## 12.5 알려진 한계(2단계, 보고 대상)

- **9거점 무데이터**: §12.3 참조. 원정·거점 확장은 전부 동작하지만 신규
  9거점에서는 드랍이 나오지 않는다.
- **거점별 시세 정보 비대칭의 공급 충격 항이 상시 0**: `market.ts`의
  `supplyShock()`는 T1/T2 리젠(G6) 발생 시각을 받아야 값을 내는데, 리젠
  시스템 자체가 아직 없다(1단계 G51.5가 이미 범위 밖으로 넘긴 것과 같은
  이유). 공식은 준비돼 있고 리젠이 붙으면 바로 동작한다.
- **관장·경매관장 고용 액션 없음**: 박물관·경매장 시설이 없어(3단계 이후
  범위) 스탯·급여 공식(`staff.ts`)은 구현했지만 실제 고용 액션은 단장만
  있다. 관장·경매관장은 시설 건립과 함께 고용이 자연스러워진다는 판단
  (`notes/decisions.md` G52.4).
- **발굴단 "본거지" 개념 단순화**: 발굴단이 이동 거리를 계산하는 기준점을
  "최초로 확정된 base"로 고정했다(팀별 본거지 선택 UI는 5단계 몫,
  `notes/decisions.md` G52.3).
- **CANDIDATE_STAT 해시에 후보 슬롯 인덱스 추가**: staff.md §4의 해시 식에는
  슬롯 구분 항이 없어 3후보가 전부 같은 스탯이 되는 공백이 있었다 — 해시
  키에 슬롯 인덱스를 추가해 메웠다(`notes/decisions.md` G52.5).
- **세계지도 렌더는 해안선 벡터 없이 도트+연결선**: 외부 네트워크가 막혀
  있어 Natural Earth 빌드 파이프라인을 만들지 않았다 — 2단계 작업 지시가
  명시한 대체안(거점 노드 + 연결선 + 위경도 그리드)을 그대로 따랐다
  (`app/src/render/worldmap.ts`). React UI에는 아직 배선하지 않았다(5단계
  몫 — 렌더 모듈 자체는 완성, 빌드도 통과한다).

## 13. 자동 검증(3단계 — 유물 데이터·제보 v0.2·라이벌 v0.2)

`notes/decisions.md` G53 참조. 이번 단계의 최우선 과제는 G52.10이 남긴
"신규 9거점 무데이터"였다 — 아래가 그 해소 실측이다.

### 13.1 유물 데이터셋 — 목표 480종 중 280종(58%), T4는 12거점 전부 확보

```
pnpm --filter relic-king qa:artifacts
```

✅ 전체 통과(0건 실패). 검사 항목: id 중복, 필수 필드(name/era/origin/holder/
note/tier/valueFactor/shape/palette/minLayer), `source` 개수(T0~T3 ≥1, T4 ≥2
독립 출처), `sourceStatus` 값 유효성, 거점당 T4 ≤1(쿼터). 상세 거점별 표·T4
목록은 `notes/decisions.md` G53.2 참조. 요약:

| 지표 | 값 |
| --- | --- |
| 총 종수 | 280 / 480(58%) |
| verified(드랍 풀 포함) | 182 |
| pending(드랍 풀 제외, G-B8) | 98 |
| T4(유일) 쿼터 위반 | 0건(12거점 전부 정확히 1종) |
| T4 확보 거점 | 12/12(페루 포함 — 사전에 가장 어려울 것으로 예상됐던 거점) |

### 13.2 필수 명령 실측 (작업 지시 "검증" 절)

| 명령 | 결과 |
| --- | --- |
| `pnpm --filter relic-king build` | ✅ 무오류(`tsc` + `vite build`) |
| `pnpm --filter relic-king sim --hours 24` | ✅ 완주, 아래 §13.3 표 |
| `pnpm --filter relic-king sim --hours 168` | ✅ 완주, 아래 §13.3 표 |
| `pnpm --filter relic-king qa:migration` | ✅ 31/31 통과(v1→v2→v3→v4 체인 + v2→v4 + v3→v4 단독 구간) |
| `pnpm --filter relic-king qa:expedition` | ✅ 11/11 통과(오프라인 적분 동등성, 스텝 크기 무관) |
| `node tests/e2e/smoke.mjs --seconds 30` | ✅ PASS(스크린샷 8장, 콘솔 오류 0건) |
| `pnpm --filter relic-king qa:artifacts`(3단계 신설) | ✅ 0건 실패(§13.1) |

세계 원장 보존(`획득 합 + 잔여 == 총량`)·T4 중복 소유 0건·오프라인 T3·T4
상실 0건은 `sim --hours 24`·`168` 양쪽에서 전부 유지된다(아래 표의 "원장
보존"·"오프라인 T3·T4 상실" 줄).

### 13.3 엔딩 도달 여부·도감 완주율 — 실측(작업 지시 "반드시 측정해 보고")

**(a) 공식 방치 기준선**(`sim/run.ts`, 레거시 단독 발굴만 — 1단계부터
있던 정책, 과거 단계와의 비교 가능성을 위해 이번에도 건드리지 않았다):

| 지표 | 24시간 | 168시간 |
| --- | --- | --- |
| 첫 유일(T4) | 1시간 20분 | 1시간 20분 |
| 엔딩 | 미달성 | 미달성 |
| 자산 | 263억 ₩(순위 1위) | 263억 ₩(순위 1위) |
| 도감 | 14/182 (8%) | 14/182 (8%) |
| 발굴력 | 3,067/s | 3,161/s |

**이 정책만으로는 24h 이후 도감이 사실상 멈춘다**(14종에서 168h 동안 변화 없음) —
1단계 G51.1이 지목한 원인(종당 유한 재고가 몇 시간 안에 소진되고 T1/T2
리젠이 아직 없다)이 그대로 재현된다. 레거시 단독 발굴은 `MAX_OWNED_SITES=3`
(base 슬롯)에 묶여 애초에 3거점(korea·egypt·rome, 그나마 로마는 24h 안에
해금 못함) 밖으로 나가지 못한다 — **신규 9거점 데이터를 채웠어도 이 정책으로는
그 효과가 전혀 보이지 않는다.** 이게 데이터 문제가 아니라 측정 정책의
구조적 한계라는 걸 확인하기 위해 (b)를 추가로 쟀다.

**(b) 발굴단을 실제로 12거점에 분산 파견하는 진단 스크립트**
(`sim/qa_endgame.ts`, 신설 — 필수 검증 목록엔 없다, 참고용):

```
pnpm --filter relic-king exec tsx src/sim/qa_endgame.ts --hours 336
```

| 시점 | 도감 | 방문 거점 | 엔딩 |
| --- | --- | --- | --- |
| 24h | 35/182 (19.2%) | 3/12 | 미달성 |
| 72h | 51/182 (28.0%) | 4/12 | 미달성 |
| 168h | 60/182 (33.0%) | 5/12 | 미달성 |
| 336h | 60/182 (33.0%, 168h 이후 변화 없음) | 5/12 | 미달성 |

**핵심 증거**: 336시간 최종 상태에서 발굴단이 실제로 도달한 거점은 검증된
종을 거의 다 또는 완전히 소진했다 — 그리스 8/8(100%), 튀르키예 8/8(100%),
이집트 14/18(78%), 로마 14/19(74%), 한반도(레거시) 16/22(73%) — **이번
단계가 채운 데이터는 완전히 동작한다**는 직접적 증거다(특히 그리스·튀르키예
100% 완주는 "9거점 드랍 0건"이던 2단계 상태에서 가장 극적으로 개선된
지점이다). 도달하지 못한 7개 거점(이스라엘·인도·중국·이라크·일본·멕시코·
페루)은 이번 진단 런의 발굴단 슬롯 수(최종 3팀 — 4번째 슬롯 해금 비용
8억 ₩을 감당하지 못했다)와 재배치 정책(거점이 "완전히" 비워지기 전엔
다른 곳으로 옮기지 않는 단순 규칙)의 한계로 방문하지 못했을 뿐, 데이터
자체는 갖추고 있다(§13.1 표 — 예: 이스라엘 19종, 이라크 17종 모두 검증
완료). 168h 이후 도감이 정체되는 이유도 1단계 G51.1·2단계 G52.10과 동일하다
— 방문한 5개 거점의 검증 종이 사실상 소진돼 더 늘 방법이 없다(T1/T2 리젠
시스템 부재).

**결론(보고 대상)**: 3단계는 "9거점 무데이터"라는 2단계의 핵심 한계를
해소했다 — 신규 사이트에 도착한 발굴단은 정상적으로 드랍을 받는다. 다만
**엔딩(도감 75%) 자체는 이번 단계로도 아직 달성되지 않는다.** 남은 격차의
원인은 이번 단계 범위 밖의 두 가지다: (i) 목표 480종 중 280종(58%)만
채워 총 분모가 작고 T0~T1(대량 티어)의 미채움분이 크다, (ii) T1/T2 리젠
시스템(G6)이 아직 없어 방문한 거점도 결국 재고가 소진되면 더 늘지 않는다.
완전한 해결은 4단계(데이터 추가 확장) + 리젠 시스템 도입 둘 다 필요하다 —
2단계 G52.10의 결론과 같은 형태이지만, 막혀 있던 지점(9거점 자체가 드랍이
안 됨)은 이번 단계로 실제로 뚫렸다.

### 13.4 알려진 한계(3단계, 보고 대상)

- **목표 480종 중 280종(58%)만 채워졌다** — 실존이 확인되지 않은 항목은
  하나도 지어내지 않은 결과다(척추 1번). 특히 T0(흔함) 티어가 목표(300종)
  대비 111종(37%)으로 가장 크게 미달한다 — 일반 유형("동전", "화살촉" 류)도
  현존 수량을 개별적으로 확신할 수 있는 만큼만 넣었기 때문이다.
- **pending 98종은 드랍 풀 밖에 있다**(G-B8) — 검증(다음 네트워크 가능
  세션)이 끝나는 대로 `sourceStatus`만 바꾸면 자동 편입된다. 코드 변경은
  필요 없다.
- **라이벌 v0.2는 축소 구현**이다 — 완전한 팀 모델 대신 "홈 거점 연속
  원정비 원천징수 + 레이스 원거리 급파"로 핵심 요구(고정비·레이스 밖 T4
  불가·오프라인 T3+ 불가)만 만족시켰다(`notes/decisions.md` G53.6).
- **제보 v0.2 UI는 미배선**이다 — `focusDig`·`emergencyDispatch`·
  `rivalExpeditions` 전부 엔진 함수로 존재하지만 React 쪽에는 아직 연결하지
  않았다(작업 지시 "UI 배선은 최소만" — 5단계 몫).
- **era↔eras 정합 검사는 저신호 휴리스틱**이다(`notes/decisions.md` G53.8) —
  이집트·로마처럼 "몇 세기" 표기를 쓰는 거점에서 대량 오탐이 난다. 실제
  오류 방지는 거점별 독립 수집 구조가 담당한다.

## 14. 자동 검증(4단계 — 시설과 시장: 감정소 확장·보관소·박물관·경매장·암시장·도난)

`notes/decisions.md` G54 참조. 이번 단계는 3단계까지 상수만 선언되고 로직에
전혀 연결되지 않았던 시설 축(감정소 해금 게이트·보존 상태→평가액·박물관·
경매장·암시장·도난)을 실제로 배선했다. 새 감사 스크립트
`app/src/sim/qa_economy.ts`(`pnpm --filter relic-king qa:economy`)를 만들어
채널별 실현 배율·무위험 차익·박물관 캡·통화 성장·도난 회수기간·감정 게이트를
전부 실측했다.

### 14.1 필수 명령 실측

| 명령 | 결과 |
| --- | --- |
| `pnpm --filter relic-king build` | ✅ 무오류(`tsc` + `vite build`) |
| `pnpm --filter relic-king sim --hours 24` | ✅ 완주, 원장 보존·오프라인 적분·오프라인 T3·T4 상실 0건 전부 유지 |
| `pnpm --filter relic-king sim --hours 168` | ✅ 완주, 동일 |
| `pnpm --filter relic-king qa:migration` | ✅ 44/44 통과(v1→v5 체인 + v2→v5·v3→v5·v4→v5 단독 구간, 4단계에서 v4→v5 구간 신설) |
| `pnpm --filter relic-king qa:expedition` | ✅ 11/11 통과(오프라인 적분 동등성, 시설 틱을 추가한 뒤에도 유지) |
| `pnpm --filter relic-king qa:artifacts` | ✅ 0건 실패(이번 단계는 데이터셋을 건드리지 않았다) |
| `node tests/e2e/smoke.mjs --seconds 30` | ✅ PASS |
| `pnpm --filter relic-king qa:economy`(4단계 신설) | ✅ 29/29 통과(아래 §14.2~§14.6) |

### 14.2 채널별 실현 배율(무위험 차익 검산 포함)

**직접매각** — 보유 base 1곳 ×1.155, 3곳(최댓값 자동 라우팅, G-B3) ×1.376.
둘 다 `REGIONAL_PRICE_MULT` 범위(0.60~1.40) 안이고, 3곳이 1곳보다 항상
유리(라우팅이 실제로 작동한다).

**경매장**(협상력 0/100, 수수료 8% 차감 후, `LOCAL_PRICE_MULT` 제외 — 순수
등급·스탯 효과만):

| 등급 | 협상력 0 | 협상력 100 | 직접매각(1.0) 대비 |
| --- | --- | --- | --- |
| 1 | ×1.058 | ×1.196 | 항상 유리(+5.8%~) |
| 2 | ×1.135 | ×1.273 | 항상 유리 |
| 3 | ×1.211 | ×1.288 | 항상 유리 |
| 4 | ×1.288 | ×1.288(등급진행도 1.0에서 이미 상한) | 항상 유리 |

`staff.md` §3의 이론표와 정확히 일치(경매관장 미배정=협상력0 기준). 실제
엔진 경로(`listAtAuction` → 6시간 뒤 `settleAuctions` 자동 정산)도 별도
검증 — 상장 직후 vault에서 빠지고, 정산 시점 자금이 실제로 늘고, 매물
목록이 비는 것까지 확인했다.

**암시장**(매입만, 판매 채널 아님) — 일반 매물 매입가 추정가×0.75, 즉시
미감정매각가 추정가×0.7. 실제 엔진 호출(매입→즉시 미감정매각)로 검증한
순손익은 **-6.7%**(G42/A5의 이론치와 정확히 일치) — **무위험 차익 없음**.
장물(도난품) 매입은 평가액×0.32로 별도 기준.

### 14.3 무위험 차익 — 실제로 하나 찾아서 고쳤다(보고)

`qa:economy` 1차 실행에서 암시장 매입→즉시매각 순손익이 **+7.8%(이득)**로
나왔다 — G42/A5가 닫았다고 판정한 루프가 실제로는 열려 있었다. 원인은
**1단계부터 있던 기존 버그**: `blindSell`/`blindSellAll`이 공용 `settleSale()`을
그대로 썼는데, 이 함수는 직접매각처럼 `LOCAL_PRICE_MULT`(최대 1.60)를
곱한다. `spec.md` §11.1 채널표는 미감정 즉시매각의 "지역성"을 **"무관"**으로
명시하는데(직접매각·경매장만 "로컬"), 구현은 이 구분을 지키지 않고 있었다.
암시장 일반 매입가(0.75)는 이 "무관"(로컬 배율 없음) 전제로 `BLIND_SELL_RATE`
(0.7)보다 높게 잡은 값이라, 매각 쪽에 몰래 로컬 배율이 곱해지면 고배율
site·shape 조합을 노려 산 뒤 그 자리에서 팔기만 해도 이득이 났다 — 3단계
까지는 암시장 매입 기능 자체가 없어 이 결함을 이용할 진입 경로가 없었을
뿐이다.

**수정**: `settleSale`(로컬 배율 적용, 직접매각·자동매각 전용)과
`settleBlindSale`(로컬 배율 미적용, 미감정 즉시매각 전용)을 분리했다
(`engine.ts`, `notes/decisions.md` G54). 재검증 결과 -6.7%로 정정됨(위 §14.2).
**결론**: 화폐 복제 루프 직접 검산 요구사항이 실제로 결함 하나를 잡아냈다.

### 14.4 박물관 순수익 30% 캡

통제된 시나리오(등급4 박물관 + 최대 스탯 관장 + T3 이상 유물 10점 전시,
발굴력은 최소로 묶어 캡을 작게 만듦)로 캡이 실제로 걸리는지 확인했다:

```
발굴 잠재수입(1h 이동평균 근사, museumDigEma) 1,358.03₩/s → 캡 1,241,781₩/h
1시간 실측 자금 증가량 1,249,784₩(발굴력 최소라 대부분 박물관 순수익)
```

캡 여유 5% 이내로 들어온다 — G24의 "30% 캡"이 사문 조항이 아니라 실제로
작동하는 안전장치임을 확인했다. "1시간 이동평균"은 버퍼 대신 1차 저역통과
필터(`World.museumDigEma`)로 근사했다(exp(-dt/3600) 감쇠, 짧은 발굴 공백에
캡이 순간적으로 0이 되는 걸 막는다는 G24의 취지를 그대로 구현) — 정확한
슬라이딩 윈도우는 아니지만 닫힌 형태 근사라 스텝 크기에 안정적이다.

### 14.5 도난 72시간 회수기간 — 척추 3번 직접 검증

이번 단계 지시서가 명시적으로 경고한 항목("회수 기간 타이머가 오프라인
중에 흐르면 위반")을 정밀 검증했다:

| 시나리오 | 결과 |
| --- | --- |
| 오프라인 73시간(게임 시각) 경과 | `onlineElapsedSeconds` 그대로 0, 사건 미해결 유지 |
| 온라인 71시간 경과 | 아직 72h 미만이라 사건 진행 중(정상) |
| 온라인 누적 73시간 경과 | 회수기간 만료 → 소유권 이전 처리 |

72시간 카운트다운이 `World.onlineElapsedSeconds`(온라인일 때만 증가하는
별도 누산기, `world.t`와 분리)만 기준으로 삼도록 설계했다 — 오프라인 중에는
경계 자체가 넘어가지 않는다. 도난 **판정**(확률 롤)도 `spawnTip`과 동일한
`if(!offline)` 블록 안에서만 실행된다. 보험은 삭제된 그대로 다시 넣지
않았다(G-C1).

### 14.6 감정소 해금 티어 게이트 + 보존 상태(condition) 배선

- `APPRAISAL_UNLOCK_LAB_LEVEL=[1,1,2,3,4]`을 실제로 배선했다 — `lab<해금레벨`인
  항목은 감정 큐에서 카운트다운 없이 무기한 대기("봉인 보관", 파괴·강제매각
  없음, G39/A1 유지)하다가 감정소 레벨이 닿는 순간 다음 틱부터 정상 진행된다.
  실측(T3 항목, lab=1로 10시간 방치 → 전혀 안 줄어듦 → lab=3으로 올리자
  즉시 진행되어 감정 완료)으로 확인했다.
- 보존 상태(condition)를 평가액에 실제로 곱았다(1단계 G51.7이 미배선으로
  남긴 항목) — `artifacts.ts`가 이미 종별로 결정론 지터를 매긴
  `artifact.condition`을 감정 완료 시 그대로 초기값으로 쓰고,
  `CONDITION_VALUE_FACTOR`(0.4~1.6)를 평가액에 곱한 뒤, 습도 저하(매일 확률
  판정, 야적 시 2배)·복원(레벨에 비례한 주기·성공률)이 그 값을 계속 갱신한다.
  168시간 혼합 시뮬(§14.7)에서 vault의 condition 분포가 실제로 갈라지는 것을
  확인했다(습도·복원 둘 다 작동한다는 직접 증거).

### 14.7 168시간 혼합 시뮬(시설·시장 전부 사용) — 통화 성장

박물관·경매장·암시장·보관소 4축 업그레이드를 전부 공격적으로 쓰는
스트레스 정책으로 168시간을 돌렸다.

```
최종 자금 2.3억 ₩   누적 실현소득(근사) 325.2억 ₩
박물관 1관(등급1)   경매장 1곳   보관소 Lv.14   습도 Lv.13   복원 Lv.12   보안 Lv.12
세계 원장 보존 ✅(암시장 "blackmarket" 소유자 포함)   T4 중복 소유 0건
```

**통화 성장 측정 방법 보고**: `notes/economy.md` §5의 정본 스무딩 창(168h)은
이 스크립트 자체가 168시간 단일 런이라 데이터가 168개 쌓여야 값이 나와
끝내 한 번도 계산되지 않는다 — G43/A6가 이미 지적한 "`MATURE_PHASE_START_HOUR=48`인데
168h MA는 무의미했다"는 함정과 정확히 같은 모양이다. 24h/72h 창은 경매장의
6시간 일괄 정산 특성상(같은 타임스탬프에 낙찰 여러 건이 몰려 정산되는
것을 로그로 직접 확인) 버스트 노이즈에 취약해 참고치로만 쓰고, 대신
**분기별(42h) 성장률의 가속 여부**로 화폐 복제 루프 유무를 판정했다 —
3분기(84~126h) 대비 4분기(126~168h) 증가율 ×0.03(급감, 램프업이 끝나고
정체) — 만약 진짜 복제 루프였다면 뒤로 갈수록 계속 가속해야 하는데
그러지 않는다. 전반부 대비 후반부가 ×7.6배로 큰 건 두 개의 **이미 상한이
있는 램프**(장비 레벨이 `MAX_GEAR_LEVEL`에 닿기 전 지수 구간 — v0.1부터의
기존 곡선, `notes/economy.md` §0 σ=1 표와 같은 모양 / 복원이 vault를
condition 4로 밀어 올리는 램프, 이번 단계 신설)가 겹친 결과로, 4분기 급감이
이 해석을 뒷받침한다.

### 14.8 알려진 한계(4단계, 보고 대상)

- **박물관 관장 채용 UI·경매장 UI는 미배선**이다 — `buildMuseum`·
  `hireCurator`·`displayArtifact`·`buildAuctionHouse`·`hireAuctioneer`·
  `listAtAuction`·`buyBlackMarketListing` 전부 엔진 함수로 완성됐지만
  React 쪽(`useGame.ts`·`VaultView.tsx` 등)에는 연결하지 않았다 — 작업
  지시가 명시한 5단계(UI) 범위다. 기존 UI(발굴·소장고)는 그대로 빌드되고
  돈다(빌드·smoke 통과로 확인).
- **경매장 "n번째 건립비" 곡선은 문서 공백을 메운 설계 판단이다** —
  spec.md·economy.md 어디에도 `AUCTION_*_BUILD_COST_*` 상수가 없어(박물관만
  있음), `AUCTION_GRADE_COST_BASE/GROWTH`(경매장 등급 승급비와 같은 상수)를
  건립비 곡선에도 재사용했다(3천만/9천만/2.7억). 새 상수를 만들지 않고
  기존 값을 재해석한 것이라 되돌리기 쉽다 — 별도 건립비 상수가 정본으로
  확정되면 이 함수(`auctionHouseBuildCost`) 하나만 고치면 된다.
- **암시장 일반(미감정) 매물은 T0~T2로 제한했다**(`BLACK_MARKET_LOOSE_MAX_TIER`,
  4단계 신설 상수) — spec.md 어디에도 이 상한이 명시돼 있지 않지만, 일반
  매물도 세계 원장에서 실제로 빼내는 구조(`take`가 아니라 전용
  `takeForBlackMarket` 경로 — 소유자 문자열만 `"blackmarket"`)라 T3 이상을
  무제한으로 찍으면 척추 1번(유일성)이 깨진다. T3 이상은 오직 장물(도난)
  경로로만 암시장에 등장한다(§11.5 그대로).
- **도난 자동 회수 시도 주기는 설계 판단이다** — spec.md는 "72시간 회수
  기간 + 관장 SECURITY_SENSE 기반 회수 성공률"만 명시하고 시도 빈도를
  정하지 않아, 온라인 1시간마다 한 번씩 자동 시도하는 것으로 구현했다
  (플레이어 조작 없음, 방치형 원칙 유지). 관장이 없으면
  `THEFT_RECOVERY_BASE`(20%) 기준값을 그대로 쓴다.
- **박물관 순수익 30% 캡의 "1시간 이동평균"은 정확한 슬라이딩 윈도우가
  아니라 1차 저역통과 필터 근사다**(§14.4) — 버퍼 배열을 두지 않아 메모리
  비용이 없고 스텝 크기에 안정적이지만, spec 문구를 문자 그대로 구현한
  것은 아니다.
- **경매장은 등급4까지 협상력을 반영해도 등급4 자체가 상한(협상력100=등급4
  기본값과 동일)이라 사실상 협상력의 한계효용이 0에 수렴**한다(등급진행도
  식 자체의 성질, staff.md 원 공식 그대로 구현했을 뿐 이번 단계가 새로
  만든 결함은 아니다 — 보고만 한다).

## 15. 마무리 패스 — 1~5단계가 남긴 공백을 닫고 엔딩 도달을 실측(`notes/decisions.md` G56)

### 15.1 필수 명령 실측 전부 통과

```
cd /home/user/proto && pnpm --filter relic-king build            ✅
cd /home/user/proto && pnpm --filter relic-king sim --hours 24   ✅
cd /home/user/proto && pnpm --filter relic-king sim --hours 336  ✅ (226h32m에 엔딩 도달, 조기 종료)
cd /home/user/proto && pnpm --filter relic-king qa:migration     ✅ (v1~v5 → v6 전 구간)
cd /home/user/proto && pnpm --filter relic-king qa:expedition    ✅ (12/12, 실측 39초)
cd /home/user/proto && pnpm --filter relic-king qa:artifacts     ✅ (경고 1건은 실패로 안 친다, §4 참조)
cd /home/user/proto && pnpm --filter relic-king qa:economy       ✅ (30/30, 실측 33초)
cd /home/user/proto/projects/relic-king && node tests/e2e/smoke.mjs --seconds 30   ✅ PASS
```

세계 원장 보존·T3/T4 오프라인 상실 0건·오프라인 적분 스텝 무관성 전부
`sim/run.ts` 출력에서 확인(유지됨).

### 15.2 엔딩 도달 실측 — 클릭 0회, 226시간 32분

`sim/run.ts`의 방치 정책을 v0.1(레거시 단독 발굴)에서 v0.2(발굴단 파견·
거점 확장·시설 건립·스텝 고용을 포함한 정책)로 교체하고(G56.12) 다시 쟀다.

| 시점 | 도감(검증 182종 기준) | 방문 거점 | 발굴단 | 자산축 | RANK_SCORE | 엔딩 |
| --- | --- | --- | --- | --- | --- | --- |
| 24h | 37/182 (20.3%) | 2/12 | 2팀 | 0.5469 | 0.2498(1위) | 미달성 |
| 168h | 83/182 (45.6%) | 6/12 | 3팀 | — | — | 미달성 |
| **226h32m** | **137/182 (75.3%)** | 11/12 | 3팀 | 1.0000 | 조건 충족 | **✅ 달성** |
| 336h(조기 종료) | 137/182(동결) | 11/12 | 3팀 | 1.0000 | 0.6004 | 달성 후 유지 |

**엔딩(v0.2) 도달 시간: 226시간 32분**(약 9.4일 — 시즌 12주=2,016시간의
11.2% 지점). `RANK_SCORE` 종합 1위 + `CODEX_SCORE ≥ CODEX_GOAL_V2`(75%)를
`SEASON_TITLE_HOLD_HOURS`(1시간) 연속 유지해 `w.ended=true`가 됐다.
**클릭은 0회** — 척추 4번의 직접 증거다.

### 15.3 도달을 막고 있던 3가지 원인 — 수치 진단과 수정

작업 지시가 요구한 "도달하지 못하면 무엇이 막고 있는지 수치로 진단하라"의
과정을 그대로 남긴다. 아래 세 가지가 **전부 있어야** 226h32m에 도달했다
(어느 하나만 고쳐서는 부족했다 — 실측으로 확인).

**(1) `CODEX_SCORE` 분모 버그(구조적으로 엔딩 불가능)** — `codexScore()`가
`ARTIFACT_SPECIES_TARGET=480`(480종·12거점 완성 시점의 목표치)을 분모로
썼다. 그런데 실제 데이터셋은 280종 중 182종만 `verified`다(3단계가 이미
"실존성 우선" 원칙에 따라 480종을 다 채우지 못한 채 보고했던 그 격차,
§13.1·§13.4). 검증된 182종을 **전부** 모아도 `182/480≈37.9%`에서 멈춰
`CODEX_GOAL_V2`(75%)를 절대 넘을 수 없었다 — 게임에 수학적으로 끝이
없었다. `codexProgress(w).total`(검증된 종만 세는 분모, G53.4가 이미
`codexProgress()`에 확립해 둔 원칙)을 그대로 재사용해 고쳤다(G56.8).

**(2) 원정비 노셔널 계산 버그(발굴단이 1팀에서 굳는다)** — 발굴단을 실제로
파견하는 정책으로 48시간만 돌려도 `w.funds`가 **-80억**까지 떨어져 이후
모든 성장(인부·장비·감정소·발굴단 슬롯)이 멈췄다. `finalizeExpedition()`의
원정비 노셔널 수입 계산이 실제 드랍 판정이 쓰는 하한(`DROP_INTERVAL_FLOOR_
SECONDS`, §9.5)을 빼먹어 원정비가 실현 가능한 수입보다 훨씬 크게 산정됐고,
한 회차 안에 여러 층을 오른 원정은 초반 저층 구간까지 귀환 시점의 최종
(최고) 층 단가로 소급 청구당해 과청구가 더 컸다. `dropThreshold()`를
그대로 재사용하고(하한 반영), 파견 시점 층(`layerAtDispatch`, 신설
필드)과 귀환 시점 층 두 단가를 평균해 소급 편향을 줄였다(G56.11).

**(3) 정책 자체가 v0.2 시스템을 안 썼다** — 위 두 버그를 고쳐도 기존
`sim/run.ts`는 여전히 레거시 단독 발굴(`w.activeSite` 하나)만 써서
`MAX_OWNED_SITES=3` 밖으로 못 나갔다. 발굴단 파견·재배정을 "미방문 거점
최우선, 귀환마다 재계산"으로 다시 설계하고(기존엔 "그 거점을 다 채울
때까지" 고정 `routine`으로 눌러앉혀 두어 팀 하나가 거점 하나에 수백
시간씩 묶였다), 다음 발굴단 슬롯 해금 비용의 1.5배를 먼저 비축하도록
팀 인원·장비 증강 순서를 조정했다(G56.12).

이 세 가지 각각을 별도로 실측했다 — (1)만 고치면 발굴단이 1팀에서 굳어
방문 거점이 3/12를 못 넘는다(24~336시간 내내 동일). (2)만 고치면 자금
교착은 풀리지만 도감 분모가 여전히 480이라 37.9%를 못 넘는다. (3)만
고치면(정책 재작성) 자금 교착 때문에 발굴단이 1팀에서 다시 굳는다. 셋을
모두 반영한 뒤에야 도감 75%·발굴단 3팀·방문 거점 11/12에 도달했다.

### 15.4 기타 닫은 공백(요약, 상세는 `notes/decisions.md` G56)

- `checkEnding()` v0.1→v0.2 정의 교체(RANK_SCORE 종합 1위 + 도감 목표
  1시간 연속 유지, G56.1).
- 명성 축 "박물관 누적 관람객" 카운터 신설(`World.museumCumulativeVisitors`,
  G56.2) — 4단계가 박물관 수입만 배선하고 남겨 둔 공백.
- 시즌 롤오버를 `step()` 루프에 실제로 걸었다(G56.3) — 이전엔 헤더에
  "시즌 D-84" 표시만 있고 카운트다운 끝에 아무 일도 안 일어났다.
- `PersistentRecord`를 별도 localStorage 키(`relic-king/record/v1`)에
  영속화했다(G56.4).
- 암시장 장물 배지에 72시간 감쇠(`BlackMarketListing.listedAt` 신설,
  G56.5).
- 전시·경매 등록에 base 비교 칩(최고가 기본 선택, 조작 단계 수 불변,
  G56.6).
- 발굴단 "빠른 설정" 프리셋(G56.7).
- 라이벌 `owned` 배열이 매 step()마다 O(n²)로 느려지던 성능 버그(G56.10) —
  `checkEnding()`이 매 스텝 `fullRanking()`을 부르게 되며 처음 드러났다.
  `qa_expedition.ts`·`qa_economy.ts`가 8분+에서 30~40초로 줄었다.
- `ASSET_SCORE_REF`는 재검증 결과 지금 데이터셋(182종 검증)에서 세계
  총가치가 문서 추정치(2,500억)와 0.8% 차이로 이미 들어맞아 상수를
  바꾸지 않았다(G56.9) — 60종·3거점 시절(G51.3)의 불일치는 3~4단계의
  데이터·거점 확장으로 이미 해소돼 있었다.
- 세이브 스키마 v5→v6(museumCumulativeVisitors·blackMarket listedAt·
  teams layerAtDispatch 3개 필드, `qa_migration.ts`에 전용 구간 추가).
- `qa_endgame.ts`(참고 스크립트)도 같은 교착에 걸려 있어 안전판만
  추가했다 — 타게팅 재설계(미방문 우선)는 반영하지 않았다(범위 밖 판단,
  G56.13). 엔딩 도달의 공식 증거는 `sim/run.ts`다.

### 15.5 남은 것(보고 대상)

- `qa_endgame.ts`는 여전히 "거점을 다 채울 때까지 눌러앉는" 옛 타게팅을
  쓴다 — 참고용 스크립트라 이번 패스에서 재설계하지 않았다.
- 온라인 UI(React)로 같은 정책을 수동 재현해 실측하지는 않았다 — 이번
  패스의 측정은 전부 `sim/run.ts`(헤드리스 엔진)를 통해서다. UI는
  `smoke.mjs`로 5탭 구조·레이아웃 안정성만 확인했다(빌드·30초 방치·
  8시간 오프라인 복귀 정상).
- 라이벌의 RANK_SCORE는 여전히 근사치(G55.1 — 라이벌에겐 박물관이 없어
  명성 축 관람객 항이 0 고정)다. 이번 패스는 관람객 카운터를
  **플레이어분만** 신설했다 — 라이벌 v0.2가 축소 구현(G53.6)이라 박물관
  시스템 자체가 없기 때문이다.

## 16. 결함 수정 패스 — 사람 스크린샷 육안 검수가 낸 결함 3건(`notes/decisions.md` G57)

§15가 "온라인 UI로 같은 정책을 수동 재현해 실측하지는 않았다"(§15.5)고
남긴 공백이 실제로 문제였다 — 사람이 `smoke.mjs`가 찍은 스크린샷을 육안으로
확인한 결과 **기본 상태(자동화 설정 전부 기본값, 클릭 0회)에서 8시간
오프라인 복귀가 완전히 멈춰 있는** 치명적 결함을 발견했다. §15까지의 모든
"클릭 0회 검증"은 `sim/run.ts`(발굴단 파견·시설 건립·재투자를 다 하는
스크립트 정책)를 통해서였는데, 그 정책은 헤드리스 시뮬 스크립트에만
있었고 실제 브라우저 기본값에는 없었다 — 엔진과 UI 기본값 사이의 간극이
이번 결함의 정체다.

### 16.1 결함 1(치명) — 기본 상태 방치가 진행되지 않는 교착

**원인**: 세 가지가 겹쳐 있었다.

1. `World.settings.autoSellBelow` 기본값이 `null`(자동매각 꺼짐)이었다 —
   감정 완료 후 T0·T1 유물이 팔리지 않고 그냥 vault에 쌓이기만 해서, 판매
   수입이라는 자금 유입 경로 자체가 기본적으로 막혀 있었다.
2. `runAppraisal()`의 감정비 게이트(`funds >= fee`, spec.md §9.2)가 자금
   부족 시 그 항목을 무기한 대기시키는데, spec이 이미 정해 둔 탈출구
   ("미감정 매각으로 언제든 풀 수 있다")가 **순수 수동 액션**(`blindSell`)
   이었다 — 클릭 0회 기본값에서는 아무도 누르지 않는다.
3. `World.workers`/`World.gear`가 시작값 0에서 전혀 오르지 않았다 —
   인부·장비를 자동으로 사는 루틴이 애초에 없어(발굴단의 "루틴"은 있지만
   레거시 단독 발굴에는 대응 기능이 없었다) 발굴력이 영원히 `BASE_DIG=1`에
   묶여 있었다.

세 가지가 맞물려 **완전한 교착**을 만들었다: 감정비를 못 내 감정이 막히고
→ 감정이 안 되니 팔 게 없고 → 자금이 안 도니 인부도 못 사고 → 발굴력이
안 올라 상황이 나아질 방법이 없다. `notes/decisions.md` G3("손대지 않아도
손실이 0에 수렴하는 기본 자동화가 있다")·`AGENTS.md` 척추 4번("클릭은
언제나 선택, 모든 밸런스는 클릭 0회 기준")을 정면으로 어겼다.

**수정**(전부 `engine.ts`에 신설, `notes/decisions.md` G57 참조):

- `createWorld()`의 기본 `settings.autoSellBelow`를 `null → 1`(희귀 이하
  자동매각)로 바꿨다. 세이브 v6→v7 마이그레이션이 기존 세이브의 `null`값도
  같이 올려 준다(returning player도 같은 결함의 피해자이므로).
- `autoLiquidatePendingOverflow(w)` 신설 — 미감정 큐가 `pendingCap(w.lab)`
  (이전까지 순수 UI 경고 임계값이던 그 함수, G54.6)을 넘으면 T0·T1 잉여만
  자동으로 `blindSell`한다. spec.md §9.2가 이미 정해 둔 탈출구를 자동화한
  것뿐, 새 매각 채널이 아니다. T2 이상은 절대 건드리지 않아 G39/A1("파괴적
  손실 금지")의 취지를 그대로 지킨다.
- `autoInvestLegacyDig(w)` 신설 — 남는 자금(여유분 `AUTO_INVEST_RESERVE`
  제외)으로 감정소·장비·인부를 자동으로 산다. `sim/run.ts`가 처음부터
  방치 기준선에 써 온, 이미 여러 라운드 검증된 정책(economy.md의 "σ<1
  기본 정책")과 **완전히 같은 우선순위·같은 함수**다 — `sim/run.ts`도
  이제 이 함수를 그대로 가져다 쓴다(로직이 두 곳으로 갈라지지 않는다).
- 두 함수를 묶은 `runAutoRoutine(w)`를 **`applyOffline()`**(오프라인
  복귀 시점 1회)·**`sim/run.ts`의 `act()`**(매 틱)·**`useGame.ts`의
  애니메이션 프레임 루프**(탭을 열어 둔 채 방치할 때 60초마다)에서
  부른다. `w.settings.autoReinvest`(신설, 기본 켬)로 재투자만 따로 끌 수
  있다 — 꺼도 손실은 없다(척추 4번, 클릭은 언제나 선택으로 남는다).

**`advance()`/`step()` 안에서는 절대 부르지 않는다** — 실측으로 두 번
확인한 제약이다. 처음엔 `step()` 내부 60초 경계마다 불렀는데 재투자가
그 즉시 `digPower()`를 바꿔 드랍·층 진행 속도 자체가 스텝 크기(1s vs 10s)에
따라 갈라지는 회귀를 냈다(`sim --hours 24`의 오프라인 적분 검사가 drops
105/106·layer 6/7까지 벌어졌다). `advance()` 맨 끝으로 옮겨도 여전히
지수 비용 곡선의 "문턱" 민감도가 귀환 후 funds를 스텝 크기별로 수천만 원
단위로 갈라놓아(`qa_expedition.ts`의 0.1% 허용치를 깼다) `applyOffline()`
전용으로 뺐다 — 자세한 진단은 `engine.ts`의 `runAutoRoutine`·
`applyOffline` 주석에 남겼다. 부수적으로, `createWorld()`가 항상 `version:
5`로 새 월드를 찍던 오래된 버전 번호 버그(실제로는 v6 필드를 다 채운
월드였다 — 재직렬화 시 무해한 재적용이었지만 정확하지 않았다)도 이번에
`version: 7`로 바로잡았다.

**실측(before/after, 코드 레벨 — 결정론적 시드, `createWorld()` +
`applyOffline()`을 직접 호출해 브라우저의 오프라인 복귀와 동일한 경로를
탄다)**:

| 지표 | 8h 방치, 수정 전 | 8h 방치, 수정 후 | 24h 방치, 수정 전 | 24h 방치, 수정 후 |
| --- | --- | --- | --- | --- |
| 자금 | 212 ₩ | 20.8만 ₩ | 212 ₩(변화 없음) | 104만 ₩ |
| 미감정 큐 | 203점 | 3점 | **238점(계속 쌓임)** | 3점 |
| 소장고 | 57점 | 15점(잉여는 자동매각됨) | 57점(변화 없음) | 15점 |
| 발굴력 | **1.0/s(8h 전과 동일)** | 208.4/s | **1.0/s(24h 전과 동일)** | 222.8/s |
| 인부/장비/감정소 | 0/Lv.0/Lv.1 | 28/Lv.4/Lv.4 | 0/Lv.0/Lv.1(변화 없음) | 30/Lv.4/Lv.4 |
| 자산(v0.1식) | 149만 ₩ | 182만 ₩ | 149만 ₩(변화 없음) | 182만 ₩ |

수정 전은 8h→24h 사이 미감정 큐만 계속 자라고(203→238) 나머지는 전부
그대로다 — 완전한 교착의 직접 증거다. 수정 후는 미감정 큐가 `pendingCap`
언저리에서 안정되고, 발굴력·자산이 실제로 증가한다. 8h와 24h의 발굴력·
자산 차이가 크지 않은 건 `OFFLINE_CAP_SECONDS`(12시간, 기존 상수·이번
패스가 건드리지 않음)가 오프라인 적분 자체를 12시간으로 잘라내기
때문이다 — 8h는 전부 적분되고 24h는 12h만큼만 적분된다는 뜻이라 두
결과가 비슷한 건 정상이다.

**실측(브라우저, `tests/e2e/smoke.mjs --seconds 45`)**: 8시간 오프라인
복귀 후 "소장고 16종 · 미감정 0점, 칸 이동 없음" — 결함 3건 수정을 전부
반영한 최종 빌드에서 실제 CDP 브라우저로 재현한 결과다(§16.4 스크린샷
`mobile-01-dig.png` — "8시간 0분 동안 자금 +20.5만 ₩" 복귀 토스트,
발굴력 186.8/s로 실제 화면에 표시됨).

**엔딩(226h32m) 재확인**: `sim/run.ts`가 이제 `runAutoRoutine`을 공유하므로
정책이 바뀌었다 — 재측정한 결과 **239시간 8분**에 엔딩 도달(수정 전
226시간 32분 대비 +12시간36분, 시즌 12주=2,016시간의 11.9% 지점, 여전히
같은 자릿수). 도감 137/182(75.3%)·자산축 1.0000(포화)·RANK_SCORE 조건
충족은 그대로 유지된다(`eval.md` §15.2와 같은 형식으로 재확인, 아래
§16.5 명령 출력 참조).

### 16.2 결함 2 — 스모크가 온보딩 모달을 안 닫아 화면 검증이 불가능했다

**원인**: 첫 감정 완료 후 20~40초 안에 "본거지를 정하자" 온보딩
오버레이(`OnboardingOverlay.tsx`)가 자동으로 뜨는데, `smoke.mjs`의 탭
전환 구간(소장고·시설·시장·도감 스크린샷)은 이걸 전혀 처리하지 않았다 —
그 결과 `04-facility.png`·`05-market.png`·`06-codex.png`가 전부 오버레이만
찍혀 있었다.

**수정**: 탭 전환 루프 진입 직전과 매 탭 클릭 직전, 두 지점에서
`.onboarding-keep`(온보딩의 "경주 유지하고 계속하기" 버튼) 옵셔널
클릭으로 방어했다. 모바일 375px 뷰포트(`Emulation.setDeviceMetricsOverride`)
스크린샷 3장(발굴·시장·소장고)도 신설했다 — `notes/ux-v02.md`가 설계한
모바일 레이아웃의 검증 스크린샷이 이전까지 하나도 없었다.

**실측**: `node tests/e2e/smoke.mjs --seconds 45` 재실행 결과 스크린샷
12장 전부 실제 화면을 담았다(§16.4 목록) — 시설·시장·도감 탭이 온보딩에
가려지지 않고, 모바일 3장이 새로 생겼다.

### 16.3 결함 3 — 세계지도 거점 라벨 겹침 + "세계지도"로 안 읽히는 문제

**원인**: `render/worldmap.ts`가 12거점을 실제 위경도로 투영하다 보니
유럽·중동 구간(로마·그리스·튀르키예·이스라엘·이라크·이집트)의 라벨이
전부 마커 우상단 고정 오프셋(`x+5, y-3`)에 찍혀 서로 겹쳤다. 또한 지도가
점(마커)과 선(연결선)뿐이라 대륙 윤곽이 전혀 없어 "세계지도"로 안 읽혔다.

**수정**:

- **라벨 충돌 회피**(세계 줌 전용, 권역 줌은 이미 4배 확대라 여유가
  있어 기존 고정 오프셋 유지): `markerState`(base>visited>unvisited)
  우선순위로 정렬한 뒤, 마커 주변 8개 후보 위치(근거리 4방향 + 리더선을
  동반한 원거리 4방향)를 `ctx.measureText()` 기반 사각형 충돌 검사로
  순서대로 시도하는 그리디 배치. 전부 겹치면 라벨은 생략하고 마커만
  남긴다(클릭 판정에는 영향 없음 — `siteScreenPositions`/`pickSiteAt`은
  건드리지 않았다).
- **대륙 실루엣**: 외부 GeoJSON 없이(환경 네트워크 제약, 기존과 동일)
  아프리카·유라시아·북아메리카·남아메리카·호주 5개를 7~10개 정점짜리
  성긴 위경도 다각형으로 근사하고, 320×160 그리드를 6칸 간격으로
  샘플링해 point-in-polygon 판정된 칸에만 옅은 도트(`PALETTE.rock[0]`,
  알파 0.16 — **새 색을 쓰지 않고 기존 40색 팔레트 안에서만** 골랐다)를
  찍는다. 그리는 순서(배경→대륙 도트→그리드선→연결선→마커→라벨)로
  가독성이 항상 대륙 도트보다 우선하게 했다.

**실측**: 빌드된 앱을 직접 CDP로 열어 세계 줌 지도를 캡처하고 밀집
구간(로마·튀르키예·그리스·이라크·이스라엘·인도·중국·한반도·일본)을
3배 확대 크롭해 육안 확인 — 튀르키예가 리더선으로 떨어져 나가고,
나머지 라벨들이 서로 겹치지 않고 개별적으로 읽힌다(이스라엘·이집트가
가장 붙어 있지만 여전히 구분된다). 대륙 도트도 배경에 옅게 드러나
지도라는 느낌을 준다.

### 16.4 스크린샷 목록(`assets/screenshots/`, `smoke.mjs --seconds 45` 재실행 결과)

| 파일 | 내용 | 확인 사항 |
| --- | --- | --- |
| `01-dig-start.png` | 발굴 탭, 시작 직후 | 정상 |
| `02-dig-running.png` | 발굴 탭 + 온보딩 오버레이 | 첫 감정 직후라 오버레이가 뜬 게 정상(결함 2와 무관 — 이 시점엔 아직 안 닫는다) |
| `03-vault.png` | 소장고 탭 | 정상 |
| `04-facility.png` | 시설 탭 | **결함 2 수정** — 온보딩에 가리지 않고 박물관 화면이 보인다 |
| `05-market.png` | 시장 탭 | **결함 2 수정** — 암시장 12슬롯이 보인다 |
| `06-codex.png` | 도감 탭 | **결함 2 수정** — 도감 그리드가 보인다 |
| `06-vault-full.png` | 8시간 오프라인 복귀 후 소장고 | **결함 1 수정** — 미감정 0점, 소장고 채워짐 |
| `07-vault-detail.png` | 소장고 유물 상세 | 정상 |
| `cover.png` | 런처 카드용 표지 | 정상 |
| `mobile-01-dig.png` | 375px 발굴 탭 | **결함 2 신설** — "8시간 0분 동안 자금 +20.5만 ₩" 복귀 토스트·발굴력 186.8/s 표시(**결함 1 수정의 실제 화면 증거**) |
| `mobile-02-market.png` | 375px 시장 탭 | **결함 2 신설** |
| `mobile-03-vault.png` | 375px 소장고 탭 | **결함 2 신설** — 미감정 1점(정상 진행 중, 봉인 보관 0) |

세계지도(결함 3)는 `smoke.mjs`가 찍는 정규 스크린샷 목록에 없다(지도는
발굴 탭 안에서 "지도로 보기" 버튼을 눌러야 나온다) — 별도로 CDP를 직접
몰아 세계 줌 지도를 캡처하고 밀집 구간을 크롭해 육안 확인했다(§16.3,
이 세션의 임시 검증용이라 저장소에는 커밋하지 않았다).

### 16.5 필수 명령 실측 — 전부 통과

```
cd /home/user/proto && pnpm --filter relic-king build              ✅
cd /home/user/proto && pnpm --filter relic-king sim --hours 24     ✅ (발굴력 1739/s, 자산축 포화)
cd /home/user/proto && pnpm --filter relic-king sim --hours 336    ✅ (239h8m에 엔딩 도달, 조기 종료)
cd /home/user/proto && pnpm --filter relic-king qa:migration       ✅ (v1~v6 → v7 전 구간, 60/60)
cd /home/user/proto && pnpm --filter relic-king qa:expedition      ✅ (11/11)
cd /home/user/proto && pnpm --filter relic-king qa:artifacts       ✅ (경고 41건은 실패로 안 친다, §4·G53.8 참조)
cd /home/user/proto && pnpm --filter relic-king qa:economy         ✅ (29/29)
cd /home/user/proto/projects/relic-king && node tests/e2e/smoke.mjs --seconds 45   ✅ PASS
```

세계 원장 보존·T3/T4 오프라인 상실 0건·오프라인 적분 스텝 무관성 전부
`sim/run.ts`·`qa_expedition.ts` 출력에서 확인(유지됨). `qa_expedition.ts`·
`qa_economy.ts`는 각각 하나씩 시나리오를 수정했다 — 둘 다 "이 구간의 자금
증가는 오직 [원정비/박물관 순수익] 때문이어야 한다"는 전제로 설계된
격리 테스트인데, 기본 자동매각이 켜지면서(이번 결함 수정) 그 전제가
깨졌다. 두 테스트 모두 제보(tip)를 끄는 것과 같은 방식으로 각자의 테스트
시작부에 `w.settings.autoSellBelow = null`을 명시해 전제를 복원했다 — 새
결함이 아니라 테스트가 통제하려는 변수 목록에 이번에 새로 생긴 기본
자동화 하나를 추가한 것뿐이다.

---

## 17. v0.3 — 유물 2000종 확대와 획득 주기 단축 (`notes/decisions.md` G59~G67)

이 절은 이전 절들의 수치를 덮어쓰지 않는다. §13·§15의 숫자는 그때의 데이터셋
(280종, 검증 182종)·드랍 바닥값(20초)에서 잰 것이고, 아래는 v0.3(2000종,
검증 1,902종, 바닥값 8초)에서 다시 잰 것이다.

### 17.1 데이터셋

| | v0.2 | v0.3 |
| --- | --- | --- |
| 총 종수 | 280 | **2,000** |
| 검증(verified) | 182 | **1,902** |
| 미검증(pending) | 98 | 98 (손글씨 몫 그대로) |
| 티어 | T0 76 · T1 89 · T2 75 · T3 28 · T4 12 | **T0 786 · T1 1,028 · T2 146 · T3 28 · T4 12** |
| 거점별 | 19~28종 | **126~177종** |

`pnpm --filter relic-king qa:artifacts` ✅ 통과. era 키워드 미겹침 경고 41건은
**전부 손글씨 기존 행**이다(생성 1,720행은 거점 자신의 층 라벨을 쓰므로 신규
경고 0건).

상위 두 티어(T3 28 · T4 12)가 v0.2에서 **한 종도 안 늘었다.** 데이터가 모자라서가
아니라 수집 파이프라인이 T2까지만 만들기 때문이다 — "국보급"과 "세상에 하나"는
소장 점수로 추론할 게 아니라 사람이 근거를 들고 판정할 일이다(G61).

### 17.2 획득 주기 — 바닥값 4지점 실측

| 바닥값 | 첫 드랍 | 20분 평균 간격 | 엔딩 | 발굴력(엔딩) | 불변식 |
| --- | --- | --- | --- | --- | --- |
| 20초 (v0.2) | 20초 | 20.3초 | 177시간 46분 | 9,423/s | ✅ |
| 12초 | — | 12.6초 | 144시간 45분 | 9,423/s | ✅ |
| **8초 (채택)** | **10초** | **9.9초** | **140시간 44분** | 9,423/s | ✅ |
| 5초 | — | 7.2초 | 123시간 6분 | 9,423/s | ✅ |

**발굴력이 네 지점 모두 동일하다** — 기본 재투자 정책이 장비 Lv.10·감정소 Lv.6에서
자연 정체해, 드랍이 늘어 수입이 늘어도 발굴력이 안 따라 오른다. 화폐 창출률은
설계상 간격에 반비례해 올라가지만(G63) 폭주로 이어지지 않는다는 직접 증거다.

### 17.3 엔딩 도달 실측 — 클릭 0회, 140시간 44분

| 항목 | v0.2 | v0.3 |
| --- | --- | --- |
| 엔딩 | 224시간 57분 | **140시간 44분** |
| 첫 유물 | 20초 | 10초 |
| 첫 국보(T3) | 47분 | 52분 45초 |
| 첫 유일(T4) | 1시간 47분 | 2시간 18분 |
| 12층 도달 | 7시간 3분 | 4시간 55분 |
| 도감 | 137 / 182 (75%) | **1,427 / 1,902 (75%)** |
| 자산 축 | 1.0000 (포화) | **0.5563** |
| 도감 축 | 0.7527 | 0.7503 |
| 명성 축 | 0.2406 | 0.1802 |
| 종합 | 0.6477 | 0.4926 |

**확대 전 추정이 틀렸다.** "2000종이면 75% = 1,500종이 필요하니 엔딩이 수천
시간대"라고 봤는데 실제로는 짧아졌다. 분모만 보고 분자의 난이도 분포가 같이
바뀌는 걸 계산에 안 넣은 오류다(G64) — 2000종은 T0·T1이 대부분이라 75% 지점이
쉬운 티어 안에서 끝난다.

불변식 전부 유지: 원장 보존 ✅ · 오프라인 적분 스텝 무관 ✅ · 오프라인 T3·T4 상실
0건 ✅ · 클릭 가속 ×1.35(기준 1.35 이하) ✅.

### 17.4 §12(480종 데이터 무결성) 기준 갱신

§8 표 12번 항목의 기준을 갱신한다. `SPECIES_PER_SITE_BY_TIER`는
`[25, 8, 4, 2, 1]`(거점당 40종) → **`[66, 85, 12, 3, 1]`**(거점당 167종),
`ARTIFACT_SPECIES_TARGET`은 480 → **2000**이다. 판정 기준(정확한 일치를 요구하지
않고 미달은 정직하게 보고)은 그대로다. 현재 verified 1,902 / 2,000 = 95.1%.

### 17.5 §15가 남긴 "자산 축 포화" 한계 해소

§15·`notes/decisions.md` G51이 "`ASSET_SCORE_REF`가 지나치게 커서 자산 축이 항상
1.0 근방으로 포화된다"를 보고 대상으로 남겨 뒀다. v0.3에서 세계 총가치가
2,500억 → 11,710억이 되며 `ASSET_SCORE_REF`가 375억 → 1,757억이 됐고, 엔딩 시점
자산 축이 **1.0000 → 0.5563**으로 내려왔다. 3축 순위가 실제로 3축이 됐다.

같은 표류가 다시 나지 않게 `qa_artifacts.ts`가 매번 `ARTIFACTS` 배열에서 총가치를
재계산해 상수와 1% 이상 어긋나면 **실패**로 잡는다.

### 17.6 자동 검증 결과

| 검사 | 결과 |
| --- | --- |
| `pnpm build` (tsc + vite) | ✅ 1,031 KB / gzip 194 KB |
| `qa:artifacts` | ✅ (경고 41건 — 전부 기존 손글씨 행) |
| `qa:economy` | ✅ (검사 지평 168h → 336h, §17.7) |
| `qa:migration` | ✅ |
| `qa:season` | ✅ |
| `qa:expedition` | ✅ 오프라인 적분 스텝 무관 |
| `pnpm smoke` (CDP) | ✅ PASS · 스크린샷 14장 · 8시간 오프라인 후 소장고 44종 |

### 17.7 QA 두 건이 깨졌는데 둘 다 측정 창 문제였다

**통화 폭주 게이트**가 ×11.26(기준 ≤×3)으로 실패했다. 504시간 8구간으로 늘려
보니 126~189h의 1회성 램프(장비 상한 도달 + 복원 수렴)가 168시간 창의 4분기를
가로지른 것이었고, 189h 이후는 완전 평탄했다(×0.96~1.07). 복리 루프가 아니다.
검사 지평을 336시간으로 늘려 3·4분기가 둘 다 램프 뒤에 놓이게 했다 → ×1.19.

**보존 상태 분산 검사**도 실패했다. 336시간이면 복원이 금고 전체를 condition 4로
수렴시켜 끝 시점 분산이 0이 된다 — 복원이 작동했다는 증거인데 판정이 그걸 실패로
읽었다. "끝 시점에 분산이 있는가"에서 "런 도중 각 유물의 상태가 처음 값에서
실제로 움직였는가"로 바꿨다(끝 시점 4급 55점 · 런 도중 이동 200점).

### 17.8 육안 확인에서 잡은 것

도감 탭이 **1,902개 버튼을 한 화면에 쏟는 벽**이 돼 있었다(스크린샷 확인).
거점 하나씩만 그리도록 바꿨다 — 12거점 칩(각각 진행도 표시) + 표시 필터
(전체·미소장만·소장만) + 거점 진행 바, 기본 선택은 지금 파고 있는 거점.
액션 깊이는 3단계 이내 유지(탭 → 거점 칩 → 유물).

생성 유물이 실제 화면에 정상 표시되는 것도 확인했다(`07-vault-detail.png`):
"고려 전기 금 머리 비녀 · 희귀 / 고려 전기 · 한반도 · 현 소장처 메트로폴리탄
미술관 / 메트로폴리탄 미술관이 10~12세기로 편년한 머리 비녀다. 재질은 금."

### 17.9 알려진 한계 (v0.3, 보고 대상)

- **생성 1,720종의 `note`는 조립문이다.** 사실은 전부 메트 카탈로그에서 오고
  원문을 옮기지 않지만(§6 라이선스 규칙 준수), 손글씨 280종의 2~3문장과 질이
  다르다. 실존의 무게는 여전히 손글씨, 특히 T3·T4가 진다.
- **티어는 추정이다.** 메트 한 곳의 소장 점수를 현존 수량의 하한으로 쓴다.
  항상 흔한 쪽으로만 틀리도록 설계했지만, 추정인 건 맞다.
- **한반도가 126종으로 가장 얇다.** 메트의 한국 소장품 자체가 581점뿐이다
  (일본 16,927 · 중국 13,487). 다른 관의 오픈액세스 덤프를 붙이면 메워진다.
- **미검증 98종은 그대로다.** 이번 확대는 신규 수집이고, 기존 `pending` 98종의
  검증은 손대지 않았다.
- **플레이테스트 0회.** 재미 가설은 여전히 미검증이다.

# 18. v0.3.1 — 소장고 중복분 자동 매각 (`notes/decisions.md` G68)

작업 지시: *"소장 유물 중에 희귀도 조건을 설정하고 자동 매각 기능을 구현한다."*

기본값이 **꺼짐**이라 §17의 기준선(엔딩 141시간 등)은 그대로다 — 아래는 전부
기능을 켰을 때 무슨 일이 벌어지는지의 측정이다.

## 18.1 규칙 단위 (`pnpm --filter relic-king qa:autosell` 1부)

손으로 짠 소장고(T0 ×3, T1 ×2 중 1점 전시, T2 ×2, T3 ×3, T4 ×1 = 11점)에서
`spareVaultItems()`의 대상 선정을 직접 확인한다. 14건 전부 통과:

| 확인 | 결과 |
| --- | --- |
| 기준 진귀(T2) → 대상 4점(T0 2 · T1 1 · T2 1) | ✅ |
| 국보·유일은 3점 쌓여 있어도 대상 아님 | ✅ |
| 전시 중인 사본은 대상 아님 / 전시 사본이 보존분 역할 | ✅ |
| 보존분 = 그 종에서 가장 값비싼 사본 | ✅ |
| 기준 흔함(T0) → 2점 · 끔(null) → 0점 | ✅ |
| 상한 초과 설정(T4)도 `AUTO_SELL_SPARE_MAX_TIER`로 잘림 | ✅ |
| 매각 후 도감 종 수 불변 · 모든 종 최소 1점 잔존 | ✅ |
| 두 번째 호출은 0점(멱등) · `stats.sold` 정확 | ✅ |

## 18.2 168시간 방치 — 회귀 게이트

G57(미감정 잉여 처분)과 G59(경매 출품)가 각각 도감을 감소시킨 전력이 있어,
세 번째 자동 매각 경로에는 **틱 단위** 게이트를 걸었다. 60초마다 자동 루틴을
돌리며 10,080틱을 측정했다.

| 검사 | 실측 |
| --- | --- |
| 도감 종 수의 틱당 최소 변화 | **0** (한 틱도 감소 없음) ✅ |
| 루틴 직후 기준에 걸리는 중복분 | 0점 ✅ |
| 루틴이 삼킨 국보·유일 | **0점** ✅ |
| 세계 원장 보존(owners + remaining == total) | 이상 없음 ✅ |

## 18.3 대조군 — 켜면 무엇을 잃고 무엇을 얻는가

같은 초기 상태에서 168시간을 방치한 두 월드(방치 전용 — 거점 해금·원정·시설
없이 레거시 단독 발굴과 자동 재투자만 돈다):

| | 기능 끔 | 기능 켬 |
| --- | ---: | ---: |
| 자금 | 11.5억 ₩ | 6.7억 ₩ |
| 소장 가치 | **183.6억 ₩** | **43.8억 ₩** |
| 3축 자산 점수 | **10.45%** | **2.49%** |
| 도감 | 126종 | 125종 |
| 소장 점수 | 338점 | 133점 |
| 드랍 횟수 | 75,516 | **75,516** |
| 발굴력 | 15,851/s | 16,238/s |

**이 표에서 제일 중요한 칸은 드랍 횟수다.** 두 런이 정확히 같다 —
`DROP_INTERVAL_FLOOR_SECONDS`(8초) 바닥값에 이미 붙어 있어서, 회수한 자금을
인부·장비에 더 부어도 유물이 더 나오지 않는다. 그래서 이 방치 전용 조건에서
기능을 켜는 건 **순수 손실**이다: 자산 축 10.45% → 2.49%를 내주고 받는 게
없다(도감 1종 차이는 두 런이 갈라진 뒤의 발산 잡음이고, 게이트도 ±3종으로 둔다).

회수한 자금이 순위로 돌아오려면 드랍 바닥값에 걸리지 않는 사용처가 있어야
한다 — 거점 해금·박물관·경매장·발굴단 확장. 즉 이 기능은 **돈을 쓸 데가 있는
플레이어를 위한 것**이고, 그래서 기본값이 꺼짐이다. 이 트레이드오프는 소장고
탭 문구에 그대로 적혀 있다("회수한 자금은 거점 해금·시설·원정에 쓸 때만 순위로
돌아온다 — 쌓아 두기만 하면 자산 축만 깎인다").

## 18.4 기존 게이트

| 검사 | 결과 |
| --- | --- |
| `qa:migration` (v1→v8 체인, v7→v8 신규 구간 5건 포함) | ✅ 전체 통과 |
| `tsc --noEmit` | ✅ |
| `sim --hours 24` 기본값 실행 결과가 v0.3.0과 동일한가 | ✅ (18.5) |

## 18.5 기본값 경로가 정말 그대로인가

`autoSellSpareBelow`가 `null`이면 `sellSpares()`는 난수도 상태도 건드리지 않고
즉시 반환한다. 그래도 논증 대신 실측으로 확인했다 — v0.3.0 커밋을 git worktree로
꺼내 같은 `--hours 24` 시뮬을 돌리고 출력을 비교했다. 차이 없음.

## 18.6 알려진 한계

- **방치 전용 조건에서만 대조했다.** 18.3의 두 런은 원정·시설·거점 해금을
  쓰지 않는다. "돈을 쓸 데가 있는" 플레이에서 이 기능이 실제로 순위를 올리는지는
  아직 측정하지 않았다 — `sim/run.ts`의 적극 플레이 경로에 이 설정을 켠 변형을
  추가하는 게 다음 측정이다.
- **플레이테스트 0회.** §17.9와 같다. 중복 정리 스트립이 실제로 발견되는지,
  기본값 꺼짐이 옳은 선택인지는 사람이 만져 봐야 안다.

---

# 19. v0.4 — 아이콘 다양화와 설명의 실사화 (`notes/decisions.md` G69~G73)

> 아이콘 구분력 척도의 정의는 `app/src/render/metric.ts` 머리말과 G70에 있다.
> 아래 수치는 전부 `pnpm --filter relic-king qa:sprites`로 재현된다.
> "이전"은 커밋 `28196f9`(v0.3.1)의 렌더러에 측정 도구만 얹어 같은 척도로 잰 값이다.

## 19.1 아이콘 중복 — 합격선 §5 그대로

| 지표 | v0.3.1 | v0.4 | **v0.4.1** | 합격선 |
| --- | --- | --- | --- | --- |
| T2 이상 186종, 비트맵이 **완전히 같은 쌍** | **15군** | 0 | **0** | 0 (실패 조건) |
| 전체 2,000종, 완전히 같은 쌍 | 128군 / 778종 | 0군 / 0종 | **0군 / 0종** | 보고만 |
| 지각거리 임계(RMS ΔE < 4.0) 이하 근접쌍 | 681쌍 / 3.958% | 6쌍 / 0.035% | **5쌍 / 0.029%** | 보고만 |
| 지각거리 최소 | 0.00 | 1.41 | **1.55** | — |
| 지각거리 p1 / 중앙 / p99 | 0.12 / 23.37 / 48.84 | 12.24 / 28.85 / 48.13 | 12.57 / 28.91 / 48.05 | — |

전수 쌍은 17,205개(186종의 조합)다. 근접쌍이 남은 원인은 한 가지다 —
`(거점, shape, palette)` 묶음의 크기가 6(변형 수)을 넘으면 라운드로빈이 한 바퀴
돌아 같은 변형이 다시 나온다. 남은 5쌍 전부가 그 경우다.

```
D=1.55  met-korea-bottle-5           ↔ met-korea-brush-holder-1
D=1.60  celadon-cloud-crane-maebyeong ↔ met-korea-wine-cup-2
D=2.70  irq-kassite-kudurru          ↔ met-iraq-palette-4
D=2.80  mexico-atlantes-de-tula      ↔ met-mexico-male-figure-2
D=2.94  met-peru-tray-6              ↔ met-peru-basket-4
```

v0.4.1이 거점 램프 편향을 뺐는데도 근접쌍이 늘지 않은 것(6 → 5)은, 이 쌍들이 **색이
아니라 실루엣 중복**으로 붙어 있기 때문이다 — 위 다섯 쌍의 IoU는 전부 0.93 이상이다.
색 편향은 같은 실루엣 두 점을 갈라 주지 못했다.

변형 수를 6 → 8로 늘리면 줄어들지만, 32px에서 "윤곽선만 봐도 갈리는" 실루엣을
shape당 8종까지 찾는 게 다음 작업이다(§19.6).

## 19.2 거점 구분 — 아이콘만 보고 거점을 맞히는 기계 판정

LOO(leave-one-out) 최근접 중심 분류. 12지 선다이므로 무작위 추측은 8.3%다.
특징 두 가지로 각각 재고 둘 다 보고한다(근거는 G70).

| 특징 | v0.3.1 | v0.4 | **v0.4.1** | 합격선 |
| --- | --- | --- | --- | --- |
| **(가) 거점 신호 채널** — 채도가중 색상 히스토그램(16칸) + 평균 L\*·채도 + 이웃 색 변화율 | 15.6% (top-3 44.1%) | 95.2% (top-3 100.0%) | **91.4%** (top-3 **100.0%**) | ≥ 85% (실패 조건) |
| (나) 전체 외형 — 8×8 축약 Lab 지도(192차원) | 11.8% (top-3 38.2%) | 65.6% (top-3 93.0%) | 34.4% | 보고만 |

v0.4.1이 3.8%p를 내준 것은 거점 램프 편향을 뺀 대가다 — 재질 판독력 45%p와 맞바꿨다
(§19.2.1, G74). top-3는 100% 그대로다. (나)가 65.6% → 34.4%로 내려간 것도 같은
이유이고, (나)는 애초에 "같은 거점 안에서도 유물이 서로 달라 보이는가"를 재는 축이라
내려간 쪽이 요구에 더 맞는다.

거점별 (가) 정확도 (v0.4.1):

| 거점 | 정확도 | 거점 | 정확도 |
| --- | --- | --- | --- |
| 한반도 | 13/16 = 81% | 이라크 | 15/17 = 88% |
| 그리스 | 14/16 = 88% | 일본 | 11/12 = 92% |
| 이집트 | 10/11 = 91% | 로마 | 17/18 = 94% |
| 튀르키예 | 18/20 = 90% | 멕시코 | 19/20 = 95% |
| 이스라엘 | 10/11 = 91% | 페루 | 15/16 = 94% |
| 인도 | 15/15 = 100% | 중국 | 14/14 = 100% |

남은 혼동: `korea → india`(3) · `turkey → india`(2) · `iraq → greece`(2) ·
`peru → india`(2) · `egypt → india`(1) · `greece → iraq`(1). 오답이 인도(사프란)로
몰리는데, 사프란은 주황이라 금·구리갈색·황토 계열 **재질** 몸통과 색상 칸이 겹친다.
v0.4에서 반복되던 `korea → egypt`(램프 편향이 만든 올리브톤)는 편향을 빼면서
사라졌다. 어느 쪽이든 top-3가 100%이므로 "거점을 못 읽는다"가 아니라 "이웃 거점과
헷갈릴 수 있다" 수준이다.

**(나)가 낮은 것을 결함으로 보지 않는 이유**를 적어 둔다. (나)는 형태까지 포함한
전체 외형이라 같은 거점 안에서도 실루엣이 10종×6변형으로 흩어진다. (나)를 100%로
만들려면 한 거점의 아이콘이 전부 비슷해져야 하고, 그건 "T2 이상 186종의 중복
최소화"와 정면으로 충돌한다. 두 요구가 서로를 제한하는 지점이고, 그래서 둘을 각각
재서 같이 보고한다.

## 19.2.1 재질 판독력 — 거점 신호가 재질을 덮지 않았는가 (v0.4.1 신설, G74)

같은 LOO 최근접 중심 분류기에 라벨만 `palette`(7재질)로 바꾼 것이다. 무작위 추측은
14.3%. 특징은 **면적 가중** 색상 히스토그램(`materialHue`)이다 — 거점 축이 쓰는
채도 가중 특징으로 재질을 재면 채도 높은 테두리 1~2px이 무채색 석재 몸통 수백 px을
이겨서 결과가 구조적으로 왜곡된다(G74).

| 지표 | v0.3.1 상당(신호 끔) | v0.4 | **v0.4.1** | 합격선 |
| --- | --- | --- | --- | --- |
| 재질 분류 정확도 | 93.0% | 36.6% | **81.7%** (top-3 **98.9%**) | ≥ 80% (실패 조건) |

재질별 (v0.4.1) — 괄호는 v0.4:

| 재질 | 정확도 | 재질 | 정확도 |
| --- | --- | --- | --- |
| 석재 | 45/48 = **94%** (0%) | 청자 | 16/19 = 84% (26%) |
| 은·청동 | 25/29 = 86% (34%) | 금 | 29/35 = 83% (60%) |
| 유리 | 11/12 = 92% (33%) | 목재 | 15/20 = 75% (30%) |
| 질그릇 | 11/23 = 48% (39%) | | |

**v0.4에서 석재 48종이 0/48이었다.** T2 이상에서 가장 많은 재질이 단 한 점도 돌로
읽히지 않았다 — 거점 95.2%는 그 대가로 산 숫자였다. 이 표가 v0.4의 합격선이 한쪽
축만 재고 있었다는 증거이고, 그래서 v0.4.1이 재질 하한을 실패 조건으로 같이 건다.

남은 혼동은 `earthenware → wood`(11)·`wood → earthenware`(4)에 몰려 있다. 질그릇과
목재 램프가 둘 다 갈색 계열이라 애초에 가까운 쌍이고, **거점 신호와 무관한 팔레트
자체의 문제**다(신호를 완전히 끈 상태에서도 같은 방향으로 틀린다). 팔레트 재설계는
이번 범위 밖으로 둔다.

## 19.3 T4 유일 12종의 분리

| 지표 | v0.3.1 | v0.4 | **v0.4.1** | 합격선 |
| --- | --- | --- | --- | --- |
| T4 12종 **서로 간** 최소 지각거리 | 0.23 | 18.73 | **19.23** | ≥ 12 (실패 조건) |
| T4 ↔ **비T4 1,988종** 최소 지각거리 | 0.00 | 13.70 | **16.32** | ≥ 10 (실패 조건) |

v0.3.1의 0.00은 "금동미륵보살반가사유상과 황금 카노푸스 관의 비트맵이 완전히
같았다"는 뜻이다(둘 다 `statue`/`gold`). 0.23은 알렉산더 모자이크 ↔ 태양의 돌
(둘 다 `tablet`/`stone`)이었다.

v0.4.1의 종별 최근접 비T4 거리:

| 유물 | 거리 | 유물 | 거리 |
| --- | --- | --- | --- |
| 와르카 화병 | 16.32 | 아스텍 태양의 돌 | 19.39 |
| 톱카프 단검 | 17.36 | 알렉산더 모자이크 | 21.37 |
| 이사야 대사본 | 18.69 | 금동미륵보살반가사유상 | 21.69 |
| 전면 토카푸 튜닉 | 18.86 | 안티키테라 기계 | 22.04 |
| 코이누르 다이아몬드 | 23.79 | 투탕카멘 황금 마스크 | 25.68 |
| 하존(何尊) | 26.57 | 풍신뇌신도 병풍 | 33.88 |

12종 전부가 v0.4보다 멀어졌다. 램프 편향을 뺐더니 유일 도안이 **자기 재질색**을
되찾아서, 같은 거점의 절차 생성분과 색이 겹치던 지점이 풀린 결과다(§19.2.1).

합격선(12/10)을 실측(19.23/16.32)에 붙여 놓지 않은 이유는 G70에 있다 — 임계를
실측에 붙이면 사소한 팔레트 조정에도 QA가 깨져 신호가 아니라 잡음이 된다.

## 19.4 눈으로 검사한 것

컨택트 시트 — `pnpm --filter relic-king sheets`, `assets/generated/`:

| 파일 | 내용 |
| --- | --- |
| `contact-after-overview.png` | 거점 12줄 × T2 이상 전부, 2배(64px). **거점 구분 검수용** |
| `contact-before-overview.png` | 같은 배치를 v0.3.1 렌더러로 구운 것. 이전/이후 비교 |
| `contact-after-<site>.png` (12장) | 거점 하나의 T2 이상, 4배(128px). 종별 구분 검수용 |
| `contact-after-t4.png` | 유일 12종, 6배. `contact-before-t4.png`와 비교 |
| `contact-after-shapes.png` | shape 10종 × 변형 6종 어휘표(60 실루엣), 3배 |
| `contact-after-index.md` | 격자 좌표 → 유물 id·이름·shape/변형·palette |

육안 검수에서 잡아 고친 것 셋:

1. **`statue` 입상이 몸통 없이 그려지고 있었다.** `triangle()` 인자 순서 문제로
   v0.3까지 `statue` 종 **전부**가 "머리 + 바닥 막대"였다. 1,900종 규모의 결함이
   컨택트 시트 한 장에서 드러났다(G69).
2. **2px 악센트 테두리가 손글씨 도안의 얇은 구조를 먹었다.** 안티키테라 기계의
   톱니가 전부 테두리색이 됐다 → T4만 1px로 좁혔다(G71).
3. **손글씨 도안의 `+`/`-` 요철이 4단 양자화에서 사라졌다.** 절차 생성용 연속 광원
   공식을 그대로 쓴 탓이다 → 유일 도안에만 문자가 기준 단을 정하는 전용 명암 경로를
   뒀다(G71).
4. **(v0.4.1) 거점 색 편향이 재질을 통째로 덮고 있었다.** 로마 행은 청동이든
   대리석이든 전부 자주색, 한반도 행은 금붙이까지 올리브색이었다. 거점 분류
   95.2%는 합격이었는데 아이콘이 "발굴물"이 아니라 색으로 분류된 UI 칩처럼
   읽혔다 — 한 축만 걸린 합격선이 설계를 그 축으로 끌고 간 사례다. 재질 분류를
   계량했더니 29.6%, 석재는 0/48이었다 → 신호를 테두리 한 채널로 좁히고 재질
   하한을 합격선에 추가했다(G74, §19.2.1).

**32px 축소 검사에 대하여**: 스프라이트는 32×32가 원본 해상도다. 도감 격자 44px·
오버레이 128px은 모두 정수배 확대(`image-rendering: pixelated`)이므로 축소가 일어나는
경로가 없다 — 뭉개질 수 있는 지점이 구조적으로 없다. 검수는 원본 32px 그대로를 확대해
보는 컨택트 시트로 한다.

## 19.5 실사 디테일·이미지 커버리지

| 항목 | 종수 | 상태 |
| --- | --- | --- |
| 실사 디테일(`ArtifactDetail`) | **40종** (T4 12 + T3 28) | 전부 `sourceStatus: "pending"` |
| 실사 이미지(`ArtifactImage`) | **0종** | 수집 미실행 |
| T2 이상 대비 디테일 커버리지 | 40 / 186 = 21.5% | — |

**디테일 40종이 전부 `pending`인 이유**: 이 세션은 외부 네트워크가 막혀 1차 자료를
열어 대조하지 못했다. 확신이 없는 수치·연도는 적지 않았다 — 비워 두는 쪽이 틀린
숫자를 적는 쪽보다 낫다(§1 원칙). `refs`도 그 종의 `source`에 이미 들어 있던 실제
URL만 옮겼다.

**이미지 0종인 이유**: egress 프록시가 위키미디어를 CONNECT 403으로 막는다(재확인).

**0장·일부·전량 어느 상태에서도 화면이 깨지지 않는지 실제로 확인했다.** 합성 이미지
1장을 `app/public/artifacts/`에 넣고 `images.generated.ts`에 항목을 넣어
`lint` → `qa:artifacts` → `build` → `smoke`를 통과시킨 뒤 되돌렸다. 빌드가 `public/`을
`dist/artifacts/`로 복사하는 것과, 상세 블록의 토글 문구가 "상세" → "실물 사진과 상세"로
바뀌는 것, 캡션에 라이선스·저작자·원본 링크가 붙는 것까지 스크린샷으로 확인했다.

스모크가 검사하는 불변 4건(신설):
- 주입한 유일 유물이 소장고에 보인다
- 소장고 상세에 실사 상세 토글이 있고, 펼치면 출처 줄과 소장 경위가 뜬다
- 도감 상세에서도 같은 블록이 열린다
- **드랍 연출(RevealModal)에는 실사 블록이 끼어들지 않는다** (G73 — 연출 보호)

## 19.6 필수 명령 실측 — 전부 통과 (v0.4.1에서 전부 재실행)

```
pnpm --filter relic-king lint          ✅
pnpm --filter relic-king build         ✅  (1,090KB / gzip 214KB — v0.3.1 대비 +3KB)
pnpm --filter relic-king qa:artifacts  ✅  (경고 1건: era 키워드 미겹침 41건 — v0.3부터 있던 휴리스틱)
pnpm --filter relic-king qa:sprites    ✅  (v0.4 신설 · v0.4.1에서 재질 축 합격선 추가)
pnpm --filter relic-king qa:migration  ✅
pnpm --filter relic-king qa:economy    ✅
pnpm --filter relic-king qa:autosell   ✅
pnpm --filter relic-king smoke         ✅  (스크린샷 16장, 신설 2장 포함)
pnpm --filter relic-king sheets        ✅  (컨택트 시트 16장 — v0.4.1 렌더러로 재생성)
pnpm build:vercel                      ✅
```

**밸런스는 재측정하지 않았다.** v0.4·v0.4.1은 표현 계층 작업이고 `valueFactor`·티어·드랍
확률·`minLayer`를 건드리지 않았다. `Artifact`에 더한 필드는 `spriteVariant`(렌더 입력)·
`detail`·`image` 셋이고 전부 정적 데이터라 세이브 스키마와 무관하다 — `qa:migration`이
v2→v8 전 구간을 다시 통과하는 것이 그 증거다. `sim`은 돌리지 않았고 §17.3의 엔딩
실측(140시간 44분)이 그대로 유효하다.

## 19.7 알려진 한계 (v0.4, 보고 대상)

1. **실사 이미지 0장.** egress 허용이 열리면 `--plan` → 검수 → `--apply` 순서로
   채운다. 필요한 도메인과 명령은 `scripts/README.md`와 `notes/artifacts-dataset.md` §15.5.
2. **실사 디테일 40종 전부 `pending`.** 1차 자료 대조가 남았다. 나머지 146종(T2)은
   `--plan`이 긁어 오는 커먼즈 파일 설명을 1차 초안으로 쓴다.
3. **근접쌍 6쌍.** 변형 수 6을 넘는 묶음에서 라운드로빈이 한 바퀴 돌아 생긴다.
   shape당 변형을 8종으로 늘리면 줄어들지만, "윤곽선만으로 갈리는" 실루엣을 8종까지
   찾는 작업이 먼저다.
4. **한반도 → 이집트 오분류 4건.** 금 재질 램프에 녹색 편향을 얹으면 올리브톤이
   되어 이집트의 금-황토와 겹친다. 색상환 배치만으로는 못 고친다 — 재질별로 편향
   방향을 다르게 주는 방법이 후보지만, 그러면 "같은 거점은 같은 색조"라는 읽기가 흔들린다.
5. **T0~T1 1,763종은 거점 신호가 없다.** 의도한 것이다(작업 지시 §1). 다만
   "이 아이콘이 어느 거점 것인지" 읽기가 상위 티어에서만 가능하다는 사실은 기록해 둔다.
6. **변형 배정이 데이터셋 크기에 의존한다.** 같은 `(거점, shape, palette)` 묶음에
   종을 더하면 그 묶음의 변형이 밀린다. 한 릴리스 안에서는 완전히 결정론적이다(G69).

---

## 20. v0.4 — 실제 세계지도 (`notes/decisions.md` G75)

측정일 2026-09-20. 명령은 `pnpm --filter relic-king build` / `sim --hours 168` /
`smoke`, 그리고 `node scripts/build-worldmap.mjs`.

### 19.1 rome 좌표 이동의 밸런스 영향 (요구된 전후 비교)

`rome`의 좌표를 나폴리(40.85/14.27)에서 폼페이 유적(40.75/14.49)으로 옮겼다.
`distanceKm`이 바뀌므로 원정 시간이 바뀐다 — 168시간 방치 시뮬을 **좌표 변경
전후로 각각 한 번씩** 돌려 비교했다. `balance.ts`는 건드리지 않았다.

| 지표 | 변경 전(40.85/14.27) | 변경 후(40.75/14.49) | 차이 |
| --- | --- | --- | --- |
| **엔딩(v0.2) 도달** | **140시간 44분** | **140시간 42분** | **−2분** |
| 이집트 해금 | 2시간 38분 | 2시간 38분 | 0 |
| 12층 도달 | 4시간 55분 | 4시간 55분 | 0 |
| 첫 유일(T4) | 2시간 18분 | 2시간 18분 | 0 |
| 도감 | 1427 / 1902 (75%) | 1427 / 1902 (75%) | 0 |
| 자산 | 1,197억 ₩ | 1,387억 ₩ | +15.9% |
| 명성 축 | 0.1802 | 0.2227 | +0.043 |
| 레이스 승/패 | 451 / 37 | 455 / 36 | +4 / −1 |

**판정: 좌표 이동을 택한다.** 기준은 "엔딩 도달 시간 ±1시간 초과면 이름 쪽을
고친다"였고 실측은 2분이다.

자산·명성이 15% 넘게 움직인 건 좌표 때문이 아니라 **난수열 발산**이다 — 이동
시간이 바뀌면 발굴단 도착 틱이 달라지고, 그 뒤의 모든 드랍·제보 판정이 다른
난수를 뽑는다. 이 시뮬은 단일 시드 1회 실행이라 자산·명성처럼 꼬리가 두꺼운
지표는 이 정도 흔들린다(§2가 "드랍 횟수와 층을 비교한다"고 적어 둔 이유와 같다).
실제로 결정론적인 축(도감 종수, 해금·12층·첫 T4 시각)은 **완전히 일치**한다.
엔딩 판정에 쓰이는 종합 점수는 0.4926 → 0.5113으로 올랐으나 엔딩 시각은 그대로다.

거리 변화 실측(폼페이 ↔ 나머지 11거점): −22km(룩소르) ~ +21km(멕시코시티),
절댓값 최대 22km. 전체 거리 행렬은 `notes/world-map.md` §2.

### 19.2 해안선 베이크

| 항목 | 값 |
| --- | --- |
| 입력 | `world-atlas@2.0.2` `land-110m.json` (Natural Earth 1:110m, 퍼블릭 도메인) |
| 그리드 | 320 × 160 (정거방형), 도트당 4×4 표본, 임계 0.5 |
| 육지 도트 | 15,566 / 51,200 (30.4%) |
| 산출 크기 | 비트마스크 6,400 B → base64 8,536자 (§6이 정한 6.4KB 상한 이내) |
| 거점 앵커 보정 | 2건 (아테네·이스탄불) |
| 런타임 번들 내 `world-atlas`/`topojson` 문자열 | **0건** (`dist/assets/*.js` grep) |

베이크 중 잡은 결함 2건은 `notes/decisions.md` G75.1에 적었다(날짜변경선 언랩
누락으로 남태평양이 육지로 나왔던 것, 해안 도시 도트가 바다로 이진화된 것).

### 19.3 스모크 — 작업 지시 §4 통과 기준

`pnpm --filter relic-king smoke` **✅ PASS**, 스크린샷 15장.

| 기준 | 결과 | 근거 |
| --- | --- | --- |
| `08-worldmap`에서 해안선으로 대륙 식별 (일본·영국·동남아 도서부) | ✅ | 스크린샷 육안 |
| 12마커 전부 육지 위 (바다에 뜬 마커 0개) | ✅ | 베이크가 12거점 도트를 전수 검사하고, 아니면 빌드를 깬다 |
| 12라벨 전부 읽힘 · 겹침 0 · 생략 0 | ✅ | `data-labels-omitted=0`, `data-labels-drawn=12` 단언 + 지중해 구간 확대 육안 |
| 권역 줌에도 해안선 | ✅ | `08b-worldmap-region`, 비-바다 픽셀 47.6% (임계 5%) |
| `mobile-04-worldmap` 12마커 전부 보임 · 가로 스크롤 없음 | ✅ | 마커 12/12가 뷰포트 안, `scrollWidth − clientWidth = 0` |
| 키보드만으로 거점 선택 → 파견 도달 | ✅ | 포커스 → Enter → 권역 → Enter → 파견 시트 제목 확인 |
| 도시명이 12화면에서 같은 규칙으로 표시 | ✅ | 시트 제목 "도시 — 앵커" + 부제 "나라 · 편도 N km" 단언, 나머지는 육안 |

새로 추가한 스모크 케이스: (a) 라벨 생략/개수 단언, (b) 권역 줌 스크린샷 +
비-바다 픽셀 비율, (c) ESC 복귀, (d) 키보드 파견 경로 + 포커스 순서가 거리순인지,
(e) 모바일 마커 개수·가로 스크롤·라벨 생략.

### 19.4 자동 검증

| 항목 | 방법 | 결과 |
| --- | --- | --- |
| 타입체크 + 빌드 | `pnpm --filter relic-king build` | ✅ 에러 0 |
| 유물 데이터 QA | `pnpm --filter relic-king qa:artifacts` | ✅ 통과(era 키워드 경고 41건은 기존값) |
| 콘솔/페이지 오류 | 스모크 CDP 로그 | ✅ 0건 |
| 조사 병기 | 스모크 활동 기록 검사 | ✅ 없음 |

### 19.5 못 한 것

- **플레이테스트 0회.** §17.9·§18.6과 같다. 키보드 경로가 "실제로 쓸 만한지"는
  사람이 Tab을 눌러 봐야 안다. 스모크가 보는 건 "포커스가 가고 Enter가 먹는다"까지다.
- **스크린리더 실기 검증 없음.** `aria-label` 문자열과 포커스 순서는 단언하지만,
  실제 리더(NVDA·VoiceOver)로 들어 보지 않았다.
- **라벨 배치는 현재 데이터에서만 검증됐다.** 12거점·현재 도시명 기준으로 생략
  0건이다. 거점이 늘거나 이름이 더 길어지면 다시 터질 수 있고, 그때는 후보를
  늘리는 대신 §6이 적어 둔 "권역 줌 전체 라벨 + 세계 줌 묶음 표기"로 간다.
  리더선끼리 교차하는 경우(이스탄불·아테네)가 있는데, 각 라벨이 제 마커로
  추적 가능해 결함으로 치지 않았다 — 교차 회피는 넣지 않았다.
- **`build-worldmap.mjs`를 정규 빌드에 걸지 않았다.** `pnpm build`가 매번
  3.6MB TopoJSON을 다시 굽게 하면 배포 빌드가 devDependency 설치에 묶인다.
  산출물을 커밋하고 `--check` 모드를 손으로 돌리는 쪽을 택했다 — 대신
  "산출물이 최신인지"는 CI가 자동으로 보지 않는다는 뜻이다.

---

## 21. v0.5 — 플레이어 간 경쟁: 기록패와 고스트 (`notes/decisions.md` G76)

측정 환경: 이 레포, `pnpm --filter relic-king sim -- --hours 168 [--ghosts N]`,
`pnpm --filter relic-king qa:rivalcard`, `pnpm --filter relic-king smoke`.

### 20.1 기록패 크기 — 세이브 코드는 왜 못 쓰는가

| 대상 | 크기 | 비고 |
| --- | --- | --- |
| 세이브 코드(`exportText`), 갓 만든 월드 | 206KB | `codex` 2,000키 + `ledger` 2,000키 |
| 세이브 코드, 24시간 진행 후 | 직렬화 561KB / base64 **749KB** | 플레이 시간과 무관하게 처음부터 크다 |
| **기록패**(`encodeCard`), QA 기준 월드 | **204자** | 목표 512자 |
| **기록패**, 스모크 실플레이(8시간 오프라인 포함) | **162자** | 유일 미소장이라 `u`가 비어 더 짧다 |

세이브 코드를 공유 통로로 쓸 수 없다는 것이 별도 스키마를 만든 이유다. 실측 3,700배
차이다.

### 20.2 왕복 — 3축이 그대로 돌아오는가

QA 기준 월드(40분 진행 + 관람객 24만 + 유일 2종 소장)의 점수와, 그 기록패를
**다른 세계**에 고스트로 들였을 때 `fullRanking()`이 매긴 점수.

| 축 | 원본 | 고스트 | 차이 |
| --- | --- | --- | --- |
| 자산 | 0.0007 | 0.0007 | 0 |
| 도감 | 0.0331 | 0.0331 | 0 |
| 명성 | 0.3233 | 0.3233 | 0 |

명성이 맞는 것은 `fameExtra` 덕분이다. 라이벌 채점식에는 관람객 항이 없어서
(NPC는 박물관을 짓지 않는다) 그 몫을 따로 들지 않으면 사람 상대의 명성만 낮게 찍힌다.

### 20.3 적대적 입력 — 21종 전부 `null`

빈 문자열 · 공백 · 잘린 코드 · base64가 아닌 문자열 · 1MB 문자열 · JSON 배열 ·
버전 없음 · 다음 버전 · 자산 축 999 · 도감 축 음수 · 명성 `"NaN"` · 인부 `"Infinity"` ·
인부 상한 초과 · 인부 소수 · 장비 등급 초과 · 없는 거점 · 없는 유일 id ·
유일이 아닌 종을 유일이라 주장 · 유일 13종 · 체크섬 조작 · 본문만 바꾸고 체크섬 유지.

**전부 `null`이고 예외가 새지 않는다.** `__proto__`는 별도로 본다 — 모르는 키를 버리는
게 파서의 규칙이라 거부가 아니라 무시가 옳고, 확인할 것은 프로토타입 오염 여부와
"파싱 결과가 스키마 안의 11개 키만 갖는가"다. 둘 다 통과한다.

### 20.4 엔딩 도달 시간 — 고스트가 밸런스를 바꾸는가

168시간 방치(클릭 0회), 고스트는 **엔딩 시점 플레이어 세기**(인부 77 · 장비 Lv.10 =
9,423/s, 자산 0.5691 · 도감 0.7503 · 명성 0.2227)로 3명.

| 구성 | 엔딩 도달 | 도감 | 레이스 승/패 |
| --- | --- | --- | --- |
| 기준선(고스트 0) | **140시간 42분** | 1427/1902 (75%) | 455 / 36 |
| 고스트 3명(최종 구현) | **141시간 3분** (+21분) | 1427/1902 (75%) | 445 / 51 |

**+21분으로 ±1시간 기준 안이다.** 차이는 제보 레이스 패배가 36건 → 51건으로 는 것에서
온다 — 그게 고스트가 내 판에 실제로 영향을 주는 유일한 경로다(§20.6).

여기까지 오는 데 실기가 두 번 있었다. 둘 다 "받으면 손해"를 만드는 결함이었다.

| 시도 | 고스트 모델 | 168시간 결과 |
| --- | --- | --- |
| 1차 | 발굴력을 인부 수로 역산 + NPC 재투자 | **엔딩 미달성**. 고스트 발굴력 9,423 → **4,243,517/s**(450배), 도감 74% |
| 2차 | 인부·장비를 그대로 이관, 배경 발굴이 원장 소모 | **엔딩 미달성**. 도감은 85%까지 갔는데 고스트가 종합 1위를 차지해 판정이 막힘 |
| 최종 | 상태 이관 + 원장 비소모 + 재투자 없음 + 엔딩 판정 제외 | **141시간 3분** |

1차의 폭주는 `gear = 0`을 가정한 역산 때문이다(인부 8,566명으로 환산된 뒤 그 위에
장비가 13단계 얹혔다). 2차는 도감 목표는 넘겼는데 "종합 1위 유지" 조건에서 걸렸다 —
이미 완주한 친구의 기록패 하나가 내 완주를 영구히 막는 구조였다.

### 20.5 고스트의 성장 — 기록된 페이스

| | 33시간 뒤 발굴력 |
| --- | --- |
| 고스트(기록 9,423/s) | **9,423/s** — 변함 없음 |
| NPC(같은 기간) | 재투자로 계속 증가 |

168시간 시뮬에서 고스트 셋은 9,423 / 9,978 / 9,991/s로 끝났다. 9,423을 넘은 둘은
게임의 추격 보정(`catchup`)이 붙은 것이지 스스로 장비를 산 것이 아니다.

### 20.6 격리 — 고스트가 내 세계에서 할 수 있는 것

| 경로 | 내 원장을 가져가나 | 검증 |
| --- | --- | --- |
| 기록패가 주장하는 유일 소장 | **아니다** | 고스트를 받아도 원장·도감이 한 칸도 안 변한다. 상대가 가졌다고 적어 온 유일이 내 세계에 그대로 `remaining = 1`로 남아 있다 |
| 배경 발굴(33시간, 125종 획득) | **아니다** | 고스트의 `owned` 125종이 내 원장의 `owners`에 하나도 없다. 같은 기간 NPC는 실제로 가져갔다(비교군) |
| 제보 레이스 | **그렇다** | §20.4의 패배 +15건이 그것이다 |

### 20.7 상한과 교체

| 항목 | 결과 |
| --- | --- |
| 상한까지 채운 위조 기록패(인부 500 · 장비 Lv.16) | 받는 순간 **28,224/s로 잘린다**(상한 30,000/s). 장비 등급은 Lv.16 그대로 — 깎는 곳은 인부 한 곳뿐 |
| 고스트 5명 연속 투입 | 3명 유지(`GHOST_MAX`), NPC 6명은 밀려나지 않음 |
| 같은 기록패 두 번 투입 | 1명(교체) |
| 이름만 바꾼 기록패 | 같은 키 — 개명으로 고스트가 둘이 되지 않는다 |

### 20.8 세이브

| 항목 | 결과 |
| --- | --- |
| 고스트 세이브 왕복 | ✅ 출처 정보(`ghost.key`)까지 보존 |
| v8 → v9 승격 | ✅ 라이벌 6명 그대로, 고스트 전용 필드 안 붙음, `rankSample` 없어도 정상 |
| `qa:migration` 전체(v1~v9) | ✅ 통과 |

### 20.9 회귀 게이트

| 항목 | 명령 | 결과 |
| --- | --- | --- |
| 타입체크 + 빌드 | `pnpm --filter relic-king build` | ✅ 에러 0 |
| 기록패 QA | `pnpm --filter relic-king qa:rivalcard` | ✅ 전부 통과 |
| 마이그레이션 | `pnpm --filter relic-king qa:migration` | ✅ 통과 |
| 기존 QA 8종 | `qa_season_rollover`·`qa_expedition`·`qa_artifacts`·`qa_economy`·`qa_autosell`·`qa_endgame`·`qa_growth`·`qa_sigma1` | ✅ 전부 통과 |
| 스모크 | `pnpm --filter relic-king smoke` | ✅ PASS — 기록패 162자, 왕복 후 순위표 8행(고스트 1) |
| 콘솔/페이지 오류 | 스모크 CDP 로그 | ✅ 0건 |

스모크는 실제 UI 경로를 탄다 — 클립보드로 복사 → `window.prompt`로 붙여넣기 →
고스트 행 렌더까지 한 번에 본다. 데스크톱·모바일 양쪽에서 고스트 행의 "언제 받은
기록인지" 표기가 살아 있는지도 단언한다(모바일은 `.rank-dig`를 숨기므로 고스트 행만
예외로 남겼고, 그 예외가 죽으면 모바일에서만 실시간 상대처럼 보인다).

### 20.10 못 한 것

- **두 사람으로 실제로 주고받아 보지 않았다.** 스모크가 하는 것은 내 기록패를 내가
  다시 붙여 넣는 왕복이다. 메신저를 거치며 코드가 잘리거나 공백이 섞이는 실제 경로는
  사람이 해 봐야 안다(파서는 그런 입력을 `null`로 떨어뜨리도록 검증돼 있다).
- **위조 방어는 없다. 앞으로도 없다.** 클라이언트 권위 구조에서 불가능하다(G76.6).
  상한과 격리로 "판을 못 쓰게 만들지 못한다"까지만 보장한다.
- **추월 예상의 정확도는 재지 않았다.** 10분 표본 두 점을 잇는 선형 외삽이라,
  성장이 계단식인 구간(거점 해금·발굴단 추가)에서는 크게 흔들린다. 화면이
  "측정 중 / 못 넘는다 / N시간 뒤"를 구분해 적는 것으로 갈음했다.
- **고스트는 상대의 단독 발굴만큼만 세다.** 발굴단(`teams`) 몫은 기록패에 담지
  않는다 — 라이벌에게는 태울 자리가 없다(G76.2).

# 22. v0.3.2 — 플레이 완결성 패스 (코어 루프·세부 조작·밸런스의 UX 실측)

작업 지시: `prompts/v0.3.2-play-integrity.md`. 판단 근거는 `notes/decisions.md` G77.

**이 절이 이전 절들과 다른 점**: §1~§18의 측정은 전부 `sim/run.ts`(헤드리스 엔진)
경유였다(§15.5가 직접 그렇게 적어 뒀다). 이 절은 **실제 React 앱을 사람처럼 조작해**
잰 값이다. 판정은 화면 문구가 아니라 앱이 실제로 저장한 `world` 상태에서 읽는다.

## 22.0 수단 — UI 실조작 하니스

```
pnpm --filter relic-king play              전체 시나리오
pnpm --filter relic-king play -- tip loop  골라서
pnpm --filter relic-king play -- --list
```

- `tests/e2e/harness.mjs` — CDP 드라이버(클릭·텍스트·스크린샷·세이브 주입·가상 시계).
- `tests/e2e/play.mjs` — 시나리오 10종, 판정 70건. 보고서는 `assets/screenshots/play/report.json`.

`smoke.mjs`와의 분업: smoke는 **화면이 깨지지 않았는지**(레이아웃·스크린샷),
play는 **게임이 의도대로 도는지**(조작 → 상태 변화)를 본다. 둘 다 유지한다.

### 함정 하나 — 세이브는 10 게임초마다만 쓰인다

조작 직후 `localStorage`를 읽으면 **조작 이전 스냅샷**이 잡힌다. 이 하니스를 쓰며
처음 만난 함정이고, 처음엔 "매각 버튼이 눌렸는데 아무 일도 안 일어난다"는 거짓
결함으로 보였다. 앱이 이미 가진 저장 경로(`visibilitychange` 핸들러)를 눌러 즉시
기록시킨 뒤 읽는다(`h.state()`) — 게임 상태를 바꾸지 않는 조작이다.

### 가상 시계가 측정을 왜곡하지 않는다는 근거

방치형은 실시간으로 20분을 기다려야 20분이 흐르므로 12배속으로 돌린다
(`Date.now`·`performance.now`·rAF 타임스탬프를 한 원점·한 배율로 묶는다).
같은 지점(t=180초)까지 네 배속으로 각각 돌린 대조:

| 배속 | 실시간 | drops | 인부 | 장비 | 감정소 | 층 | 시계 손실 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 180.6초 | 16 | 2 | 0 | 1 | 2 | 0.16% |
| 6 | 30.4초 | 17 | 2 | 0 | 1 | 2 | 0.55% |
| 12 | 15.6초 | 17 | 2 | 0 | 1 | 2 | 1.93% |
| 24 | 8.0초 | 17 | 2 | 0 | 1 | 2 | 3.92% |

단조 증가 지표는 전부 일치한다. `funds`는 매각·수수료로 초 단위로 요동쳐 샘플
시각이 3초만 달라도 비교가 성립하지 않으므로 이 대조에서 제외했다. 경과는
실시간이 아니라 항상 `world.t`로 기다리므로, 시계가 잘려도 측정값은 오염되지
않고 실행이 길어질 뿐이다. 기본값 12배속(손실 1.9%, 대부분 마운트 전 공백).

## 22.1 결함 1 (치명·B) — 배경 탭에 둔 시간이 통째로 사라졌다

| | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| 배경 240 게임초 중 반영된 시간 | **1.4초 (0.6%)** | **241.4초 (100.6%)** |
| 그 동안의 드랍 | 3 → 3점 (0점) | 3 → 19점 |
| 복귀 안내 | 없음 | 요약 노출 |

탭을 **닫았다 열면** 60% 효율로 정산되므로, 수정 전에는 **닫는 편이 이득**이었다.
원인과 세 겹 수정은 `notes/decisions.md` G77.1. 측정은 CDP로 탭을 실제로 얼려서
한다(`play background`) — 배경 탭에서는 `requestAnimationFrame`이 아예 멈추므로
프레임 루프에 gap 감지를 넣는 것만으로는 부족하다는 것도 그렇게 확인했다.

## 22.2 결함 2 (B) — 제보 배너가 "너는 못 낀다"고 거짓말했다

발굴단을 꾸리기 전의 **모든** 제보가 "반응할 발굴단이 없다 — 정보로만 뜬다"로 떴다.
엔진은 같은 상황에서 레거시 직접 발굴에 `TIP_PLAYER_HIT`(28%)를 그대로 적용하고
있었다. 제보 간격이 평균 180초라 첫 세션에서만 5~6번 연속으로 이 문구를 본다 —
이 게임의 **유일한 긴장 장치**(재미 정의 ②)가 거기서 죽었다.

- 수정 전: `배너의 '반응 가능' 안내가 엔진 판정과 일치한다` ❌
- 수정 후: ✅ — `직접 발굴이 이 자리에서 쫓는 중 — 적중 28%`

## 22.3 결함 3 (치명·B/C) — 감정 파이프라인이 주기적으로 통째로 멈췄다

B(교착)이면서 C(기준 자체가 이 경험을 못 잡았다)다 — §16.1이 고친 "기본 상태
방치가 진행되지 않는 교착"의 **재발**이다. 그때는 완전
교착이라 눈에 띄었고, 이번엔 주기적 교착이라 총량 기준 게이트(통화 성장·엔딩
도달)를 전부 통과하고 있었다. 깨지는 건 오직 그 몇 분 동안 화면 앞에 앉아 있는
사람의 경험이다 — 드랍은 쌓이는데 감정도 매각도 수입도 전부 0인 구간.

**시드 5개 × 1.5시간 중앙값** (`pnpm --filter relic-king qa:pipeline`,
수정 전은 같은 스크립트를 v0.3.1 커밋 worktree에서 실행):

| 지표 | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| 정지 시간 비율 | **23.4%** | **4.1%** |
| 최장 연속 정지 | **354초** | **59초** |
| 드랍 | 648 | 652 |
| 감정 후 매각 | 437 | **479** |
| 미감정 매각 | 19 | **5** |
| 소장 | 158 | 154 |
| 도감 | 111 | 110 |
| 자산(십억₩) | 2.9 | 3.25 |
| 발굴력 | 280 | 273 |

정지가 5.7배 줄었고 진행 지표는 통계적으로 그대로다. 미감정 매각 19→5는 개선이다
(큐가 터질 때까지 몰아서 헐값에 던지던 것이 제때 감정해 파는 쪽으로 바뀌었다).
남은 4.1%·최장 59초는 자동 루틴 주기(60초)가 만드는 하한이다.

**단일 시드로 판정하지 않은 이유**: 첫 대조는 시드 하나였는데 자산이 152.1억 →
34.0억으로 4.5배 나빠진 것처럼 보였다. 자산은 T2·T3 몇 점이 좌우하는 두꺼운 꼬리
분포라 단일 시드 비교가 성립하지 않는다 — 시드 5개 중앙값에서는 2.9 → 3.25로
오히려 미세하게 나은 쪽이었다.

**게이트 신설**: `qa:pipeline`. 기준은 정지 비율 중앙값 ≤10%, 최장 ≤90초,
어느 시드도 ≤15%, 도감 진행 ≥80종, 미감정 매각 < 감정 후 매각의 10%.

## 22.4 결함 4 (A) — 유일(T4)을 손에 넣는 순간에 아무 일도 일어나지 않았다

`spec.md` §3.3은 T4에 대해 **드랍(소유 확정) 시점**에, "감정소 레벨과 무관하게"
전체 연출을 재생하라고 못박았다. 코드는 `report.appraised`만 보고 있었고,
`APPRAISAL_UNLOCK_LAB_LEVEL[4] = 4`라 감정소 Lv.1~3에서 유일을 얻으면 **봉인
보관으로 들어가며 아무 신호도 나지 않았다** — 재미 정의 ①이 걸리는 바로 그 순간이
비어 있었다.

검증은 상태를 주입하지 않는다. 제보를 유일 유물로 걸어 **엔진의 `take()`가 실제로
실행되는 프레임**을 만들고, 그때 화면에 무엇이 뜨는지를 본다:

| 판정 | 수정 전 | 수정 후 |
| --- | :---: | :---: |
| 제보 레이스로 유일을 실제로 획득했다 | ✅ | ✅ |
| 획득 순간(감정 전) 전체 연출이 뜬다 | **❌ 아무 연출도 없다** | ✅ |
| 그 연출에 "세계에 단 하나" 배지가 있다 | — | ✅ |
| 세계 재고가 0이 됐다(owners=player) | ✅ | ✅ |

`spec.md` §9.2의 분리를 화면에서도 지킨다 — 획득 판은 이름·내력·평가액을 말하지
않고("이름과 내력은 감정이 끝나면 공개된다"), 그건 감정 완료 판의 몫이다.

## 22.5 결함 5 (B) — 드랍 바닥값 구간이 보이지 않았다

§18.3이 "드랍 횟수가 정확히 같다"로 관측한 그 구간. UI에서 직접 확인했다 —
최대 층에서 **발굴력을 10배로 올려도 드랍률이 7.4 → 7.6점/분으로 그대로**인데,
어느 화면도 그 사실을 말하지 않았다. 발굴력 숫자만 계속 올라간다.

밸런스는 건드리지 않았다. 이건 규칙이 틀렸다는 문제가 아니라 **규칙을 숨기고
있었다**는 문제다(척추 5번). 헤더 "다음 드랍" 줄에 `· 최소 간격 8초 도달`을,
발굴 탭 카드에 그 뜻(여기 더 넣지 말고 새 거점·시설·발굴단에 쓰라)을 적는다.

같은 이유로 **단장 고용 카드**에도 한 줄을 붙였다. 자금이 자동 재투자로 60초마다
요동쳐 고용 버튼이 켜졌다 꺼졌다 하는데(고용 가능 상태 유지 비율 **9.3%** 실측),
왜 잔고가 안 쌓이는지 어디서도 알 수 없었다.

## 22.6 결함 6 (B) — 복귀 요약이 "자금 +0 ₩"이라고 했다

6시간 복귀 실측 문구: **"유물 1,619점 발굴 · 자금 +0 ₩"**. `Math.max(0, …)` 클램프
때문이다 — 복귀 직후 자동 재투자가 번 돈을 인부·장비·감정소에 쓰면 순변화가 음수가
되는데, 그걸 0으로 잘라 **"벌지 못했다"와 "벌어서 썼다"를 같은 문장으로** 만들고
있었다. spec.md §3.3 "복귀 요약은 손실을 숨기지 않는다"는 이득도 지출도 숨기지
말라는 뜻이다. 부호를 그대로 쓰고, 줄었으면 그 이유를 같이 적는다.

수정 후 같은 자리: `자금 +4290만 ₩ (급여·유지비 차감후)`.

## 22.7 코어 루프 1회전 — UI 실조작 완주 (작업 지시 §5.2)

발굴 → 드랍 → 감정 → 소장 → 매각 → 재투자 → 다시 발굴. 전부 ✅ (`play loop`).

| 단계 | 판정 | 실측 |
| --- | :---: | --- |
| ① 발굴 — 클릭 0회로 진척이 돈다 | ✅ | layer 1 · progress 42 |
| ② 드랍 — 유물이 실제로 나온다 | ✅ | 2점 |
| ③ 감정 — 미감정이 소장고로 넘어간다 | ✅ | 소장 5점 · 매각 2건 |
| ④ 소장 — 소장고 화면에 실제로 보인다 | ✅ | 6종 |
| ⑤ 매각 — 상세 → 매각 → 자금 증가·사본 감소 | ✅ | 6,238 → 15,941₩ · 빠진 사본 1점 |
| ⑥ 재투자 — 방치 중 발굴력이 자란다 | ✅ | 인부 0→3 |
| ⑦ 루프 — 한 바퀴 뒤에도 드랍이 계속 는다 | ✅ | 2 → 28점 |
| 콘솔 오류 | ✅ | 0건 |

## 22.8 세션 3지점 재현 (작업 지시 §5.3)

### (a) 첫 세션 20분 — `play first`

| 마일스톤 | 실측(게임초) | 20분(1,200초) 예산 |
| --- | ---: | :---: |
| 거점 — "본거지를 정하자" 온보딩 | 41 | ✅ |
| 감정 — 소장고 입고 | 48 | ✅ |
| 전시 — 등급0 임시 전시대 | 77 | ✅ |
| 제보 — 배너 + 참가 안내 | 1,077 | ✅ |
| 원정 — 단장 고용 가능(200,000₩) | **1,301** | ❌ (20분 초과) |

원정만 예산 밖이고, 그마저 **시점이 흔들린다** — 고용 가능 상태가 유지되는 비율은
40분 창에서 9.3%, 헤드리스에서 자동 루틴 직후만 보면 처음 도달이 2,100초(35분),
0.5초 간격으로 재면 971초(16분 11초)다. 판정과 처방은 `notes/decisions.md` G77.5
(**문서를 고친다** — `brief.md` §첫 세션 1이 약속한 "발굴단 1팀 자동 배치"는 구현된
적이 없고, 레거시 직접 발굴이 그 자리를 대신하기로 G52.7이 이미 결정했다).

### (b) 복귀 세션 8h·24h — `play return`

| 검사 | 8h | 24h |
| --- | :---: | :---: |
| 복귀 요약이 뜬다 | ✅ 토스트(조치 0건) | ✅ 토스트(조치 0건) |
| 경과 시간을 말한다 | ✅ "7시간 59분 동안" | ✅ "12시간 0분 동안"(상한 적용) |
| 오프라인 적분이 실제로 돈다 | ✅ t 156 → 28,988 | ✅ +43,200초(12h 상한) |
| 닫으면 게임으로 돌아온다 | ✅ | ✅ |

**2층 구조의 모달 경로**(조치 필요 ≥1건)는 도난 1건을 심어 따로 열었다:
모달 전환 ✅ · 상단 고정 4줄 ✅ · 자금·유물·도감·순위를 전부 말한다 ✅ ·
조치 카드 노출 ✅(🔴 도난 — 금동미륵보살반가사유상).

### (c) 중반 이후 — `play steps`

12거점 전부 보유·층 6·자금 충분한 세이브를 주입한 상태에서 주요 액션 10종이
전부 동작하고 화면이 관리 가능한 상태로 남는지 확인했다(아래 §22.9 표).

## 22.9 `notes/ux-v02.md` 수치 약속 전수 검사 (작업 지시 §5.4)

### 조작 단계 수 (§2 — 시작 화면은 발굴 탭, 탭 전환도 1단계)

| # | 액션 | 예산 | 실측 | |
| --- | --- | :---: | :---: | :---: |
| 1 | 인부 고용 | 3 | 1 | ✅ |
| 2 | 발굴단 파견(신규 거점) | 3 | 3 | ✅ |
| 3 | 소장고에서 매각 | 3 | 3 | ✅ |
| 4 | 미감정 즉시 매각 | 3 | 2 | ✅ |
| 5 | 박물관 전시 | 3 | 3 | ✅ |
| 6 | 암시장 매입 | 3 | 2 | ✅ |
| 7 | 규칙 확인(📋) | 2 | 1 | ✅ |
| 8 | 순위표 확인 | 2 | 1 | ✅ |
| 9 | 설정 열기(⚙) | 2 | 1 | ✅ |
| 10 | 도감에서 유물 상세 | 3 | 2 | ✅ |

**3을 넘는 행이 없다** — §2 표의 약속이 실코드에서도 지켜진다. (§2는 18종을
세지만, 나머지 8종은 이 패스의 하니스로 재현 비용이 큰 항목이라 미측정으로 남긴다.)

### 알림 3계층 (§7)

| 계층 | 검사 | 수정 전 | 수정 후 |
| --- | --- | :---: | :---: |
| L1 | 제보 슬롯이 min-height로 고정(레이아웃이 안 밀린다) | ✅ 46px | ✅ |
| L1 | 동시 1장 | ✅ | ✅ |
| L1 | 카운트다운이 보이고 실제로 줄어든다 | ✅ 2:18 → 2:00 | ✅ |
| L2 | 도난이 소장고 탭 배지로 뜬다 | ✅ | ✅ |
| L2 | 암시장 장물이 시장 탭 배지로 뜬다 | ✅ | ✅ |
| L2 | 배지가 **카운트다운**이다("시한부 뱃지") | **❌ 건수만** | ✅ `5h` / `72h` |
| L2 | 임박(6h 이내)일 때만 붉어진다 | — | ✅ |
| L3 | 활동 기록이 도감 탭 원장에 쌓인다 | ✅ | ✅ |
| L3 | 조사 병기("이(가)" 등)가 없다 | ✅ | ✅ |

### 규칙 공개 화면(📋) — 화면의 숫자가 실제 상수와 같은가 (척추 5번)

9종 전부 일치 ✅ — 3축 가중식(30/35/35), 제보 플레이어 적중 28%, 라이벌 10%,
집중 굴착 60%, 오프라인 효율 60%, 도난 회수 창 72h, 오프라인 상한 12시간,
라이벌 현황 6행(반칙하지 않는다는 확인).

### 온보딩 (§1.5)

첫 감정 시점(t=41초)에 뜬다 ✅ · 추천 카드 3장 ✅ · "지금 거점 유지"가 1단계로 닫는다 ✅.

## 22.10 재미 가설 — 자동으로 닫은 것과 사람이 필요한 것 (작업 지시 §5.5)

§6의 4개를 그대로 미검증으로 넘기지 않고, **원리적으로 사람이 필요한 부분**과
**자동으로 반증 가능한 부분**을 갈랐다.

| §6 가설 | 자동으로 닫은 것 | 사람이 필요한 것 |
| --- | --- | --- |
| T4 획득 직후 소장고·도감을 다시 여는가 | 획득 순간 연출이 뜨고(§22.4), 세계 재고가 0이 되고, 소장고/봉인 보관에 실제로 들어간다 | 그래서 **다시 여는가** |
| 제보 배너가 뜨면 개입하는가 | 배너가 20분 안에 뜨고(t=1,077초), 카운트다운이 정확히 줄고, 반응 경로가 열려 있다고 **정직하게** 표시된다(§22.2) | 개입률 30% 이상인가 |
| 유물 내력을 끝까지 읽는가 | 내력이 감정 완료 시점에 실제로 공개되고 도감 상세에 남는다 | **읽는가** |
| 팔지 말지 망설이는 순간이 세션당 1회 이상인가 | 매각이 자산 축을 실제로 깎고(§18.3), 되돌릴 수 없고, 화면이 그 트레이드오프를 적고 있다 | **망설이는가** |

즉 "그 순간이 오는가"는 전부 자동으로 닫았고, "그때 사람이 무엇을 하는가"만 남았다.
남은 넷은 사람이 앉아야 답이 나온다 — 재현용 세이브와 스크린샷을
`assets/screenshots/play/`에 남겨 둔다.

## 22.11 밸런스 — 기본값 경로 회귀 없음

| 게이트 | 결과 |
| --- | --- |
| `build`(tsc + vite) | ✅ |
| `sim --hours 168` — 엔딩 도달 | ✅ **140시간 42분** (v0.3 기준선 140시간 44분, 차이 −2분 = 0.02%) |
| 엔딩 시점 3축 | 자산 0.5748 · 도감 0.7503(1,427/1,902종) · 명성 0.2254 · 종합 0.5139 |
| `qa:migration` | ✅ |
| `qa:artifacts` | ✅ (era 키워드 경고 41건은 기존과 동일) |
| `qa:economy` | ✅ |
| `qa:expedition` | ✅ (스텝 무관성 유지 — 이번 수정은 `advance()` 경로 밖이다) |
| `qa:season` | ✅ |
| `qa:autosell` | ✅ |
| `qa:pipeline` (신설) | ✅ |
| `smoke --seconds 45` | ✅ 스크린샷 14장 |
| `play` (신설, 시나리오 10종) | ✅ 판정 70건 전부 통과 |

**§18.3 대조표가 갱신됐다.** 정지 구간이 사라지면서 168시간 방치의 절대값이
달라졌다(기능 끔: 소장가치 183.6억 → **60.05억**, 자산축 10.45% → **3.42%**).
그 절의 **결론은 그대로다** — 드랍 횟수가 두 런에서 여전히 같고(75,539 / 75,540),
켜면 소장가치·자산축이 내려가고 자금이 회수된다는 트레이드오프 방향도 같다.

## 22.12 스크린샷 — 무엇을 증명하는 장면인가

`assets/screenshots/play/` (`pnpm --filter relic-king play` 재실행 결과).
기계 판독용 원본은 같은 폴더의 `report.json`(판정 74건 전부).

| 파일 | 증명하는 것 |
| --- | --- |
| `01-loop-vault.png` · `02-loop-dig.png` | 코어 루프 1회전 — 소장고에 실제로 쌓인 상태와, 재투자로 발굴력이 자란 발굴 탭 |
| `01-first-onboarding.png` | 첫 감정 시점(t=41초)에 뜨는 "본거지를 정하자" — 추천 카드 3장 |
| `02-first-displayed.png` | 첫 세션 안(t=77초)에 임시 전시대에 올라간 유물 |
| `03-first-tip.png` | 첫 세션의 제보 배너 — 발굴단 없이도 참가 중임을 알리는 문구(결함 2 수정 후) |
| `01-tip-banner.png` | 제보 L1 배너 — 동시 1장, 카운트다운, 반응 경로 |
| `01-unique-acquired.png` | **유일을 손에 넣는 순간**의 전체 연출 — "세계에 단 하나", 이름은 아직 감춰져 있다(결함 4 수정) |
| `02-unique-vault.png` | 그 유일이 실제로 내 것이 된 소장고 상태 |
| `01-background-return.png` | 배경 탭에서 돌아온 직후 — 비운 시간이 정산되고 요약이 떠 있다(결함 1 수정) |
| `01-return-8h.png` · `02-return-24h.png` | 복귀 요약 토스트(조치 0건) — 8시간·24시간(12시간 상한 적용) |
| `03-return-action-modal.png` | 복귀 요약 **모달** 경로 — 상단 고정 4줄 + 조치 카드(ux-v02 §8 2층 구조) |
| `01-notify-badges.png` | L2 시한부 뱃지 — 소장고 `5h`(임박, 붉음) · 시장 `72h`(결함 4 수정) |
| `02-notify-ledger.png` | L3 조용한 로그 — 도감 탭 원장 서브탭 |
| `01-floor-capped.png` | 드랍 바닥값 구간 — 헤더의 "최소 간격 8초 도달"(결함 5 수정) |
| `01-rules-modal.png` | 규칙 공개 화면 — 화면의 숫자 9종이 실제 상수와 일치 |
| `01-steps-end.png` | 중반 이후(12거점 보유) 상태에서 조작 단계 수 10종 측정을 마친 화면 |

## 22.12 알려진 한계 (v0.3.2, 보고 대상)

- **감정 파이프라인 정지가 완전히 없어지지는 않았다.** 중앙값 4.1%·최장 59초가
  남는다. 자동 루틴이 60초 주기라서 생기는 하한이다 — 더 줄이려면 루틴을
  `advance()` 안으로 들이거나 주기를 바꿔야 하는데, 둘 다 스텝 무관성 검증
  (`qa:expedition`·`qa:economy`)이 서 있는 경계를 건드린다. 이 패스에서는 손대지 않았다.
- **`notes/ux-v02.md` §2의 18종 중 10종만 쟀다.** 경매 등록·거점 이전·루틴 변경 등
  8종은 선행 상태(경매장 건립·base 2곳 이상)를 만드는 비용이 커서 미측정이다.
- **모바일 실기기는 여전히 미확인**(§5 마지막 항목). 이번 패스도 헤드리스
  뷰포트(375×812)까지만 봤다.
- **재미 가설 4개의 "사람 몫"은 그대로 열려 있다**(§22.10). 자동화로 닫을 수 있는
  선까지 닫았을 뿐이고, 플레이테스트 0회라는 사실 자체는 변하지 않았다.
- **시작 발굴단 1팀**(`brief.md` §첫 세션 1의 원래 설계)은 넣지 않았다. 넣는다면
  엔딩 기준선 재측정이 함께 와야 한다 — G77.5에 비용과 함께 v0.4 후보로 기록했다.
- **`qa_endgame.ts`는 여전히 옛 타게팅을 쓴다**(§15.5에서 이월). 이번 패스에서도
  손대지 않았다.

---

# 23. v0.3.3 — 플레이 계측 (어떤 이벤트가 어느 정도 시간과 노력으로 일어나는가)

작업 요청: *"플레이 과정에서 어떤 이벤트가 어느 정도 시간과 노력으로 이루어지는지를
실제 플레이를 통해서 데이터를 쌓고 분석 결과를 보고해줘."*
분석 원문은 `notes/play-telemetry.md`, 판단 근거는 `notes/decisions.md` G78.

**§22와 다른 점**: §22는 "의도대로 도는가"(합격/불합격)를 봤다. 이 절은
**"무슨 일이 언제 몇 번 일어나고, 그중 몇 번이 사람 손을 요구하는가"**를 센다.

## 23.0 수단

```
pnpm --filter relic-king playlog                 168시간 × 두 정책, 이벤트 타임라인
pnpm --filter relic-king playlog -- --hours 24
pnpm --filter relic-king play -- effort          UI 조작 단계 수·소요 시간 실측
```

- `app/src/sim/telemetry.ts` — `World` 스냅샷 diff로 이벤트를 뽑는다. **엔진에 훅을
  심지 않는다** — 계측 때문에 게임이 달라지면 계측이 무의미하고, 스텝 무관성
  검증이 서 있는 경계를 흔들게 된다.
- `app/src/sim/playlog.ts` — 수집·집계. `player`/`auto` 두 phase로 나눠 기록한다.
- `app/src/sim/policy.ts` — **신설(리팩터)**. 방치 정책이 `run.ts` 안에만 있어
  계측이 가져다 쓸 수 없었다. 정책만 떼어 내 `run.ts`와 `playlog.ts`가 같은
  코드를 타게 했다 — 동작은 한 줄도 바뀌지 않았다(`sim --hours 2` 출력 동일 확인).

### 무엇을 "사람 손"으로 세는가

게임의 기본 자동화(`runAutoRoutine` — 인부·장비·감정소 재투자, 미감정 잉여 정리)는
**`auto`로 센다.** UI가 이미 해 주는 일이라 사람 몫이 아니다. 첫 계측에서 이걸
`player`로 잘못 계상해 2시간에 44회가 나왔고, `act()`를 세 조각으로 쪼개
(`liquidateSurplus` / `runAutoRoutine` / `actExpansion`) 바로잡았다.

## 23.1 탭만 열어 두면 2일차부터 아무 일도 일어나지 않는다

의미 있는 이벤트(도감 신규 종·층 돌파·제보·레이스·상실·확장·전시 등) 일차별:

| 일차 | 탭만 열어 둠 | 운영 |
| --- | ---: | ---: |
| 1일차 | **279** | 635 |
| 2일차 | **1** | 301 |
| 3일차 | **1** | 421 |
| 4일차 | **0** | 434 |
| 5일차 | **0** | 2,124 |
| 6일차 | 36 | 596 |

| 지표 (168시간) | 탭만 열어 둠 | 운영 |
| --- | ---: | ---: |
| 반복 이벤트(드랍·감정·매각) | 226,347 | 249,257 |
| 의미 있는 이벤트 | **317** | 3,984 |
| 반복 : 의미 | **714 : 1** | 55 : 1 |
| 최장 무이벤트 구간 | **66시간 53분** | 5시간 21분 |
| 15분 이상 공백이 차지하는 비율 | **96.0%** | 24.4% |
| 도달 도감 | **125 / 1,902종** | 1,427 / 1,902종 |
| 엔딩 | 미도달 | 140시간 42분 |

**6~24시간 구간(18시간)에 일어난 의미 있는 이벤트 15건은 전부 `영구 상실`이다** —
탭만 열어 둔 플레이어에게 그 시간 동안 일어나는 유일한 사건은 라이벌이 내 유물을
가져가는 것이다. 척추 4번("안 눌러도 손실 0")과 정면으로 부딪히는 실측이다.

## 23.2 "클릭 0회 엔딩 140시간"은 조작 0회가 아니다

엔딩에 도달한 것은 **운영** 플레이이고, 그 플레이가 사람에게 요구한 양:

> **334회 · 879단계 — 하루 57회 · 150단계**
> (`notes/decisions.md` G3의 관여 예산 "하루 2~3회 × 3분"의 **19~28배**)

| 조작 | 횟수 | 단계/회 | 총 단계 | 비중 |
| --- | ---: | ---: | ---: | ---: |
| **경매 출품** | **189** | 3 | **567** | **64.5%** |
| 발굴단 증강 | 82 | 2 | 164 | 18.7% |
| 보관소·습도·복원·보안 | 15 | 3 | 45 | 5.1% |
| 전시 | 10 | 3 | 30 | 3.4% |
| 박물관 확장 | 14 | 2 | 28 | 3.2% |
| 원정 파견 | 9 | 2 | 18 | 2.0% |
| 나머지 9종 | 15 | 1~3 | 27 | 3.1% |

노력과 보상이 어긋나 있다 — 조작이 가장 많은 날이 소득이 가장 적다:

| 일차 | 사람 조작 | 신규 종 |
| --- | ---: | ---: |
| 1 | 58 | 440 |
| 2 | 44 | 242 |
| 3 | 37 | 62 |
| **4** | **115** | **5** |
| 5 | 42 | **1,595** |
| 6 | 38 | 233 |

## 23.3 조작 단계 수·소요 시간 — UI 실측 (`play effort`, 21건 전부 통과)

시작 화면은 항상 발굴 탭, 탭·서브탭 전환도 1단계. 소요 시간은 첫 클릭부터
**결과가 상태에 반영될 때까지**다.

| 조작 | 단계 | 소요 |
| --- | ---: | ---: |
| 인부 고용 · 단장 고용 · 발굴단 슬롯 해금 | 1 | 0.4초 |
| 발굴단 파견(새 거점) · 인원 증강 · 루틴 켜기 | 2 | 0.1~0.7초 |
| 거점 해금(새 base) · 중복 자동 정리 켜기 | 2 | 0.4초 |
| 박물관 건립 / 등급 승급 / 마케팅 · 관장 고용 | 2 | 0.7초 |
| 전시 · 경매장 건립 / 승급 / 경매관장 고용 | 3 | 1.0초 |
| **경매 출품(1점)** | **3** | 0.7초 |
| 보관소 정원 · 습도 · 복원 · 보안 | 3 | 1.0초 |

**전부 3단계 이하** — `notes/ux-v02.md` §2 예산은 지켜진다. 문제는 단계 수가
아니라 **반복 횟수**다(§23.2).

## 23.4 계측이 찾아낸 결함 2건 (수정 완료)

### (치명) 경매 등록 버튼이 앱 전체를 언마운트시켰다

`listAtAuction`이 `w.vault.splice()`로 **배열을 제자리에서** 고치는데 `VaultView`가
`useMemo(…, [world.vault])`로 묶음 목록을 캐시하고 있었다 — 참조가 그대로라 메모가
갱신되지 않고, 사라진 uid가 `DisplayAction`의 `world.vault.find(...)!`에서
`undefined`로 단정돼 TypeError를 냈다. React 트리 전체가 내려가 탭 바까지 사라진다
(새로고침 전까지 게임 없음).

- **하필 그 버튼이 전체 플레이어 조작의 64%를 차지한다**(§23.2).
- `smoke`도 §22의 `play` 시나리오도 이 경로를 밟지 않아 드러나지 않았다.
- 수정: 묶음 목록의 메모이즈 제거(엔진이 제자리에서 고치는 객체를 의존성에 두는
  것 자체가 틀렸다) + `DisplayAction`이 사라진 유물이면 렌더하지 않음.
  같은 이유로 `WorldExplorer`의 `useMemo(…, [world])`(추천 거점이 마운트 시점에
  굳어 있었다)도 걷어냈다.
- 회귀 게이트: `play effort` 시나리오가 매 조작 뒤 "앱이 살아 있는가"(탭 바 존재)를
  확인한다.

### 보관소·습도·복원·보안에 진입 경로가 없었다

넷 다 엔진에 있고(`spec.md` §9.3·§9.4) `useGame`도 내보내고 **시뮬 정책은 사 왔다**
— 즉 지금까지의 168시간 기준선은 "보관소를 키운 플레이"였는데 실제 플레이어는 그걸
할 수 없었다. 소장고 탭은 "정원을 넘기면 보존 저하 2배"라고 경고하면서 정원을 올릴
방법을 주지 않고 있었다. 계측이 "단계 수를 셀 수 없는 조작 15회"로 처음 드러냈다.

수정: 시설 탭에 **보관소 서브탭** 신설(정원·습도조절·복원기술·보안, 각 3단계).
지금 값 → 다음 값과 효과를 한 줄에 보여 준다.

## 23.5 제보 응답 창 — 초반엔 설계의 1/4, 후반엔 65%가 만료

| | 탭만 열어 둠 | 운영 |
| --- | ---: | ---: |
| 제보 횟수 | 58 | 1,297 |
| 창 중앙값 | **15초** | 1분 30초 |
| 첫 1시간 중앙값 | **16초** | **15초** |
| 승 / 패 / 만료 | 43 / 15 / 0 | 440 / 19 / **838** |

설계는 60~150초(`TIP_DURATION_ONSITE_MIN~MAX`)다. 초반이 짧은 이유는 레이스가
먼저 끝나면서 배너도 같이 닫히기 때문이다(드랍 8초 × 적중 28% → 2~3드랍이면 결판).
`brief.md` §첫 세션 10의 "배너가 뜨면 1탭으로 개입한다"는 화면을 들여다보고 있지
않으면 성립하지 않는다. 반대로 6시간 이후 운영 플레이에서는 제보가 시간당 13회까지
뜨고 **65%가 그냥 만료**된다 — L1의 "유일하게 방해하는 UI" 특권이 스스로 무너진다.

## 23.6 시간대별 의미 있는 이벤트 밀도(시간당)

| 구간 | 탭만 열어 둠 | 운영 |
| --- | ---: | ---: |
| 0~20분 | 117.0 | 129.0 |
| 20분~1시간 | 130.5 | 127.5 |
| 1~6시간 | 27.6 | 94.2 |
| **6~24시간** | **0.8** | **2.0** |
| 24~72시간 | 0.0 | 15.0 |
| 72시간~ | 0.4 | 45.9 |

운영 플레이조차 6~24시간에서 시간당 2건까지 떨어졌다가 회복한다. 그 구간에 큰 벽이
몰려 있다 — 발굴단 슬롯 해금(7시간 48분, 5,000만₩) · 경매장 건립(9시간 33분) ·
박물관 건립(13시간 57분). 첫날 시간당 130건을 겪은 플레이어가 둘째 날 2건을 만난다.

## 23.7 게이트 (회귀 없음)

| 게이트 | 결과 |
| --- | --- |
| `build`(tsc + vite) | ✅ |
| `qa:migration` · `qa:artifacts` · `qa:expedition` · `qa:season` | ✅ |
| `qa:economy` · `qa:autosell` · `qa:pipeline` | ✅ |
| `smoke --seconds 45` | ✅ |
| `play` (시나리오 11종) | ✅ 판정 95건 전부 통과 |
| `sim --hours 2` — 정책 리팩터 전후 동일 | ✅ 자산 30.5억 · 도감 210종 동일 |
| `playlog --hours 168` 엔딩 | ✅ 140시간 42분 (§22.11과 동일) |

## 23.8 알려진 한계 (v0.3.3, 보고 대상)

- **두 정책은 "사람"이 아니다.** `idle`은 UI 프레임 루프와 같은 일만 하고,
  `active`는 시뮬 정책이다. 실제 사람은 그 사이 어딘가에서, 훨씬 불규칙하게 논다.
  이 계측이 답하는 건 "이 게임이 그 두 극단에서 어떤 모양인가"이지 "사람이 어떻게
  노는가"가 아니다 — 후자는 여전히 플레이테스트가 필요하다.
- **시드 1개다.** §22.3에서 배운 대로 두꺼운 꼬리(자산·T4) 지표는 단일 시드로
  판정하면 안 된다. 여기 수치 중 자산·유일 관련은 그 성격이므로 참고값으로 읽는다.
  이벤트 **건수·간격·조작 수**는 정책이 결정론적이라 시드 민감도가 낮다.
- **조작 단계 수는 "가장 짧은 경로"다.** 실제 사람은 화면을 찾고 값을 비교하느라
  더 누른다. 879단계는 **하한**이다.
- **`STEP_LATE = 15초` 틱 격자** 때문에 이벤트 시각이 15초 단위로 양자화된다.
  15초 미만의 간격 분포는 이 계측으로 말할 수 없다.
- §7의 처방(루틴 기본값·경매 자동화·제보 창·6~24시간 벽)은 **적용하지 않았다.**
  전부 밸런스·설계 판단이라 기준선 재측정이 함께 와야 한다.

---

# 24. v0.3.4 — 계측 처방 ①② 적용과 그 효과 실측

요청: `notes/play-telemetry.md` §7.1(탭만 열어 둔 플레이의 죽음)과 §7.2(경매 출품
자동화)를 적용. 판단 근거는 `notes/decisions.md` G79·G80.

측정은 §23과 **같은 도구·같은 조건**이다(`playlog --hours 168`, 시드 기본값,
두 정책). 그래야 전후 비교가 성립한다.

## 24.1 ① 탭만 열어 둔 플레이 — 전후

| 지표 (168시간, 클릭 0회·조작 0회) | 전 | 후 |
| --- | ---: | ---: |
| 의미 있는 이벤트 | 317 | **2,090** |
| 반복 : 의미 | 714 : 1 | **112 : 1** |
| 최장 무이벤트 구간 | 66시간 53분 | **26시간 44분** |
| 5분 이상 공백 비율 | 97.3% | **66.2%** |
| 1시간 이상 공백 비율 | 93.9% | **33.7%** |
| 도감 | 125 / 1,902종 | **298 / 1,902종** |
| 자산 | 60.6억₩ | **472억₩** |
| 제보 발생 | 58회 | **1,382회** |

일차별 의미 있는 이벤트 / 신규 종:

| 일차 | 전 | 후 |
| --- | --- | --- |
| 1일차 | 279건 / 125종 | 278건 / 126종 |
| 2일차 | **1건 / 0종** | **392건 / 162종** |
| 3일차 | 1건 / 0종 | 398건 / 9종 |
| 4일차 | 0건 / 0종 | 356건 / 1종 |
| 5일차 | 0건 / 0종 | 334건 / 0종 |
| 6일차 | 36건 / 0종 | 332건 / 0종 |

**"게임이 죽는다"는 고쳐졌다** — 2일차 1건이 392건이 됐고, 이후에도 매일 330건
이상이 계속 일어난다(원정 파견·귀환, 제보, 레이스, 층 돌파).

**"도감이 끝까지 간다"는 아니다.** 신규 종은 여전히 3~4일차에 멈춘다(298종).
1팀으로 12거점을 다 캘 수는 없다 — 그건 발굴단 슬롯 해금(5,000만₩)을 넘어야
하는 문제고, `notes/play-telemetry.md` §7.4의 6~24시간 벽과 같은 뿌리다.
이 패스에서 건드리지 않았다.

## 24.2 ② 경매 출품 자동화 — 전후

| 지표 (168시간, 운영 플레이) | 전 | 후 |
| --- | ---: | ---: |
| 총 조작 | 334회 | **143회** (−57%) |
| 총 조작 단계 | 879단계 | **309단계** (−65%) |
| 하루 평균 | 57.0회 · 149.9단계 | **24.4회 · 52.7단계** |
| 그중 경매 출품 | 189회 · 567단계 | **0회**(자동) |
| 엔딩 도달 | 140시간 42분 | **140시간 39분** |
| 도감 | 1,427종 | 1,427종 |
| 자산 | 1,357억₩ | 1,608억₩ |

`G3`의 관여 예산(하루 2~3회 × 3분) 대비 **19~28배 → 8~12배**로 내려왔다.
아직 예산 안은 아니다.

**노력과 보상의 어긋남도 완화됐다.** 전에는 4일차에 조작 115회가 몰리고 5일차에
보상이 1,595종으로 터졌다. 지금은:

| 일차 | 조작(전 → 후) | 신규 종(전 → 후) |
| --- | --- | --- |
| 1일차 | 58 → 40 | 440 → 436 |
| 2일차 | 44 → **11** | 242 → 398 |
| 3일차 | 37 → **1** | 62 → 73 |
| 4일차 | **115 → 84** | 5 → **76** |
| 5일차 | 42 → **4** | **1,595 → 233** |
| 6일차 | 38 → **3** | 233 → 213 |

## 24.3 남은 최대 조작 — 발굴단 증강 87회 (적용하지 않음)

수정 후 143회 중 **87회(174단계, 전체의 56%)가 발굴단 인원·장비 증강**이고,
그중 **81회가 4일차 하루에 몰린다**. `autoInvestLegacyDig`가 레거시 인부·장비는
자동으로 사면서 발굴단 쪽은 안 사는 비대칭이다.

자동화하지 않은 이유는 `notes/decisions.md` G80.1에 적었다 — 팀 발굴력을 올리면
원정비도 같이 커져, 슬롯을 더 해금해 커버리지를 넓히는 더 나은 투자로 갈 자금이
한 팀에 흡수된다(G56에서 실측된 결합). 밸런스 판단이 함께 와야 한다.

## 24.4 게이트 (회귀 없음)

| 게이트 | 결과 |
| --- | --- |
| `build`(tsc + vite) | ✅ |
| `qa:migration` (v1→**v9** 체인, v8→v9 신규 6건 포함) | ✅ |
| `qa:artifacts` · `qa:economy` · `qa:season` · `qa:autosell` · `qa:pipeline` | ✅ |
| `qa:expedition` | ✅ (전제 복원 — 아래) |
| `sim --hours 168` 엔딩 | ✅ **140시간 39분** (전 140시간 42분) |
| `smoke --seconds 45` | ✅ (칸 안정성 검사 교체 — 아래) |
| `play` (시나리오 11종, 판정 96건) | ✅ 96/96 |

### 깨진 테스트 전제를 고친 방식 (기준을 낮추지 않았다)

- `qa_migration` — "teams·staff가 빈 배열로 채워진다"는 더 이상 참이 아니다
  (v8→v9가 시작 발굴단을 준다). 검사를 **그 처방이 실제로 적용됐는가**로 바꾸고,
  "플레이어가 고정한 루틴 대상은 덮어쓰지 않는다" · "이미 팀이 있으면 또 주지
  않는다" · "루틴이 없던 팀은 자동 순회로 켜진다"를 새로 넣었다.
- `qa_expedition` — 이 테스트는 **왕복 한 회차**의 스텝 무관성을 본다. 새 월드에
  팀이 이미 있어 `createTeam`이 실패했고, 루틴 기본 켬 때문에 귀환 즉시 재파견돼
  "둘 다 idle" 단언이 깨졌다. 통제된 전제를 복원했다(팀을 비우고 다시 꾸린 뒤
  루틴을 끈다). 단언은 그대로다.
- `deserialize`에 `ensureShape()` 신설 — `log` 같은 필수 컨테이너가 빠진
  페이로드도 열린다. 마이그레이션 체인은 "그 버전이 새로 요구하는 필드"만
  채우므로 원래 스키마에 있던 것이 빠진 경우를 막을 곳이 없었다.
- `play` 5곳 — 파견·고용·루틴 켜기 측정이 "시작 발굴단이 없다"를 깔고 있었다.
  사람이 그 버튼을 실제로 마주하는 조건(유휴 팀 / 해금된 빈 슬롯 / 루틴이 꺼진
  1팀)을 만들어 다시 쟀다. 루틴 판정은 `some()` → `teams[0]`으로 좁혔다 —
  새 팀은 루틴이 켜진 채 태어나므로 `some()`이면 늘 통과한다.
  덤: 제보 배너 판정이 세이브를 읽느라 15초짜리 배너를 놓치고 있었고(DOM
  폴링으로 교체), 미감정 즉시 매각 측정은 대기열이 마침 차 있느냐는 운에 걸려
  있었다(한 점 들어오는 순간을 기다렸다 누른다).
- `smoke` 소장고 칸 안정성 — "첫 칸·끝 칸 좌표가 4.5초간 그대로인가"로 쟀는데,
  발굴단이 새 종을 계속 물어 오면서 "끝 칸"이 아예 다른 종이 됐다. 재배열(나쁨)과
  칸이 느는 것(정상)을 구별 못 하는 검사였다. 좌표를 **종 id(`data-aid`)로
  짝지어** 대조하게 바꿨다 — 두 스냅샷에 모두 있는 종이 움직이면 실패. 두 칸만
  보던 이전보다 넓게 본다.

## 24.5 알려진 한계 (v0.3.4)

- **도감은 여전히 3~4일차에 멈춘다**(탭만 열어 둔 플레이, 298/1,902종). §24.1.
- **조작은 아직 관여 예산 밖**이다(하루 24.4회 vs 설계 2~3회). 남은 56%가
  발굴단 증강이고, 그건 밸런스 판단이 걸려 있다(§24.3).
- **6~24시간 벽은 그대로**다 — 의미 있는 이벤트가 시간당 0.8(탭만) / 2.0(운영)건.
  두 처방 모두 이 구간을 건드리지 않았다.
- **제보 창 문제도 그대로**다(초반 중앙 15초, 후반 만료 65%→77%). 오히려 제보
  발생이 58 → 1,382회로 늘어 후반 만료 비율이 커졌다 — `notes/play-telemetry.md`
  §7.3의 처방이 더 시급해졌다.
- 측정은 여전히 **시드 1개**이고, 두 정책은 사람이 아니라 양 극단이다(§23.8).

---

# 25. 첫 10시간 — 10분 단위 경험 계측 (v0.3.4)

요청: "플레이 10시간까지 10분 단위로 어떤 경험을 하고 각 경험이 어떻게
연결되는지." 전체 보고와 다이어그램은 `notes/play-first-10h.md`, 여기엔 수치만
남긴다.

측정: `playlog --hours 10 --bucket 10`(§23·§24과 같은 도구·같은 시드). `--bucket`은
이 측정을 위해 추가한 옵션으로, 구간별 이벤트와 **구간 끝의 세계 상태**(도감·
발굴력·자금·자산·소장고)를 같이 찍는다.

## 25.1 국면별 (운영 플레이, 10분 격자 합산)

| 국면 | 구간 | 신규종 | 10분당 | 제보 | 사람 조작 |
| --- | --- | ---: | ---: | ---: | ---: |
| ① 첫 삽 | 0~10분 | 24 | 24.0 | 0 | 0 |
| ② 첫 경쟁 | 10분~1시간 10분 | 86 | 14.3 | 19 | 2 |
| ③ 거점 개봉 | 1시간 10분~3시간 30분 | 310 | 22.1 | 43 | 13 |
| ④ 조용해짐 | 3시간 30분~4시간 20분 | 12 | 2.4 | 0 | 1 |
| ⑤ 배경 소음 | 4시간 20분~10시간 | 4 | 0.1 | 0 | 6 |

10시간 합계: 반복 12,815건 · 의미 611건(21.0:1) · 조작 22회·59단계.
신규 종 436개 중 **420개가 첫 3시간 30분**에 들어왔고, 제보 62회는 전부 그 안이다.
⑤의 의미 있는 이벤트 24건 중 **15건이 영구 상실**(남이 가져갔다는 통지)이다.
반복 이벤트는 구간당 164~229건으로 처음부터 끝까지 일정하다.

## 25.2 두 플레이 (10시간)

| | 탭만 열어 둠 | 운영 |
| --- | ---: | ---: |
| 도감 | 126종 | 434종 |
| 새 종이 사실상 멈춘 시각 | 2시간 40분 | 3시간 30분 |
| 의미 있는 이벤트(건수 합) | 277 | 611 |
| 반복 : 의미 | 47.0 : 1 | 21.0 : 1 |
| 제보 | 57회(첫 14분 30초) | 62회(첫 12분) |
| 영구 상실 | 24건(첫 3시간 44분) | 15건(첫 6시간 10분) |
| 사람 조작 | 0회 | 22회·59단계 |
| 소장 자산 | 199억₩ | 398억₩ |

두 플레이를 가르는 것은 사실상 **거점 해금 2회**다. 나머지 조작 20회(시설·
경매장·슬롯)는 도감을 1종도 늘리지 않았다.

## 25.3 정체의 세 원인 (진단)

| 원인 | 실측 | 결과 |
| --- | --- | --- |
| `recommendSites`가 거리를 보지 않음 | t=0 추천 1위 peru — 도착 35.3h·귀환 176.6h (japan은 1.3h·6.3h, 종 수 92%) | 첫 10시간에 발굴단 축이 **없다** |
| `playerCanReactAt`이 활성 거점 하나로 축소 | 제보 풀 반응 가능 13 → 15 → 10 → 2(3.5h) → **0**(4h) | 제보가 3시간 30분에 끊긴다 |
| `MAX_OWNED_SITES = 3` | 세 번째 base 2시간 18분, 이후 해금 불가(자금은 충분) | 도감 천장 434종(22.8%) |

앞 둘은 뿌리가 같다 — 첫 원정 대상 선택 하나다. 처방 후보는
`notes/play-first-10h.md` §8에 우선순위와 부작용 추정까지 적어 뒀다(전부 미적용).

## 25.4 부수 관찰

- 소장고 정원 초과가 **46분부터 10시간까지 상시**다(정원 300점 vs 보유 633점,
  보존 저하 2배). 단 정책이 보관소를 Lv.6에서 멈추는 탓도 있다 — 엔진에 레벨
  상한은 없다.
- 경매장이 **9시간 25분**에야 선다. v0.3.4의 경매 출품 자동화(§24.2)는 첫
  10시간에는 거의 작동하지 않는다(출품 4건).
- 첫 사람 조작이 **59분**에 온다. 그 전까지 플레이어가 내릴 결정이 없다.

---

# 26. v0.5.1 — 두 줄기 합류 후 게이트 재측정

v0.3.1에서 갈라진 계측 라인(§22~§25)을 v0.5 위로 합친 뒤(`notes/decisions.md` G81),
**양쪽 게이트를 전부 다시 돌렸다.** 합친 트리에서 실제로 도는지를 보는 것이
이 절의 전부다.

| 게이트 | 결과 | 비고 |
| --- | --- | --- |
| `build`(tsc + vite) | ✅ | |
| `qa:migration` | ✅ | 체인 v1→**v10** |
| `qa:rivalcard` | ✅ | 기록패·고스트(v0.5) |
| `qa:sprites` | ✅ | 완전중복 0쌍 · 거점정확도 91.4% |
| `qa:season`·`qa:expedition`·`qa:artifacts`·`qa:economy`·`qa:autosell`·`qa:pipeline` | ✅ | |
| `sim --hours 168` | ✅ | 엔딩 **140시간 36분** · 도감 1,427/1,902종(75%) |
| `smoke` | ✅ PASS | 세계지도 라벨 12 · 권역 줌 47.6% · 기록패 195자 · 소장고 칸 이동 없음 |
| `play` | ✅ **97/97** | 첫 세션 5종 전부 20분 예산 안 |

**밸런스는 움직이지 않았다.** 엔딩 140시간 39분(합류 전, §24) → **140시간 36분**.
3분 차이는 고스트 없는 기본 조건에서 두 줄기의 코드가 같은 경로를 탄다는 뜻이다.

## 26.1 깨진 테스트 전제 2건 — 기준을 낮추지 않고 고쳤다

`notes/decisions.md` G80.2와 같은 원칙이다. 둘 다 **단언이 틀린 게 아니라 전제가
낡았다.**

- **세이브 체인 끝 버전**(`qa_migration` 10건, `qa_rivalcard` 2건) — "체인 끝은
  v9"라고 단정하고 있었는데, 합류로 v10 단계가 하나 더 붙었다. `deserialize()`는
  체인을 끝까지 돌리므로 v8 세이브도 이제 v10으로 나온다. 단언을 v10으로 옮겼다.
- **거점 해금 측정**(`play effort`) — 측정 준비가 12거점을 **전부** base로 열어
  두고 있었다. v0.3까지는 그 상태에서도 "새 본거지로 열기"가 화면에 남아 있었지만,
  v0.4의 탐색 패널은 이미 본거지인 곳에 그 버튼을 주지 않는다 — 즉 측정 대상
  조작이 화면에서 사라져 "진입 경로 없음"으로 읽혔다. **base를 둘만 열어** 한
  자리를 비우고(상한 3), 목록에서 "본거지"가 아닌 거점을 골라 다시 잰다.
  실측 **2단계 · 0.4초** — 합류 전과 같다.

## 26.2 합류가 실제로 바꾼 화면 3곳

머지 도중 양쪽이 같은 자리를 고친 UI다. 어느 쪽도 버리지 않았다.

| 화면 | 계측 라인(v0.3.x) | v0.4·v0.5 | 합친 결과 |
| --- | --- | --- | --- |
| 헤더 드랍 안내 | "최소 간격 8초 도달" 꼬리표 | 거점 이름을 도시명으로 | 도시명 + 꼬리표 |
| 발굴단 루틴 줄 | "아직 못 채운 거점"(자동 순회) | 도시명 + 조사 | 자동 순회면 "아직 못 채운 거점으로", 고정이면 "룩소르로" |
| 시설 탭 | 보관소 서브탭 신설 | 실사 디테일 블록 | 둘 다 |

## 27. v0.5.2 — 달러 표기 · 소장고 여러 개 선택

판단 근거는 `notes/decisions.md` G82·G83.

| 게이트 | 결과 | 비고 |
| --- | --- | --- |
| `build` | ✅ PASS | 타입체크 + 빌드 |
| `sim --hours 168` | ✅ 엔딩 **140시간 36분** | v0.5.1과 같다. 밸런스 상수는 바꾸지 않았고, `sellSpares`·`auctionSpares`를 새 경로로 합친 것도 결과를 움직이지 않았다 |
| `qa:bulk`(신설) | ✅ 18/18 | 대상 선정 · 도감 보존 · `speciesEmptiedBy` 예고와 실제 일치 · 경매 자리 초과분 잔류 |
| `qa:autosell`·`qa:economy`·`qa:migration`·`qa:expedition`·`qa:season`·`qa:artifacts`·`qa:pipeline`·`qa:rivalcard`·`qa:sprites` | ✅ 전부 통과 | |
| `play` | ✅ **109/109** | 기존 97건 + `bulk` 12건 |

**`play bulk` 실측.** 두 종에 사본을 3점씩 더 심은 뒤 `여러 개 선택 → 칸 2개 → 매각`,
총 4탭으로 6점($3.5만)을 팔았다. 두 종 모두 1점씩 남았고 도감은 그대로였다. 보존을 끄고
경매에 올리자 "도감에서 2종이 빠진다"는 확인이 뜨고, 확인 뒤 2점이 경매에 올랐다.
스크린샷은 `assets/screenshots/play/01-bulk-selected.png`·`02-bulk-confirm.png`.
