# Evaluation — 땅따먹기

## 자동 검사

| # | 검사 | 방법 | 통과 기준 |
| --- | --- | --- | --- |
| A1 | 타입 검사 + 규칙 검사 | `pnpm --filter land-grab test` | 타입 오류 0, 규칙 검사 18건 전부 통과 |
| A2 | 빌드 | `pnpm build:project -- land-grab` | `app/dist/index.html` 생성, 런 경로 복사 성공 |
| A3 | 레지스트리 | `pnpm sync:registry` 후 `pnpm validate:projects` | `land-grab` 항목 유효 |
| A4 | 플레이테스트 | `pnpm playtest --project land-grab` | 시나리오 전 구간 스크린샷 생성, 콘솔 에러 0 |

## 규칙 검사 — 시뮬레이션 (`tests/rules/check.ts`)

브라우저 없이 엔진만 돌려 확정적으로 확인한다. 플레이어가 스폰 직후부터 자동으로
움직이기 때문에 브라우저 시나리오로는 같은 상황을 매번 재현할 수 없다.

| 묶음 | 확인 내용 |
| --- | --- |
| 사망 | 벽 충돌 사망, 사망 시 영토 중립화, 자기 꼬리 사망 |
| 킬 | 남의 꼬리를 밟으면 그 상대가 죽고 밟은 쪽은 살아남으며 킬이 기록된다 |
| 점령 | 고리를 닫으면 안쪽이 내 땅이 되고 꼬리가 비워진다 |
| 점령 예외 | 상대가 안에 있으면 그 영역은 넘어오지 않는다 (splix 구멍 막기 규칙) |
| 조작 | 180° 전환 무시, 한 칸에서 두 번째 전환 무시 |
| 스폰 | 가장 가까운 벽의 반대쪽을 향한다, 각자 `5 × 5` = 25칸으로 시작 |

## 규칙 검사 — 브라우저 (플레이테스트 시나리오에서 확인)

| # | 검사 | 통과 기준 |
| --- | --- | --- |
| R1 | 시작 영토 | 게임 시작 직후 플레이어 점유율이 0보다 크다 |
| R2 | 점령 | 밖으로 나갔다 돌아오는 왕복 후 점유율이 직전보다 증가한다 |
| R3 | 자기 꼬리 충돌 | 꼬리로 고리를 만들어 스스로 밟으면 목숨이 1 줄어든다 |
| R4 | AI 생존성 | 20초 경과 후 모든 AI의 점유율이 0보다 크다 (벽·자기 꼬리에 갇혀 전멸하지 않는다) |
| R5 | 시간 종료 | 90초가 지나면 `result` 화면으로 전환된다 |
| R6 | 일시정지 | `Esc`를 누르면 `elapsedMs`가 더 이상 증가하지 않는다 |

## 수동 확인

- 1440×900 데스크톱과 390×844 모바일 뷰포트 양쪽에서 보드가 잘리지 않는다.
- 터치 스와이프로 4방향 전환이 모두 동작한다.
- 90초 한 판에서 프레임 드랍이 눈에 띄지 않는다(체감 60fps).
- 색맹 대비: 4색이 명도로도 구분된다(영토 경계선이 항상 그려진다).

## 마지막 실행 결과 (2026-09-14, v0.2.0)

- `pnpm --filter land-grab test` — 타입 오류 0, 규칙 검사 18건 전부 통과.
- `pnpm playtest --project land-grab` — A2~A4 통과. page error 0, console error 0, 스크린샷 8장.
  - R1 시작 점유율 0.74% (`5 × 5` = 25칸 / 3,364칸).
  - R2 왕복 1회로 0.74% → 1.46%.
  - R3 자기 꼬리를 밟아 목숨 3 → 2.
  - R4 21초 시점 플레이어 2.53%, AI 4.67% / 4.99% / 0.74%.
  - R5 결과 화면 전환 확인. R6 일시정지 중 `elapsedMs` 고정 확인.

## 관찰된 균형 문제

시나리오처럼 "6칸 나갔다 옆줄로 복귀"만 반복하면 플레이어는 20초 시점에 1~2.5%에
머무는 반면 AI는 5% 안팎을 가져간다. AI가 한 번에 `4 × 7`~`5 × 9` 직사각형을 그리는데
비해 얕은 왕복은 효율이 낮기 때문이다. 규칙 문제는 아니지만,
사람이 처음 잡았을 때 "AI가 너무 빠르다"고 느낄 여지가 있다.

수치를 건드리는 대신 규칙으로 푸는 안(「물막이 배율」)을 만들어 측정했고 **기각**했다.
전말과 데이터는 `docs/design/differentiation.md` §8.

요약: 배율은 깊이에 대한 보상 기울기를 만들지만 최적 깊이를 바꾸지 못하고,
AI 가 평소에 78~112칸을 막아 배율을 그대로 챙긴다(대칭 규칙). 측정 중 진짜 원인이 드러났다 —
플레이어가 죽으면 영토가 중립이 되고 그것을 옆의 AI 가 먹는다. 깊게 팔수록
AI 점수가 1,893 → 3,311로 함께 오른다. 문제는 점수 공식이 아니라 눈덩이의 이전 경로다.

**이 항목의 숫자(R4 점유율)로는 점수 규칙을 잴 수 없다.** 점수 실험은
`tests/bench/capture-bonus.ts` 로 잰다.

## 알려진 한계

- AI는 경로 탐색을 하지 않는다. 좁은 통로에서 비효율적으로 움직일 수 있다.
- 리스폰 위치는 빈 영역 무작위 선택이라, 혼잡할 때 상대 근처에 나올 수 있다.
- 플레이테스트 시나리오의 조작은 사람의 플레이와 다르다. R3(자기 꼬리 충돌)은
  다른 유닛에게 죽어도 `deaths`가 늘어 통과할 수 있다. 사망 원인까지는 구분하지 않는다.
