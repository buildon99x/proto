# Evaluation — Wave Runner

## 여전히 미수행인 중단 판정 (1단계)

> **제작자 외 3명에게 아무 설명 없이 건넸을 때, 2명 이상이 지시 없이 5회 이상 재시도하는가.**

**아직 수행되지 않았다.** 2단계는 이 판정을 통과한 뒤 착수하는 것이 원래 순서였고, 지시에 따라 먼저 진행했다. 기저 비행이 재미없으면 빌드도 게이트도 구해주지 못하므로 이 판정은 유효하며, 사람 손에 들어가는 즉시 물어야 한다. 시도 횟수는 스테이지 셀에 표시된다.

## 자동 검증

```bash
pnpm --filter wave-runner lint                                          # 타입
node projects/wave-runner/tests/smoke/lookahead.test.mjs                # 선행 가시 시간 상수
pnpm exec tsx projects/wave-runner/tests/verify/solver-check.ts         # 솔버가 신뢰 가능한가
pnpm exec tsx projects/wave-runner/tests/verify/generation.ts           # 생성기가 난이도를 겨냥하는가
pnpm exec tsx projects/wave-runner/tests/verify/sector-probe.ts         # 축이 양날인가
pnpm exec tsx projects/wave-runner/tests/verify/stage-paths.ts          # 전 경로 통과 가능성(정확)
pnpm exec tsx projects/wave-runner/tests/verify/endless-ramp.ts         # Endless 난이도 램프
pnpm exec tsx projects/wave-runner/tests/verify/curate-stages.ts        # 스테이지 시드 재선별(표를 다시 굽는다)
pnpm exec tsx projects/wave-runner/tests/verify/repetition-gain.ts      # 반복이 이미 완화하고 있는 양(ms)
pnpm exec tsx projects/wave-runner/tests/verify/relief-curve.ts         # 반복 완화 곡선·상한·코스 불변
pnpm exec tsx projects/wave-runner/tests/verify/relief-pilot.ts         # 완화의 오토파일럿 교차 검증 + 여유↔지연 환산
```

| 검사 | 현재 결과 |
|---|---|
| 타입·빌드·런처 전체 빌드 | 통과 |
| 선행 가시 1.2초 | 360×640에서 1.34초, 세로 가시 100% |
| **솔버 신뢰도** | 84개 조합에서 오토파일럿과 모순 0. 통과 불가 사례를 실제로 잡아냄 |
| **생성기 겨냥** | 4유형 × 난이도 4단계 전부 통과 가능. 목표 폭 30→9에 대해 실측 여유 354ms→97ms |
| **축이 양날인가** | 각도·속도·편향 3축 모두 섹터 유형에 따라 부호가 뒤집힘 |
| **전 경로 통과 가능성** | 12개 스테이지 × 16경로 — 94~100% 통과, 최선 경로 여유 122~198ms |
| **Endless 램프** | 지연 60ms 5339 → 180ms 1008. 모든 지연에서 런이 종료됨 |
| 게이트 교환 성립 | 실제 주행 352게이트 전부 한 축 +1 / 다른 축 −1 · 축합 이탈 0 (`gate-offers.ts`) |
| 브라우저 플레이테스트 | Stage 클리어(81초·게이트 4개·사망 0) · Endless 2508m · page/console 오류 0 |
| **반복 완화 곡선** | 12스테이지 × 16경로 × 8단계 전부 통과 가능 · 여유 단조 증가 · 코스 지문 전 단계 동일 |
| **완화 상한** | k=6 에서 티어4 최선 219ms ≤ 무완화 티어1 221ms, k=7 에서 228ms 로 넘어섬 · 티어 순서 보존 |
| **완화 교차 검증** | 지연 0·40·80ms 576회 주행 통과 220(38%) → 484(84%) · 줄어든 칸 0 · 솔버 모순 0 |
| **여유↔지연 환산** | **성립하지 않는다.** 단일 통로 유형 0.84, 산개 0.35 (단일 구간 기준 0.48) |
| **기존 반복 장치의 값** | 암기 +59ms · 연습 체크포인트 0ms · 축 상한 해금 최선 −2ms / 최악 +22ms |

### 여유는 사람의 지연 허용치가 아니다 (3단계 주장의 정정)

3단계는 솔버의 여유(ms)와 프로브의 반응 지연 허용치(ms)가 **같은 단위**라고 적었다.
4단계에서 처음으로 대조했고 **성립하지 않는다**(`relief-pilot.ts` ①).

| 유형 | 파일럿 지연 허용 / 솔버 여유 |
|---|---|
| 협곡·회랑·맥동 | 0.69 ~ 1.00 (평균 0.84) |
| **산개** | **0.34 ~ 0.77** (총 길이 기준 평균 0.35) |

원인 절반은 지표의 결함이었고 고쳤다 — `minWidth` 는 생존 집합의 **총 길이**라 집합이
여러 조각으로 쪼개지면 난이도를 과소평가한다. 아바타는 한 조각 안에 있고 건너뛸 수
없다. `minSpanWidth`(가장 넓은 단일 구간)를 솔버에 더했고, 산개의 비가 0.35 → 0.48 로
회복된다. 나머지 절반은 모델의 한계다 — 여유는 가장 좁은 한 순간만 보고 나머지 구간을
완벽히 달렸다고 가정하는데, 지연을 가진 추종자는 코스 내내 오차를 흘린다.

스테이지 규모에서는 더 벌어진다. 솔버는 **192경로 전부 통과 가능**이라 하고 지연 0의
오토파일럿은 **76/192(40%)** 만 통과한다.

**여유는 여전히 티어 목표로 쓸 수 있다.** 다만 그 값을 사람의 지연 허용치로 곧바로
읽으면 안 되고, 산개 유형에는 폭이 아닌 다른 지표가 필요하다. 후자는 미해결이다.

### 왜 통과율이 아니라 여유(slack)로 고르는가

3단계의 솔버로 다시 재보니 **통과 가능성은 거의 모든 경로에서 참**이었고(94~100%), 대신 **여유가 최선 198ms 와 최악 3ms 로** 갈렸다. 여유 3ms 인 경로는 이론상 통과 가능해도 사람에게는 불가능하다. 통과율은 공정성 바닥을 볼 뿐 난이도를 말하지 못한다.

그래서 큐레이션 기준을 셋으로 바꿨다 — **모든 경로가 통과 가능할 것**(보이지 않는 막다른 길 금지), **최선 경로의 여유가 티어 목표 구간에 들 것**(티어 1은 190ms, 티어 4는 100ms), **최선과 최악의 차이가 35ms 이상일 것**(차이가 없으면 게이트가 아무것도 묻지 않는다). 세 번째는 근사로는 잴 수 없어 3단계에서 비로소 가능해진 판정이다.

### (2단계 기록) 통과율 100%가 아니라 50~80%인 이유

전 경로 검증의 목적은 **공정성 바닥**이다 — 눈에 보이지 않는 막다른 길이 없어야 한다. 그런데 100%면 무슨 선택을 해도 통과되어 게이트가 무의미해지고, 20%면 퍼즐이 아니라 시행착오 강요다. 그래서 `curate-stages.ts`가 후보 시드를 훑어 **통과율 45~85% 구간의 시드만 남겨** `stage-seeds.json`에 굽는다. 게임은 그 표만 읽는다.

### 이제 각 도구가 무엇을 말하는가

- **솔버** — 통과 가능/불가능을 **정확히** 판정하고 생존 회랑 폭을 계측한다. 증인 기반의 한계("불가능을 증명하지 못한다")는 여기서 해소됐다.
- **오토파일럿** — 여전히 필요하다. 솔버가 옳은지 교차 검증하고(모순이 있으면 솔버가 틀린 것), 브라우저에서 실제 엔진을 구동한다.
- **섹터 프로브** — 반응 지연 허용치로 **사람 체감**을 근사한다. 솔버의 여유(ms)와 같은 단위라 서로 대조된다.

## 손으로 확인할 것

- [ ] 조작 설명 없이 첫 10초 안에 버튼의 의미를 파악하는가
- [ ] **게이트를 지난 직후, 무엇과 무엇을 바꿨는지 글자 없이 아는가**
- [ ] **서로 다른 두 빌드의 궤적을 스크린샷 한 장으로 구분할 수 있는가**
- [ ] **두 모드를 각각 해본 사람이 "왜 둘 다 있는지"를 한 문장으로 말할 수 있는가**
- [ ] **Stage 재시도 중 빌드를 바꿔본 비율이 30% 이상인가** (0%면 Stage의 로그라이크 성분이 죽은 것)
- [ ] Endless 사망 후 자기 빌드 선택을 원인으로 지목하는가
- [ ] 섹터 4유형을 이름 없이 실루엣만으로 구분하는가
- [ ] `running` 중 화면에 HUD가 하나도 없는가
- [ ] 세로 화면에서 한 손으로 플레이되는가
- [ ] 60fps가 유지되는가 (T 패널)
- [ ] 새로고침 후 해금과 기록이 남아 있는가
- [ ] **아무 설명 없이 한 런을 마친 사람이 자기가 무엇이었고 어디를 달렸는지 한 문장으로 말하는가** (침·혈관·마족 중 둘 이상이 나오면 통과 — `running` 밖의 글자만으로 컨셉이 전달됐다는 뜻이다)
- [ ] **Endless를 해본 사람이 "여긴 왜 끝이 없는가"를 스스로 설명하는가** (사체 또는 "심장이 없다"가 나오지 않으면 [narrative.md §3.4](./notes/narrative.md)는 문서 안에서만 성립한 것이다)
- [ ] **오버레이 문구를 읽은 사람과 건너뛴 사람의 같은 스테이지 클리어 시도 횟수가 눈에 띄게 다르지 않은가** (다르면 네러티브가 난이도를 올린 것이고, 곧 HUD 없음 제약을 깬 것이다)
- [ ] **한 스테이지에서 다섯 번 죽으면 아바타가 작아지고 벽이 물러난 것을 글자 없이 알아채는가**
- [ ] **완화가 걸린 채로 통과했을 때, 왜 코어가 없는지 납득하는가** (`ready` 오버레이와 클리어 화면이 그 이유를 말한다)
- [ ] **완화 통과 칸과 정식 클리어 칸이 스테이지 목록에서 구분되는가** (점선 · 주황 · "완화 통과")
- [ ] **완화가 걸린 뒤에도 게이트가 여전히 무언가를 묻는다고 느끼는가** (k≥4 에서 제안 풀이 좁아진다)
- [ ] `running` 중에 완화 단계를 알려주는 것이 화면에 없는가

## 남은 것

- **음악과 배속 동기화.** 리듬 격자는 들어갔지만 오디오는 없다. 속도 축이 배속이므로 음악을 붙이려면 오디오를 같이 늘여야 한다.
- **계정 동기화.** 영구 해금이 `localStorage` + 내보내기/가져오기에 머물러 있다.
- **런 초반 3섹터는 생성되지 않는다.** 공장에 줄 시간이 없는 자리라 수제 섹터로 **고정했다**(0.3.1). 재현성은 그래서 성립하지만 첫 30초의 다양성은 수제 12개 안에서만 나온다.
- **Endless 도입부 3섹터 고정.** 재현성을 위해 그 자리를 수제 섹터로 못박은 대가다. 난이도 1.0~1.26 구간에는 유형별 후보가 하나뿐이라 변주가 유형 순환에서만 온다.
- **1단계 중단 판정.** 여전히 미수행이다.
- **완화의 최악 경로 두 개.** 4:1 과 4:3 은 완화 상한에서도 최악 경로가 84ms 로 목표 100ms 밑이다. 근본 해법은 큐레이션 기준에 "최악 경로 하한"을 넣어 시드를 다시 굽는 것이고, 별도 작업이다.
- **`minSpanSlackSec` 을 판정에 쓰지 않는다.** 지금은 진단 지표로만 있고 생성기·큐레이션은 총 길이 기준 그대로다. 기준을 바꾸면 스테이지 시드를 다시 구워야 한다.
- **완화가 사람에게 듣는지는 미검증.** 증명한 것은 "지연을 가진 순수 추종자에게 쉬워졌다"이지 "사람에게 쉬워졌다"가 아니다.
