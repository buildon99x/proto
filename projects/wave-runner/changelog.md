# Changelog

> **0.5.x 가 두 번 나온다.** 기체 베리에이션과 코스 큐레이션이 0.5.0 이전에 갈라져
> 서로 모른 채 각자 번호를 썼다. 0.6.0 이 둘을 합쳤고, 지난 항목은 어느 줄기인지만
> 표시하고 그대로 둔다 — 번호를 다시 매기면 커밋 기록과 어긋난다.

## 0.1.0 — 2026-09-14

1단계 수직 슬라이스. 빌드·게이트·Endless·솔버는 포함하지 않는다.

- 원버튼 비행 — 홀드=상승 / 릴리스=하강, 중립 없음, 1히트 즉사, 고정 스텝 1/240초 적분과 스윕 충돌
- 수제 스테이지 5개 (첫 물결 / 좁아지는 길 / 톱니 / 회랑 / 합류). 회랑은 짧은 탭 연타로 직진을 합성해야 통과된다
- 사망 후 500ms 자동 재시작. 확인 입력 없음
- 선행 가시 시간 1.2초를 줌 규칙으로 구조적 보장 + 회귀 테스트
- 개발 튜닝 패널(T) — 각도·속도·관성·크기 실시간 비교. 부사 3축의 셋째를 정하기 위한 계측기
- 스테이지 목록, localStorage 진행 저장, WebAudio 효과음 3종
- e2e 오토파일럿 시나리오 — 5개 스테이지 통과 가능성 검증

## 0.2.0 — 2026-09-15

2단계. 부사 3축·교환 게이트·섹터 4유형·두 모드·메타 해금.

- **부사 3축** 각도·속도·편향. 동사(홀드=상승/릴리스=하강)는 불변이고 수행 계수만 런마다 조립된다
  - 셋째 축은 측정으로 정해졌다 — 후보였던 관성과 크기는 모든 구간에서 한쪽만 유리해 탈락했다
- **교환 게이트** 한 축 +1 / 다른 축 −1. 통로가 두 관으로 갈라지고 선택 행위가 곧 회피 조작이라 게임이 멈추지 않는다
- **섹터 4유형** 협곡·회랑·산개·맥동, 수제 12개. 각 유형이 특정 축의 부호를 뒤집도록 설계됐다
- **Endless 모드** 무한 코스 + 통로 조임으로 난이도 천장을 열었다
- **Stage 티어** 4티어 × 3스테이지. 시드는 통과율 45~85% 구간으로 선별해 구웠다
- **메타 해금** 프리셋·축 상한·확장 섹터 풀. 전부 출발점이 넓어지는 것이지 강해지는 것이 아니다
- **아바타가 곧 빌드 표시** 궤적 색이 축 구성을 나타내고, 게이트 통과 직후 오른 축 색으로 고리가 퍼진다
- **검증 4종** 축 양날 프로브 · 전 경로 통과 가능성 · Endless 램프 · 시드 큐레이션

수정한 버그

- 게이트 교차 판정이 이미 지나간 조각을 못 찾아 **교환이 한 번도 적용되지 않았다**
- 게이트 출구에서 칸막이가 선 채로 바깥벽이 좁아져 아바타를 끼웠다 — 갈라진 길이 다시 합쳐지게 했다
- 섹터마다 통로 중앙이 달라 고정 y로 출발하면 첫 프레임에 벽 안이었다 — 규격 입구·출구를 도입했다

## 0.3.0 — 2026-09-15

3단계. 정확한 솔버, 절차적 생성, 리듬 격자, 연습 모드, 사망 원인 표시.

- **정확한 솔버** — 도달 가능 집합을 구간 집합으로 정확히 전파해 통과 가능/불가능을 판정하고 **생존 회랑 폭**을 계측한다. 관성이 축에서 탈락해 수직 속도가 입력만으로 결정되기에 가능해진 일이다
  - 폭을 시간으로 환산(`slack ≈ W/2·rate`)해, 난이도를 "여유 190ms" 같은 사람의 단위로 말할 수 있게 했다
- **절차적 생성** — 형상을 매개변수화하고 후보를 솔버로 채점해 목표 폭에 가장 가까운 것만 내보낸다. "생성했는데 통과 불가"가 원천 차단된다
- **리듬 격자** — 장애물·셔터·계단 경계를 120BPM 박자에 스냅. 속도 축은 리듬 게임의 배속 조절과 같은 역할을 한다
- **프레임 예산 준수** — 생성은 프레임당 3ms만 진행하고 못 끝내면 사전 검증된 수제 섹터로 폴백한다. 측정 결과 p50 16.7ms / p99 17.5ms
- **Endless 런타임 생성** — 현재 빌드를 입력으로 코스를 뽑으므로 "이 빌드로는 불가능"이 생기지 않는다
- **연습 모드** — 게이트마다 체크포인트, 사망 시 거기서 재개. 기록에는 남지 않는다
- **사망 원인 표시** — 접촉 지점에서 지나갈 수 있었던 자리를 0.5초간 겹쳐 보여준다. 정지 화면 없이, 글자 없이
- **검증 6종** — 솔버 교차 검증과 생성기 겨냥 검사가 추가됐고, 전 경로 검증과 큐레이션이 솔버 기반으로 바뀌었다

설계 판단이 바뀐 곳

- **큐레이션 기준이 통과율에서 여유로.** 솔버로 재보니 통과 가능성은 94~100%로 거의 무의미했고 여유가 198ms와 3ms로 갈렸다. 이제 최선 경로의 여유를 티어 목표에 맞추고, 최선과 최악의 차이가 35ms 이상인 시드만 채택한다 — 차이가 없으면 게이트가 아무것도 묻지 않는다

수정한 버그

- 솔버의 도달 집합을 **이동 후 합집합**으로 전파해 격자 위의 점들이 되었고 측도가 0이었다. 플레이어는 아무 때나 입력을 바꿀 수 있으므로 **팽창**이 맞다. 오토파일럿과의 교차 검증이 잡았다

## 0.3.1 — 2026-09-15

Endless 코스 재현성 수정.

수정한 버그

- **같은 시드가 같은 코스를 주지 않았다.** 폴백을 쓸 때만 수제 섹터를 뽑느라 난수를 더 썼다 — 공장이 제때 끝냈는지, 즉 **프레임 타이밍이 난수 스트림을 바꿨다**. 사양을 뽑는 시점에 폴백까지 함께 정해 소비량을 고정했다
- **섹터 유형 순환이 어긋났다.** 자리마다 유형을 두 번 뽑아, 공장에 주문한 유형과 폴백으로 쓴 유형이 서로 달랐다. 자리 하나에 사양 하나만 두고 주문에 자리 번호를 붙여, 늦게 도착한 생성물이 엉뚱한 자리에 끼지 못하게 했다
- **첫 세 자리를 수제 섹터로 고정했다.** 0·1번은 생성자가 곧바로 채우고 2번은 첫 프레임에 필요해져 공장에 줄 시간이 사실상 0이었다 — 기기 속도에 따라 생성물이 되기도 폴백이 되기도 하던 유일한 자리다. 3번부터는 14초 앞서 주문하므로 공장이 진 적이 없다
- Endless 램프 검증이 `course` 가 다시 내보내지 않는 `SECTOR_LEN` 을 들여와 단조성 비교가 항상 `NaN` 이었다

- **검증 추가** — `endless-determinism.ts`: 공장 예산을 0~50ms로 흔든 주행들이 같은 코스를 주는지, 실제 프레임 밀도에서는 통로 좌표까지 같은지 본다

## 0.3.2 — 2026-09-15

플레이 중 발견한 버그 수정.

- **Endless 재시도가 같은 코스를 반복했다.** `restart` 가 같은 config 로 다시 만들어 시드가 그대로였다. 0.3.1 에서 생성이 결정적이 되면서 드러난 것으로, 그전에는 프레임 타이밍이 코스를 흔들어 가려져 있었다. 재시도마다 시드를 LCG 한 스텝 굴린다 — 런 전체는 여전히 (최초 시드, 재시도 횟수)로 재현된다
- **반복 클리어에도 "+N 코어 (최초 1회)" 를 보여줬다.** 실제 지급액은 0이다. 지급한 값을 그대로 표시한다
- 실행 불가였던 `axis-probe.ts` 삭제 — 2단계에서 없어진 `stages` 모듈을 임포트하고 있었다. 이 검증기가 답하려던 질문(셋째 축)은 `sector-probe.ts` 가 현재 모델 위에서 대신한다

알려진 문제

- **게이트의 24%가 교환이 아니다.** `applyTrade` 가 축별로 따로 클램프하므로 오를 축이 이미 상한이면 +1 만 먹히고 −1 은 적용된다(순손해 9%), 반대면 공짜 상승(순이득 9%), 둘 다면 무효(6%). 측정은 축 상한 ±2·게이트 5600개 기준이다. 이를 막으려고 쓴 `canOffer` 는 어디서도 호출되지 않는다. 표시도 실제와 무관하게 늘 화살표와 막대를 그린다
- **Endless 도입부 3섹터가 사실상 고정이다.** 0.3.1 이 그 자리를 수제 섹터로 못박았고, 난이도 램프상 그 구간의 목표 난이도가 1.0~1.26 이라 유형별 후보가 하나뿐이다

## 0.4.0 — 2026-09-15

게이트를 진짜 교환으로 만들고, 그 위에서 스테이지를 다시 큐레이션했다.

- **게이트 제안을 직전 게이트를 지날 때 정한다.** 코스 조립 시점에 박아 두면 지나갈 때쯤 축이 상한에 닿아 한쪽만 먹혔다 — 측정하니 축 상한 ±2 에서 게이트의 **24.5%** 가 교환이 아니었다(무효 6.0 · 순손해 9.2 · 공짜 상승 9.3). 쓰이지 않던 `canOffer` 를 제 자리에 넣어 양쪽이 모두 살아 있는 쌍만 남긴다. 지금은 **0%** 이고 세 축의 합이 런 내내 0 을 벗어나지 않는다
  - 게이트에 시드를 고정해 두어 제안은 (코스 시드, 지나온 선택)의 결정적 함수다. "같은 스테이지는 언제나 같은 코스"는 유지되고, 축 상한을 해금하면 통로는 그대로인 채 제안만 달라진다
  - 솔버도 같은 함수를 부르므로 경로마다 제안이 갈리는 것까지 정확히 푼다
- **스테이지 코스가 해금 상태를 보지 않는다.** 확장 섹터 풀을 사면 같은 번호의 스테이지가 다른 코스가 되고 있었다. 스테이지는 수제 12개 전부를 쓰고, 해금은 Endless 생성기에만 걸린다
- **스테이지 시드 12개 재큐레이션.** 판정 기준도 바꿨다 — 여유 목표는 처음 만나는 조건(±2)으로 재고, "모든 경로 통과 가능"은 ±2 와 ±3 **양쪽에서** 요구한다. 상한을 해금했다고 보이지 않는 막다른 길이 생기면 해금이 아니라 함정이다
  - 전 경로 검증도 두 상한에서 돌린다: 12스테이지 × 16경로 × 2상한 = 192경로 전부 통과 가능
- **검증 추가** `gate-offers.ts` — 실제 주행으로 통과한 게이트마다 정확히 두 축이 ±1 움직였는지 센다

바뀐 것

- **Endless 가 조금 어려워졌다.** 모든 게이트가 실제로 기체를 바꾸므로 중립에 머무르는 주행이 불가능하다(오토파일럿 지연 20ms 기준 도달 거리 7479 → 6164)
- Endless 램프 검증의 판정 기준을 "인접 구간 단조 감소"에서 "충분히 큰 지연에서 도달 거리가 무너진다(≤40%) + 두 섹터 넘는 반등 없음"으로 바꿨다. 20ms 와 60ms 는 둘 다 통로를 따라갈 만큼 빨라서 그 사이를 가르는 것은 지연이 아니라 뽑힌 섹터와 빌드이고, 표본을 8개로 늘려도 인접 두 칸은 뒤집힌다

## 0.5.0 — 2026-09-16 (기체 줄기)

4단계의 첫 조각 — **기체 베리에이션의 각도 축.** 스킬과 룬은 아직 없다.

- **기체 4종** 표준·둔각·예봉·환. 프리셋이 아니라 **축 곡선**이 다르다 — 시작 눈금이 아니라 눈금을 계수로 옮기는 방식이 다르고, 축은 하나도 늘지 않았다
  - 곡선이 `Tuning` 에 접혀 들어가므로 **솔버·오토파일럿·카메라·검증 스크립트가 한 줄도 바뀌지 않았다.** 축 상한이 이미 쓰던 방식이다
  - 표준 기체의 계수는 2단계 표와 **소수점까지 같다**(편차 0). 그 위에서 구운 스테이지 시드와 여유 수치가 그대로 살아, 기존 검증 8종이 전부 같은 값으로 통과한다
- **지그재그 각도를 계측했다** — 기본 45°/45°(꼭지각 90°), 축으로 21.6°~64.5°. 그리고 **속도는 각도를 전혀 바꾸지 않는다**(변동 1.4e-14°). `rate = slope × (1±bias) × speed` 이고 각도가 `atan(rate/speed)` 라 속도가 약분되기 때문이다. 줌도 x·y 에 같은 배율이라 월드 각도가 곧 화면 각도다
- **실루엣이 곡선에서 파생된다** — 뒤로 젖혀진 두 날이 실제 상승·하강 각도 그대로 뻗어, 코가 벌어진 각이 그 기체의 꼭지각이다(표준 90.0° · 둔각 78.7° · 예봉 102.2°). 히트박스는 전부 같고 외접원도 같다
- **기록이 기체별로 나뉜다** — 키가 `"기체:티어:번호"` 가 되고 Endless 최고 거리도 기체별이다. 스테이지 셀에 클리어한 기체의 실루엣이 쌓인다. 티어 해금만은 기체를 보지 않는다
- **프리셋 해금 폐지** — 기체가 시작 빌드를 정한다. 저장이 v3 으로 오르고 v2 는 자동 이관된다(기록은 표준 기체로, 프리셋은 개당 120 코어 환급). v2 키는 지우지 않는다
- **검증 3종 추가** `angles.ts`(각도표·속도 독립성·중립 없음·표준 기체 동일성) · `runner-probe.ts`(기체가 양날인가) · `runner-paths.ts`(1536경로 공정성)

설계 판단이 바뀐 곳

- **양날은 폭이 아니라 중심에서 나온다.** 기획서의 가설은 반대였다 — 폭이 양날이고 중심은 난이도 슬라이더라고 봤다. 프로브가 뒤집었다: **폭은 좁을수록 전 섹터에서 유리했다**(둔각 > 표준 > 예봉 > 환 > 잔상 단조). 하방이 없으면 축이 아니라 스탯이라는, 2단계에서 관성과 크기를 탈락시킨 바로 그 모양이다. 중심은 부호가 뒤집혔다
- **편향에 중심을 넣었다.** 기획서는 "축 합 불변식과 어긋난다"며 배제했는데 그 근거가 틀렸다 — 불변식은 **눈금**의 합이지 값의 합이 아니다. 영구적인 치우침이 오르는 회랑과 내려가는 회랑에서 가장 깨끗하게 부호를 뒤집는다(환: 상승회랑 +41% / 하강회랑 −12%)
- **기체 6종이 4종이 됐다.** 각인은 속도 곡선이 들어오기 전에는 표준과 구분되지 않아 미뤘고, 잔상은 폭만 넓어 전 섹터에서 불리해(−10~−32%) 뺐다
- **공정성 판정에 "사람 하한"을 넣었다.** 통과 가능성만 보면 여유 11ms 인 경로도 통과로 세어진다 — 솔버가 맞아도 사람의 타이밍 산포보다 좁다. 모든 (기체, 스테이지, 상한) 에서 최선 경로의 여유가 40ms 이상일 것을 요구한다

프로브가 잡은 것

- **중립 한 점에서 재면 폭이 안 보인다.** 중심이 1.0 인 기체가 표준과 정확히 0% 로 나왔다 — 당연하다, 눈금 0 의 계수가 글자 그대로 같다. 빌드 표본을 훑도록 고쳤다
- **최댓값으로 재면 전부 뭉갠다.** 그 유형을 가장 잘 푸는 빌드를 쓰면 전 기체가 ±7% 안에 들었다. 최댓값은 **플레이어가 갖지 못한 자유**를 모델링한다 — 게이트는 거절할 수 없고 매 섹터 두 축을 강제로 민다. 평균으로 바꿨다
- **유형 평균이 편향의 부호를 상쇄한다.** 회랑은 평탄·상승·하강 셋인데 편향의 유불리는 상승과 하강에서 정확히 반대다. 표는 유형별로 보여주되 판정은 섹터 12개 각각으로 한다
- **환이 스테이지 3·2 를 전 경로 통과 불가였다.** 영구 치우침 위에 시작 눈금까지 편향을 얹어 가파른 하강 회랑이 막혔다. 시작 빌드를 중립으로 되돌렸다 — 난이도가 아니라 함정이었다

## 0.5.1 — 2026-09-16 (기체 줄기)

기체를 **읽히게** 만들었다. 메커니즘은 그대로다.

0.5.0 을 실제로 달려 보니 네 기체는 측정상 확실히 다른데 화면에서는 구분되지 않았고,
설명문은 체감과 **반대로** 읽혔다.

- **피커가 실제 지그재그를 그린다.** 도형 하나로는 표준 45.0° 와 둔각 39.4° 의 5.6° 차이가 잡히지 않는다. 각도를 과장하면 "코가 벌어진 각이 곧 꼭지각"이 거짓말이 되므로, 과장 대신 여러 주기를 보여준다 — 같은 폭에 담기는 봉우리가 둔각 2 · 표준 3 · 예봉 4 로 갈리고 치우친 기체는 톱니가 기운다. x·y 배율이 같아 각도 왜곡이 없다
- **측정된 성격을 막대 두 개로 보여준다.** `runner-grades.ts` 가 스테이지 12개를 끝까지 달려 **여유**(클리어되는 최대 입력 지연)와 **길**(16경로 중 통과 비율)을 재서 `runner-grades.json` 으로 굽는다. 두 축인 이유는 예봉이 "길은 절반인데 가장 너그럽다"이기 때문이다 — 하나로 뭉치면 그 대비가 사라진다. 오토파일럿이 잰 값이라 숫자는 적지 않고 기체 사이 비교인 막대로만 그린다
- **설명문을 측정에 맞췄다.** 첫 판본은 설계 의도를 적어서 "완만하다"는 둔각이 가장 빡빡하고 "오버슈트한다"는 예봉이 가장 너그러운 실제와 어긋났다
- **스테이지 칸의 기체 표시를 자리로 바꿨다.** 작은 실루엣을 나열했더니 12px 에서 네 도형이 구분되지 않아 개수만 읽혔다. 기체마다 고정 칸을 두어 **위치가 기체를 지정**한다 — 빈 칸이 곧 남은 숙제다
- **검증 추가** `runner-grades.ts`(등급표를 굽는다) · `angles.ts` 에 등급표 누락 검사

측정 기록 (축 상한 ±2, 스테이지 12개)

| 기체 | 여유(중앙값) | 길 /16 |
|---|---|---|
| 표준 | 70ms | 11.5 |
| 둔각 | 0ms | 9.4 |
| 예봉 | 110ms | 5.7 |
| 환 | 80ms | 10.7 |

바로잡은 것

- 0.5.0 플레이테스트 보고에서 둔각을 "경로는 넉넉한데 실행이 빡빡하다"고 적었는데, 그건 예봉과 비교할 때만 맞다. **표준 대비로는 두 축 모두 낮다** — 전체로 가장 까다로운 기체다

## 0.5.2 — 2026-09-16 (기체 줄기)

사용자 경험·시각 완성도 리뷰를 따로 돌리고 지적을 코드에서 하나씩 확인했다. 확인이
본체였다 — 지적 하나는 **내가 쓴 문서가 근거**였고 둘은 서로 충돌했다.

### 고쳐진 보장

- **선행 가시 1.2초가 지켜지고 있지 않았다 — 실제 0.86~0.93초.** `cap` 이 화면 **전체 폭**을 건너는 시간을 쟀는데, 아바타는 `cameraAnchor` 지점에 있어 그 왼쪽은 이미 지나온 구간이다. 공식에 `(1 − cameraAnchor)` 를 넣어 **코앞 시간**을 직접 보장하고, 줌 감소를 상쇄하려 `cameraAnchor` 를 0.28 → 0.18 로 당겼다. 390×720 에서 줌 6.35 · 레터박스 위아래 43px · 코앞 정확히 1.20초
- **회귀 테스트가 같은 오류를 복사하고 있었다.** `lookahead.test.mjs` 가 "2줄만 재기술"한 식이 구현과 똑같이 틀려 있었다. 공식을 고치는 것과 별개로, 아바타를 화면에 놓고 우측 끝 픽셀의 월드 좌표를 역산하는 **독립 검산**을 추가했다 — 공식이 또 틀려도 그 줄은 틀리지 않는다
- **그려진 기체가 히트박스보다 1.5배 컸다.** `silhouetteOf` 는 "보이는 것과 판정이 어긋나 보이면 안 된다"며 외접원 1 로 정규화해 놓고 렌더가 `r × 1.7` 을 곱하고 있었다. 각도를 과장하지 않으려 애쓰면서 크기는 과장하고 있었다. 이제 그려진 도형의 최원점이 곧 판정 반지름이다

### 막혀 있던 길

- **폰에서 런을 빠져나갈 수 없었다.** 종료가 `Escape` 하나뿐 · 오버레이는 `pointer-events:none` · 스테이지 사망은 자동 재시작 — 셋이 겹쳐 들어가면 완주 전까지 갇혔다. 캔버스의 형제로 좌상단 나가기 버튼을 항상 둔다(`running` 중에는 거의 투명). e2e 시나리오도 이제 그 버튼으로 화면을 옮긴다
- **두 손가락 홀드가 깨졌다.** `pointerup` 하나만 와도 홀드가 풀려 손가락을 바꾸는 순간 낙하했다. 활성 포인터를 세서 하나라도 닿아 있으면 홀드로 친다
- 폰에 없는 키를 누르라고 쓰던 푸터와 "Esc 나가기" 문구를 뺐다. 음소거는 실제 버튼이 맡는다

### 읽히는 방향

- **게이트 표식이 고를 수 없을 때 나타났다.** 분기 중앙에 두던 탓에 표식이 보인 뒤 관이 확정되기까지 0.66초뿐이었고, 정작 1.5초짜리 리드인은 비어 있었다. 분기점에 묶어 **화면 우측 끝에 핀으로 고정**했다 — 게이트가 화면 밖인 동안 한자리에서 읽히고, 분기가 들어오면 함께 흐른다
- **교환 표식의 위계가 결정과 반대였다.** 두 관의 오르는 축이 같은 경우가 흔한데 큰 쐐기가 그 축이고, 유일한 정보인 내리는 축이 가장 약한 요소였다. 막대를 쐐기와 같은 무게로 올리고, 오르는 축이 **실제로 같을 때만** 쐐기를 죽인다
- **벽이 통로보다 2.4배 밝았다.** 죽는 영역이 밝으면 순간 판단의 반사가 반대로 걸린다. 통로를 밝게, 벽을 거의 검게 뒤집었다. 벽 폴리곤이 레터박스까지 덮으므로 남는 띠도 "갈 수 없는 곳"으로 읽힌다
- **벽 색이 각도 축 색과 같은 `#3de1ff` 였다.** 각도 쐐기가 벽과 같은 색이고, 각도가 오른 교환 펄스가 벽 색으로 번지고, 각도를 올린 아바타가 벽 계열로 흡수됐다. 벽을 중성 은청으로 뺐다 — 축 팔레트는 빌드 전용이다
- **사망 프레임에 "여기에 맞았다"가 없었다.** 맞은 자리에 흰 X 를 얹고, 첫 프레임부터 감쇠하던 알파를 앞 40% 는 세웠다. 띠 색을 완주선의 초록과 분리했다. 재시도 지연은 건드리지 않았다 — 루프 속도가 이 게임의 재미다
- **근접 강조.** 코앞 구간의 실제 여유가 히트박스 3배 안으로 들어오면 그 구간의 벽이 밝고 굵어진다. 과장이 아니라 화면 밖에서 재던 값을 안으로 옮긴 것이다
- **게이트 통과음.** 73초 런에 들리는 소리가 시작·끝 둘뿐이었다. 오른 축을 음정으로 실어 화면 정보를 늘리지 않고 채널을 하나 얻는다

### 피커의 봉우리 주장은 틀렸다

0.5.1 은 "봉우리가 둔각 2 · 표준 3 · 예봉 4 로 갈린다"고 적었다. **세어 보지 않고 썼다.**
실제 값은 1 · 1 · 2 · 1 이고 표준과 둔각은 같은 그림이었다. 다리 개수가
`w × slope / (2 × h × amp)` 라 **창 너비가 약분되므로**, "여러 주기를 보여준다"에는 애초에
레버가 없었다.

누적 대신 **비교**로 바꿨다 — 표준(45°)의 지그재그를 점선으로 깔면 위상차가 오른쪽으로
갈수록 벌어져 카드 끝에서 진폭의 절반만큼 어긋난다. 과장은 여전히 한 줄도 없다. 깔린 선도
실제 궤적이다.

### 충돌한 두 권고

시각 리뷰는 궤적을 0.46초 → 1.2초로 늘려 런 안에서도 기체가 구분되게 하라고 했다.
카메라를 고치면서 `cameraAnchor` 가 0.18 로 내려가 **보이는 과거가 0.26초**가 됐고, 늘린
궤적은 화면 밖에서 그려진다. 288 스텝으로 찍어 보고 110 으로 되돌렸다. 선행 시야가 과거
시야보다 우선한다. **런 안의 기체 구분은 미해결로 남긴다** — 값싼 해법이 없고 있는
해법(도형 과장)은 거짓말이다.

### 손끝과 눈

- 주 동선(모드 2택)을 기체 그리드 **위**로. 네 카드가 설명문을 모두 펴던 판본은 370px 을 먹어 720px 화면에서 Stage 가 접힘 아래였다. 설명문은 고른 기체에만 편다
- 스테이지 목록에 **현재 기체**를 세우고, 네 칸 슬롯 중 현재 기체 자리에 링을 둘렀다 — 기체를 바꾸면 링이 옮겨가며 "자리 = 기체"가 스스로 가르쳐진다. 그 매핑을 알려 주는 채널이 `title` 속성(터치에서 안 뜬다)뿐이었다
- 돌아가기를 목록 맨 아래에서 위로. 탭 타깃 26px → 44px. 상점 버튼·`summary` 도 44px
- 성격 막대 라벨 9.5px `#6d7d99`(대비 4.48:1, AA 미달) → 11px `#8b9ab5`(6.3:1). 트랙을 보이게 하고, 0 을 7% 비율이 아니라 3px 최소 두께로 — 눈금의 바닥과 "데이터 없음"이 갈린다
- 기체 그리드를 2열 그리드로(고정 148px 두 장이 패널 우변과 46px 어긋나 있었다). 티어별로 다르던 셀 높이 고정. 빈 슬롯 테두리 1.57:1 → 2.3:1. 잠긴 티어는 헤딩 말고 칸만 죽인다
- `:active` 와 `:focus-visible` 이 693줄 어디에도 없었다 — 모바일에서 탭해도 다음 화면이 뜰 때까지 아무 반응이 없었다
- 상점에서 "보유"와 "코어 부족"이 같은 회색이었다. 현재 축 상한도 보이지 않아 "±3" 의 뜻이 서지 않았다
- 코어 줄이 곧 해금 진입이다(별개 링크 하나와 스크롤 한 번이 준다). "최고 티어"가 티어 **개수**를 세고 있었고, 같은 뜻을 정확히 계산하는 `highestTierCleared` 는 죽어 있었다
- 성능: 프레임마다 부르던 `getBoundingClientRect()`(강제 리플로우) 제거. 벽 글로우를 `shadowBlur` 에서 2패스 스트로크로 — DPR2 에서 그림자는 화면 전체 재래스터화다. 완주선을 지나간 뒤에도 매 프레임 그리던 것 정지. 판정은 살아 있는데 안 보이던 셔터 구간(`depth ≤ 0.2`) 제거


## 0.5.0 — 2026-09-15 (코스 줄기)

플레이 피드백. 달리는 동안 얼마나 왔는지 알 수 있게 하고, 끝난 뒤 기록을 마주 보게 했다.

- **주행 표시** — 화면 맨 위 3px 진행 레일과 좌상단 숫자(Stage 경과 초 · Endless 거리). brief 의 "HUD 없음"과 정면으로 부딪히는 자리라, 조항이 지키려던 것을 먼저 갈라놓고 지었다
  - 지키려던 것은 픽셀의 수가 아니라 **시선 예산**이다. 그래서 표시를 통로와 그 앞을 훑는 시선 경로 **밖**에만 뒀다 — 레일은 숫자가 없어 주변시로 읽히고, 숫자는 아바타(좌측 28%)의 뒤쪽이라 선행 가시 시선과 겹치지 않는다
  - 그래도 비용이 0 은 아니므로 **끌 수 있다**(주행 중 `H`, 홈 토글, `meta.hud` 로 저장). 끄면 0.4.0 까지의 무표시 주행 그대로다. brief 의 불변 항목은 "시선을 요구하는 HUD 없음"으로 좁혀 다시 썼다
  - Endless 레일은 **최고 거리 × 1.25** 를 눈금으로 삼는다. 기록이 80% 지점에 서므로 넘어서는 것이 화면에서 보인다
- **기록 화면** — 클리어·Endless 종료에서 이번 기록·최고 기록·시도(또는 코어)를 같은 크기로 나란히 놓고, 갱신했으면 신기록과 차이를 말한다. Stage 결과에는 **그 티어 세 스테이지의 최고 기록**이 함께 뜬다. 출발 화면(`ready`)에도 겨룰 기록을 미리 보여준다 — 주행 중에 읽게 하면 그것이 곧 시선 비용이다
- **시각 정리** — 오버레이 등장 모션(160ms, `prefers-reduced-motion` 존중), 신기록 배지, 스테이지 목록의 최고 기록 표기, 기록이 없을 때 `0` 대신 "아직 기록 없음"

의도적으로 넣지 않은 것

- **Stage 의 페이스 고스트.** 주행 중 기록 비교는 참·거짓이 확정되는 것만 말한다 — Endless 는 "최고 거리를 넘었는가", Stage 는 "경과가 최고를 넘었는가"(넘는 순간 경신 불가가 확정된다). "지금 기록보다 앞서는가"는 빌드마다 속도가 달라 주행 궤적을 저장해야 정확해지고, 추정을 기록처럼 보여주지 않으려고 뺐다

수정한 버그

- 자동 플레이테스트의 주행 상한(180초)이 puppeteer `protocolTimeout` 기본값과 같아, Endless 가 오래 버틴 날에는 시나리오가 아니라 **프로토콜이 먼저 끊겨** 실패했다. 상한을 120초로 낮춰 여유를 뒀다
- 시나리오에 주행 중 화면 두 장(`03b-stage-running`, `06b-endless-running`)을 추가했다 — 페이지 안에서 도는 오토파일럿을 기다리지 않고 그 사이에 찍는다

## 0.5.1 — 2026-09-16 (코스 줄기)

스테이지 재큐레이션. 함정 경로를 없애고 코스를 갈랐다.

큐레이션 기준 네 가지를 바꿨다.

- **공정성에 하한이 생겼다(50ms).** 그전 1번 기준은 `passable` 뿐이었는데 솔버의
  `passable` 은 여유 3ms 도 참이다. 12스테이지 중 4개가 사람에게 불가능한 경로를 품고
  있었고(티어 1 의 두 스테이지는 16경로 중 8개) 검증은 전부 통과하고 있었다
  - 하한 값은 예민하지 않다. 후보들의 최악 여유가 **양극 분포**라(티어 4 기준 0~10ms
    48개 · 60ms 이상 145개 · 그 사이 3개) 30ms 든 60ms 든 남는 후보가 같다
- **최소 스프레드 기준을 없앴다.** 후보 3600개를 전수로 풀어 보니 어떤 값에서도 병목이
  아니었고, 방향이 더 나빴다 — 스프레드는 최선과 최악의 차이이므로 **함정이 깊을수록
  높은 점수를 받는다.** 함정이 가장 깊던 두 스테이지의 스프레드가 206·207ms 로 최고였다
- **첫 합격에서 멈추지 않는다.** 그전에는 조건을 만족하는 첫 시드에서 `break` 해서 인접
  티어가 같은 골격으로 빨려 들어갔다. 이제 후보를 모두 모은 뒤, 티어를 가로질러 이미
  채택한 코스와 같은 자리 섹터가 3개 이상 겹치면 건너뛴다
- **티어마다 성격을 요구한다.** 티어 1 협곡·회랑 위주 · 티어 2 산개 · 티어 3 맥동 ·
  티어 4 3유형 이상

결과 (축 상한 ±2)

- 함정 경로 **23개 → 0개**. 최악 여유의 최소값 3ms → 56ms
- 코스 중복 4/5 이상 **9쌍(완전 동일 1쌍) → 0쌍**, 최악 2/5
- 티어 곡선 214/178/152/115 → **187/159/141/122** (목표 190/160/130/100)
- 섹터 편중 완화 — gorge-2 28% → 20%, `corridor-flat` 1회 → 5회, `scatter-1` 0회 → 4회

**시드가 전부 바뀌었으므로 저장된 스테이지 최고 기록은 다른 코스의 것이 된다.**

남은 것

- **티어 4 가 목표보다 +22 쉽다.** 함정이 만들던 어려움은 여유로 되찾을 수 없다 —
  최난 구간의 지속 길이를 티어 지표로 들여야 한다. T4·1 과 T4·2 는 성격까지 쌍둥이다
- **±3 에서 곡선이 역전된다**(티어 3 이 티어 2 보다 쉽다). 큐레이션이 ±2 로만 재고
  ±3 은 공정성만 묻기 때문이다

수정한 것

- 플레이테스트 주행 상한을 240초로. Stage 는 사망하면 처음부터라서 상한이 한 주행이
  아니라 몇 번의 재시도를 담아야 하는데 120초는 한 번 죽으면 모자랐다. 함께
  `playtest.mjs` 의 `protocolTimeout` 을 600초로 열었다 — 기본값 180초가 시나리오의
  상한을 대신 정하고 있었다

## 0.5.2 — 2026-09-16 (코스 줄기)

티어 사다리를 도달 가능한 범위로 다시 잡고, 티어 4 의 난이도 축을 바꾸고, 해금이 난이도를
되돌리던 것을 막았다.

- **목표 사다리 190/160/130/100 → 200/180/150/130.** 그전 사다리는 티어 4 에서 도달 불가라
  밴드 완화가 상시 걸렸다 — 목표가 목표 노릇을 못 했다. 공정성 하한을 통과한 후보 3600개의
  실제 분포에서 다시 잡았다. ±2 곡선이 **211 → 181 → 158 → 146** 으로 계단 30/23/12 가 됐다
- **티어 4 의 난이도 축을 여유에서 지속으로 옮겼다.** 여유로는 티어 3(158ms)과 티어 4(146ms)를
  12ms 밖에 못 벌린다. 최난 구간(최선×1.25 이내가 이어지는 길이)으로 재면 티어 1~3 이
  45~79단위, 티어 4 가 **352~373단위**로 5~8배 벌어진다
  - 이 축은 취향이 아니라 측정이다. 티어 1~3 후보는 **전부** 140단위 이하이고(슬롯 난이도상
    구조적으로 뾰족할 수밖에 없다) 티어 4 는 146개 중 88개가 330단위 이상이다
  - 티어 4 는 이제 "더 좁다" 가 아니라 **"더 오래 좁다"** 다. `corridor-flat` 이 1회에서 6회로
    늘어난 것이 그 결과다
- **해금이 난이도를 되돌리지 못하게 했다(`AXIS_CAP_DRIFT_MS` 30).** ±3 곡선이 역전돼 있었다 —
  티어 3 이 티어 2 보다 쉬웠고, 한 스테이지는 ±2 에서 143ms 인데 ±3 에서 200ms 로 튀었다.
  큐레이션이 ±2 로만 재고 ±3 은 공정성만 물었기 때문이다. 두 상한의 최선 여유 차이를 묶으니
  ±3 곡선이 **191 → 171 → 155 → 133** 으로 단조가 됐다
- **섹터 커버리지를 동점 처리에 넣었다.** 목표 근접도가 16ms 안에서 비슷하면 아직 안 쓴
  섹터를 데려오는 쪽을 고른다. 난이도를 희생하지 않고 11/12 를 유지한다
- **티어 사다리 상수를 `tests/verify/tiers.ts` 로 모았다.** 큐레이션과 점검 도구가 각자 목표
  공식을 들고 있어서, 사다리를 옮긴 뒤 점검표의 "편차" 열이 옛 목표로 계산돼 멀쩡한
  스테이지가 +51ms 벗어난 것처럼 보였다

결과 (±2)

| | 0.5.0 | 0.5.1 | 0.5.2 |
|---|---|---|---|
| 함정 경로 | 23개 | 0 | 0 |
| 코스 중복 4/5 이상 | 9쌍 | 0쌍 | 0쌍 |
| 곡선 | 214/178/152/115 | 187/159/141/122 | **211/181/158/146** |
| ±3 곡선 | 168/157/172/126 ⚠역전 | 168/171/155/133 ⚠역전 | **191/171/155/133 ✓** |
| 티어 4 최난 구간 | 373/56/43 | 373/373/43 | **352/373/373** |
| 섹터 커버리지 | 11/12 | 11/12 | 11/12 |

남은 것

- **`gorge-3` 를 쓸 수 없다.** 그 섹터를 포함한 후보 중 함정이 없는 것이 5% 뿐이고(다른
  섹터는 41~88%), 그 5% 도 티어 4 밴드 밖이다. 어떤 빌드로도 대응 못 하는 경로를 만들어
  내는 섹터라, 고치려면 큐레이션이 아니라 섹터 자체를 손봐야 한다
- **게임 전체가 0.5.1 보다 관대해졌다.** 사다리를 올린 대가다. 티어 4 는 지속으로 메웠지만
  티어 1~3 은 여유가 그대로 넓어졌다

## 0.5.3 — 2026-09-16

협곡 3종을 다시 만들었다. 막던 섹터가 묻는 섹터가 됐고, 12개 섹터가 처음으로 전부 쓰인다.

- **`gorge-3` 는 통과할 수 없는 섹터였다.** 도달 가능한 19개 빌드(세 축의 합이 0 인 조합)로
  단독 주행을 시켜 보니 **5개가 어떤 조작으로도 통과하지 못했다.** `gorge-2` 도 ±3 에서 최소
  여유가 10ms 라 사람에게는 같은 문제였다. 큐레이션이 이 섹터를 계속 거른 이유가 이것이다
- **원인은 램프 기울기와 통로 폭의 관계를 계산에 넣지 않은 것이었다.** 램프에서 뒤처지는
  거리는 `(램프 기울기 − 기체 dy/dx) × 램프 길이` 이고, 이것이 통로의 여유 폭보다 크면
  **조작과 무관하게** 벽에 닿는다. 옛 `gorge-3`(폭 22 · 램프 1.15)은 이 값이 폭을 넘었다
- **진폭을 고정하고 램프 기울기와 계단 수로만 난이도를 올린다.** 폭은 뒤처짐을 흡수할 만큼
  넉넉히 두되(30~32) 램프를 가파르게 해서 각도를 묻는다 — 낮은 각도는 벌을 받되 죽지 않는다

  | | 진폭 | 계단 | 폭 | 램프 | 최소 여유(모든 빌드) | 각도 선호 |
  |---|---|---|---|---|---|---|
  | gorge-1 | 16→**17** | 3 | 30→**32** | 0.95→**1.05** | 113 → **110ms** | −8 → **+60ms** |
  | gorge-2 | 19→**17** | 4→**5** | 26→**30** | 1.05→**1.15** | 10 → **80ms** | +62 → **+84ms** |
  | gorge-3 | 22→**17** | 4→**7** | 22→**30** | 1.15→**1.35** | 통과불가 → **56ms** | +72 → **+104ms** |

- **협곡이 처음으로 사양대로 각도를 묻는다.** 그전 `gorge-1` 은 각도를 올리면 여유가 오히려
  8ms 줄었다 — 통로가 넓어 램프를 따라잡을 필요가 없었고 큰 각도는 오버슈트만 키웠다.
  지금은 셋 다 각도가 오를수록 여유가 늘고, 어려운 협곡일수록 더 강하게 묻는다
- **`makeGorge` / `GORGE_SPEC` 을 내보낸다.** 형상 파라미터를 도구가 스윕할 수 있어야
  "모든 빌드가 통과하는가" 를 눈대중이 아니라 솔버로 고를 수 있다

재큐레이션 결과 (형상이 바뀌었으므로 시드를 다시 골랐다)

- **섹터 커버리지 11/12 → 12/12.** `gorge-3` 이 T3·1 · T4·2 · T4·3 에 들어갔다 — 12개 수제
  섹터가 처음으로 전부 쓰인다
- **티어 4 의 후보가 열렸다.** 전 경로 통과가 256/900 → **884/900**. 협곡이 경로를 봉쇄하지
  않게 되자 티어 4 에서 고를 것이 3.5배로 늘었다
- ±2 곡선 211/181/158/146 → **199/185/151/135**, 목표(200/180/150/130) 대비 편차 −1 ~ +5
- 최악 여유의 최소값 58ms → **76ms**. 함정 0 · 코스 중복 4/5 이상 0쌍 · 네 티어 모두 완화 없음

**시드가 다시 전부 바뀌었으므로 저장된 스테이지 최고 기록은 무효다.**

알려진 문제 (0.5.4 에서 해소)

- ~~**회랑 둘에 같은 결함이 남아 있다.**~~ 새로 만든 `tests/verify/sector-fairness.ts` 가
  `corridor-up` 7개 · `corridor-down` 13개 빌드 봉쇄를 잡아냈다. 지속 경사 0.92 가 같은
  이유로 낮은 각도 기체를 막는다. **협곡처럼 파라미터로 풀리지 않는다** — 경사를 낮추면
  통과는 되지만 편향의 유불리 뒤집힘이 사라진다(하강 회랑인데 편향 + 가 유리해진다).
  경사를 유지한 채 짧게 끊고 평탄한 회복 구간을 끼우는 쪽이 답으로 보이나 형상 생성을
  다시 써야 해서 별도 작업으로 둔다
  - 두 방향은 대칭도 아니다. 세 축의 합이 0 이라 (각도 −3, 편향 −3)은 존재하지 않으므로
    **상승 한계의 최악은 0.62** 인데 (각도 −3, 편향 +3)은 존재하므로 **하강 한계의 최악은
    0.397** 이다
- **±3 에서 티어 2(159ms)와 티어 3(156ms)이 사실상 같다.** 역전은 아니지만 3ms 차이라
  해금하면 두 티어가 구분되지 않는다. 드리프트 제한이 ±2 목표만 맞추기 때문이다

검증 추가

- `sector-fairness.ts` — 12개 섹터 × 도달 가능한 모든 빌드로 단독 주행. 이 결함은 **코스
  단위 검증에 잡히지 않았다** — 문제의 섹터를 쓰는 시드가 큐레이션에서 조용히 탈락했을
  뿐이라 `stage-paths.ts` 는 계속 ✓ 였고 섹터는 죽어 있었다. 섹터는 섹터 단위로 물어야 한다

## 0.5.4 — 2026-09-16

회랑을 다시 만들었다. 이제 **12개 섹터 전부가 모든 빌드에게 통과 가능**하고, 후보 시드의
**900/900 이 전 경로 통과**한다.

- **회랑도 막는 섹터였다.** 지속 경사 0.92 를 64단위 내리 달려서, 그 기울기를 못 내는
  기체가 조작과 무관하게 벽에 닿았다 — `corridor-up` 7개 · `corridor-down` 13개 봉쇄.
  0.5.3 의 `sector-fairness.ts` 가 잡아낸 그 문제다
- **경사를 낮추는 것은 답이 아니었다.** 0.5 로 낮추니 통과는 되지만 **편향의 유불리
  뒤집힘이 사라졌다** — 하강 회랑인데 편향 + 가 유리해졌고(+60ms) 최소 여유가 224ms 로
  뛰면서 "좁고 긴 통로" 성격도 함께 사라졌다
- **경사를 짧게 끊고 사이에 평탄한 회복 구간을 둔다.** 토막 하나에서 벌어지는 거리는
  토막 수에 반비례하고 평탄 구간이 그만큼을 도로 메운다. 압박은 남고 누적만 사라진다
- **끊은 다음에는 경사를 오히려 더 가파르게 해야 한다(0.92 → 1.3).** 토막이 짧아진 만큼
  기울기를 올리지 않으면 하강 속도를 압박하지 못해, 하강 회랑인데 편향 0 이 양쪽보다
  유리해진다. 이걸 놓치면 "통과는 되는데 아무것도 묻지 않는" 섹터가 된다

  | | 폭 | 경사 | 토막 | 회복 | 통과 불가 | 최소 여유 | 편향 뒤집힘 |
  |---|---|---|---|---|---|---|---|
  | corridor-up | 22→**30** | 0.92→**1.3** | 1→**2** | —→**55** | 7개 → **0** | 2 → **69ms** | +180 → **+158ms** |
  | corridor-down | 20→**30** | 0.92→**1.3** | 1→**2** | —→**55** | 13개 → **0** | 19 → **62ms** | +160 → **+81ms** |
  | corridor-flat | 16 | — | — | — | 0 | 73ms | — |

  편향 뒤집힘이 약해진 것은 대가다. 그전의 강한 수치는 상당 부분 **봉쇄에서 나온 것**이라
  공정하게 만들면 줄어드는 것이 당연하다. 부호는 그대로다 — 상승은 편향 +, 하강은 편향 −.

- **회복 구간은 리드인·꼬리와 같은 폭으로 넓힌다.** 같은 폭으로 두면 한쪽으로 치우친
  기체가 평탄 구간에서 벌을 받아 경사가 묻는 질문을 도로 상쇄한다

재큐레이션 결과

| 지표 | 0.5.3 | 0.5.4 |
|---|---|---|
| 섹터 공정성(12개 × 모든 빌드) | 회랑 2종 실패 | **전부 통과** |
| 후보 시드 전 경로 통과(티어 3·4) | 839 / 884 of 900 | **900 / 900** |
| ±2 곡선 | 199/185/151/135 | **196/181/158/132** (목표 200/180/150/130) |
| ±3 곡선 | 194/159/156/131 | **183/169/154/129** |
| ±3 티어 2↔3 간격 | 3ms | **15ms** |
| 함정 · 코스 중복 · 커버리지 | 0 · 0쌍 · 12/12 | 0 · 0쌍 · 12/12 |

±3 에서 티어 2 와 3 이 3ms 차이로 붙어 있던 것이 15ms 로 벌어졌다 — 회랑이 공정해지면서
큐레이션이 고를 수 있는 후보가 늘어난 결과다.

**시드가 다시 전부 바뀌었으므로 저장된 스테이지 최고 기록은 무효다.**

남은 것

- **회랑의 난이도 표기가 명목뿐이다.** 번호가 드리프트에 용접돼 있어(1=상승 · 2=하강 ·
  3=평탄) 상승과 하강이 같은 형상을 쓴다. 드리프트와 난이도를 떼어내면 회랑 3종이
  9종이 되고, 티어마다 다른 편향 질문을 물을 수 있다

## 0.5.5 — 2026-09-16

회랑의 드리프트와 난이도를 분리했다. 수제 섹터 풀이 **12개에서 18개로** 늘었고,
큐레이션이 **18개를 전부** 쓴다.

- **회랑 3종 → 9종 (드리프트 × 난이도).** 0.5.4 까지는 난이도 번호가 드리프트에 용접돼
  있었다(1=상승 · 2=하강 · 3=평탄). 그래서 사양이 약속한 세 질문(평탄 각도 − · 상승
  편향 + · 하강 편향 −)이 **티어마다 한 종류씩만** 물어졌다 — 티어 1 에서는 상승만,
  티어 2~3 에서는 하강만, 평탄은 티어 4 에서만. 축을 가르는 자리가 난이도에 묶여
  낭비되고 있었다
- **난이도는 폭과 오르내리는 높이로 가른다.** 둘을 함께 움직이는 이유는 월드 크기다 —
  중앙선이 `50 ± rise/2` 를 오가므로 `rise + width` 가 94 를 넘으면 통로가 월드 밖으로
  나간다. 쉬운 칸은 넓고 짧게, 어려운 칸은 좁고 길게

  | | 폭 | 높이 | 최소 여유 | 축 선호 |
  |---|---|---|---|---|
  | corridor-up-1 / 2 / 3 | 34 / 32 / 30 | 44 / 54 / 64 | 126 / 98 / 69ms | 편향 + 139 / 148 / 158 |
  | corridor-down-1 / 2 / 3 | 30 / 28 / 30 | 54 / 54 / 64 | 115 / 87 / 62ms | 편향 − 58 / 63 / 81 |
  | corridor-flat-1 / 2 / 3 | 22 / 18 / 14 | — | 107 / 84 / 61ms | 각도 − 166 / 131 / 95 |

  값은 전부 솔버로 골랐다 — 9칸 모두 도달 가능한 빌드를 하나도 막지 않으면서 드리프트마다
  난이도가 단조로 내려간다. 축 선호도 난이도와 함께 강해진다

- **완화 순서를 바꿨다 — 지속을 성격보다 뒤에 푼다.** 첫 시도에서 티어 3 이 "성격까지
  완화" 로 떨어지며 **370단위짜리 지속형 코스**를 집었다. 0.5.2 에서 티어 4 에만 부여한
  "더 오래 좁다" 라는 정체성이 흐려진다. 성격(유형 구성)이 그보다 무른 선호다
- **해금 드리프트 제한을 30 → 20ms 로 조였다.** 30 에서는 티어 1·2 가 −28ms 짜리 후보를
  집어 ±3 곡선에서 티어 2 와 3 이 158ms 동률이 됐다. 섹터가 전부 공정해진 뒤로 후보가
  900/900 남으므로 조일 여유가 생겼다

재큐레이션 결과

| 지표 | 0.5.4 | 0.5.5 |
|---|---|---|
| 수제 섹터 풀 | 12개 | **18개** |
| 섹터 커버리지 | 12/12 | **18/18** |
| ±2 곡선 | 196/181/158/132 | 185/181/158/135 |
| ±3 곡선 | 183/169/154/129 | **187/169/158/118** |
| 함정 · 코스 중복 | 0 · 0쌍 | 0 · 0쌍 |
| 섹터 공정성 | 12개 통과 | **18개 통과** |

**시드가 다시 전부 바뀌었으므로 저장된 스테이지 최고 기록은 무효다.**

남은 것

- **±2 에서 티어 1 과 2 의 간격이 4ms 다**(185 대 181). 드리프트를 20ms 로 조이면서
  티어 1 이 목표 200ms 에 닿지 못하게 된 대가다. ±3 단조를 되찾는 쪽을 택했다 —
  처음 만나는 조건이 ±2 이지만, 해금 뒤 곡선이 무너지는 것이 더 나쁘다고 봤다
- **`gorge-2` 가 60자리 중 18회(30%)로 여전히 편중돼 있다.** 회랑이 9종으로 쪼개지면서
  개별 회랑의 지분이 낮아진 반사 효과이기도 하다

## 0.5.6 — 2026-09-16

런 결말 수집. 레포 최초의 백엔드이고, 솔버의 난이도 수치를 처음으로 사람 데이터와
대조할 수 있게 하는 배관이다.

**"죽은 위치"를 그대로 저장하면 틀린 곳을 고치게 된다.** 중립이 없는 조작에서는 생존
회랑을 벗어난 지점과 벽에 닿는 지점이 언제나 떨어져 있어, 사망 x 를 집계하면 좁은
섹터가 앞 섹터의 죄를 뒤집어쓴다. 그래서 클라이언트는 **원인을 계산하지 않고 재구성에
필요한 최소 입력만** 보낸다 — 이탈 지점은 오프라인 분석기가 솔버로 코스를 되살려 구한다.

- **결말 셋을 센다** — `death` · `clear` · `abort`. Stage 는 스스로 재시작하므로 사망만
  세면 분모가 없고, 그만두고 떠난 사람은 `abort` 로만 잡힌다. Stage 사망은 지금까지
  셸로 올라오지도 않았다
- **선택열(`lanes`)이 재구성의 전부다** — 게이트 제안은 (게이트 시드, 통과 시점의 빌드) 의
  결정적 함수이므로, t/b 한 글자씩 쌓은 문자열만 있으면 그 사람이 탄 코스가 정확히 되살아난다
- **코스 지문** — 섹터 노드·시드표·튜닝·축 눈금별 실제 계수의 내용 해시. 0.5.5 에서
  시드표가 통째로 갈렸듯 코스는 버전마다 다른 코스다. 마지막 항이 `axes.ts` 변경까지
  잡으므로 **손으로 올리는 상수가 없고, 따라서 올리는 것을 잊을 수 없다**
- **저장은 Vercel Blob(비공개)** — 경로 `tele/<project>/<지문>/<날짜>/` 에 지문이 들어가
  합산 금지가 디렉터리로 강제된다. 확인은 `tools/death-report/pull.sh`
- **개발에서는 `.telemetry/` 로 떨어진다** — Blob 토큰이 없어도 성공 경로를 밟아 볼 수 있다.
  스토어를 붙이기 전에는 "수집된다"를 배포해 봐야만 알게 되는 것이 싫었다
- **자동 플레이테스트를 명시 차단** — puppeteer 시나리오가 실제로 죽으므로, 끄지 않으면
  오토파일럿의 사망이 사람의 사망으로 집계된다. `?telemetry=off`

왜 Blob 인가

| 선택지 | 판정 |
|---|---|
| 런타임 로그에 `console.log` | 탈락. Hobby 는 런타임 로그를 **1시간**만 보관하고 Log Drains 는 Pro 이상이다 |
| Redis(Upstash) | 가능하지만 Vercel KV 사문화 이후 Marketplace 통합이라 외부 벤더 계정과 토큰이 새로 생긴다 |
| **Vercel Blob (비공개)** | 채택. 1st-party, Hobby 무료 한도 안, CLI 가 OIDC 로 인증해 레포에 읽기 토큰을 두지 않는다 |

Blob 에 append 가 없어 배치 하나가 객체 하나가 된다. 앞선 검토에서 이 점 때문에 뺐지만,
배치 50건이면 테스터 한 세션이 객체 열 개 남짓이라 `list` 한 페이지 안에 들어간다 —
**규모가 작아서 단점이 단점이 아니다.**

만들면서 잡은 것

- **빈 선택열이 400 으로 거부됐다.** 첫 게이트 전에 죽은 런의 `lanes` 는 빈 문자열이고,
  그것이 신규 플레이어의 가장 흔한 사망이다. 서버가 모든 문자열에 "길이 > 0"을 요구해
  **가장 필요한 데이터를 통째로 막고 있었다.** 검증기가 키 집합만 보고 값 제약을 보지
  않아 놓친 것이라, 거울을 값까지 보도록 고치고 이 경우를 회귀로 넣었다
- **죽은 엔드포인트를 두드리는 뜨거운 루프.** 전송이 실패하면 큐가 계속 50 이상이라
  사망마다 재전송을 때리게 된다. 연속 실패에 간격을 두 배씩 늘리고(최대 8분), 백오프
  중에는 크기 조건으로 보내지 않게 했다
- **한도를 넘기면 이미 모은 것도 못 꺼낸다.** Hobby 의 Blob 한도를 초과하면 스토어
  접근이 30일 막힌다. 그래서 크기·레이트 상한은 위생이 아니라 **데이터를 지키는 장치**이고,
  실질적 방어는 주기적으로 덤프해 레포 밖에 두는 것이다

측정

| 무엇 | 결과 |
|---|---|
| 재구성 가능성 | 스테이지 12개 × 축 상한 2종 = 24주행, `lanes` 재현 빌드와 기록 빌드 **불일치 0** |
| 이벤트 크기 | 건당 250~262B. 배치 50건이 13KB 로 서버 상한 32KB 안 |
| 실주행 수집 | 브라우저 13회 사망 → 배치 1건(11 이벤트·2.9KB) POST 204, 탭 종료에서 `abort` 까지 도착, 큐 잔여 0 |

남은 것

- **분석기가 아직 없다.** 배관만 섰고, 여유 구간별 사망률 표와 이탈 지점 `k*` 재구성은
  2차다. 그 표가 티어 목표치(200/180/150/130ms)를 데이터로 다시 정하는 근거이고,
  지금 그 값들은 프로브 추정이지 사람으로 검증된 것이 아니다
- **수집 기본값이 켬이다.** 좌표·시각·빌드 눈금뿐이고 끄기가 한 번의 클릭이지만 판단이다.
  끔이 기본이면 이탈률은 사실상 못 잰다
- Blob 스토어를 레포 공용으로 둘지 wave-runner 전용으로 둘지 — 경로에 프로젝트가 들어가
  있어 하나로 충분하지만, 한도 잠금이 프로젝트 전체로 번진다

## 0.5.7 — 2026-09-16

분석기. 0.5.6 이 세운 파이프의 뒷단이고, 이 기능이 존재하는 이유다 —
**솔버의 난이도 수치가 사람 체감과 어디서 어긋나는가.**

`tools/death-report/` 세 걸음: 내려받기(`pull.sh`) → 보고서(`report.ts`) → 그림(`figure.mjs`).

- **표 A 섹터별 조건부 사망률** — 분모가 진입 횟수다. 사망만 세면 Stage 가 스스로
  재시작하는 탓에 언제나 앞 섹터가 가장 어렵게 나온다. 한 섹터가 티어 사망의 40% 를
  넘으면 재큐레이션 대상으로 지목한다
- **표 B 여유 구간별 사망률** — 20ms 밴드. 티어 목표치(200/180/150/130ms)를 데이터로
  다시 정하는 근거이고, 밴드 간 역전을 찾아 "솔버가 못 보는 요인"을 짚는다
- **표 C 경로별 사망률** — 큐레이션이 통과 가능성은 보장하므로, 여기서 찾는 것은
  *이론상 가능하지만 사람이 못 하는* 경로다
- **표 D 시도 분포** — brief 의 미수행 중단 판정("지시 없이 5회 이상 재시도")이 직접 읽힌다
- **그림** — 여유 곡선 위에 실측 사망을 겹쳐 한 장. 솔버가 예측한 최난 구간과 사람이
  실제로 죽는 구간이 겹치는지가 한눈에 보인다
- 표본 바닥(섹터당 사망 30 · 스테이지당 주행 20)에 못 미치는 행은 `보류` 로 표시한다.
  숫자는 나오지만 판정은 서지 않는다는 것을 숨기지 않는다

### 설계 전제 하나가 틀렸다

이 기능 전체의 출발점은 "사망 좌표는 원인보다 한참 하류다"였다. 중립이 없는 조작에서는
생존 회랑을 벗어난 지점과 벽에 닿는 지점이 떨어져 있으니 사망 x 를 집계하면 좁은 섹터가
앞 섹터의 죄를 뒤집어쓴다 — 그래서 이탈 지점의 상계 `k*` 를 계산하게 만들었다.

**재 보니 그 거리가 중앙 1.1 · 최대 5.2 월드 단위였다.** 섹터 하나가 460 단위이고,
조각을 옮긴 사망은 0% 였다. 이유는 지오메트리다 — 생존 회랑을 죄는 쪽 경계가 곧
플레이어가 부딪히는 벽이라, 회랑을 벗어나는 자리와 벽에 닿는 자리가 사실상 같은 x 다.

그래서 **사망 x 로 집계해도 섹터 단위에서는 틀리지 않고**, 대신 `k*` 는 상계로서
아무것도 배제하지 못한다. 상류 원인을 짚으려면 궤적 표본(`tail`)이 있어야 한다.
`k*` 계산은 남겨 뒀다 — 값이 싸고, 조각을 옮기는 사망이 생기면 그때 잡는다.

만들면서 고친 것

- **솔버 추적에 `up`·`down` 을 실었다.** 역추적이 추적만으로 가능해야 분석기가 솔버의
  내부 계산을 복제하지 않는다. 추적은 기본이 꺼져 있어 런타임에는 영향이 없다
- **코스 전체 회랑을 다시 구한다.** 솔버의 B[k] 는 그 조각 끝까지의 생존이라 조각 경계
  근처에서 S 가 크게 부풀고, 보정 없이는 `k*` 가 사망 지점에 붙는 것이 지오메트리
  때문인지 계산 방식 때문인지 가릴 수 없었다
- **`tests/` 와 `tools/` 가 타입체크 밖에 있었다.** `tsconfig.tools.json` 을 만들어
  `lint` 에 넣자 `gate-offers.ts` 가 `RunConfig` 를 없는 곳에서 가져오고 있던 것이
  드러났다. tsx 는 타입 임포트를 지워 버려 런타임에는 보이지 않던 오류다
- **그림의 경로 라벨이 조기 사망 경로로 뽑혔다.** 선택열은 죽은 자리까지만 쌓이므로
  최빈값을 그냥 쓰면 "두 번째 게이트에서 죽은 경로"가 뽑혀, 그림이 아무도 끝까지 가
  보지 않은 코스를 말하게 된다. 완주 길이의 경로를 우선하게 고쳤다
- **사망 막대가 1 월드 단위 격자라 한 픽셀도 안 됐다.** 20단위로 묶었다

데이터가 없을 때는 `simulate.ts` 가 오토파일럿으로 합성 데이터셋을 만든다. 실제 엔진과
실제 이벤트 빌더를 쓰므로 스키마와 좌표는 수집본과 같은 모양이지만, **사람 데이터가
아니다** — 오토파일럿은 시야가 완벽해 사람이 어디서 죽는지를 말해 주지 않는다.
보고서와 그림이 지문 `synthetic` 을 그렇게 표시한다.

남은 것

- **사람 데이터가 0 건이다.** 파이프도 분석기도 섰지만 아직 아무것도 모른다.
  Vercel 에 비공개 Blob 스토어를 붙이고 사람 손에 들어가야 표 B 가 의미를 갖는다
- `tail` 표본을 넣을지는 표 A 가 한 섹터를 지목한 뒤에 정하는 것이 낫다

## 0.5.8 — 2026-09-16

Blob 스토어를 실제로 쓰는 쪽을 마저 구현했다. 0.5.6 은 **쓰기**만 코드였고 읽기는
`vercel` CLI 에 기대고 있었다.

- **`doctor.ts` — 스토어가 도는가.** 쓰기 → 비공개 확인 → 목록 → 읽기 → 삭제를 한 번
  돌려 **어디서 끊기는지 이름을 붙인다.** 코드가 있다고 수집이 되는 것이 아니다 —
  토큰 없음 · 공개 스토어 · 한도 잠금 · 미연결은 증상이 전부 다르고, 특히 공개/비공개는
  스토어를 만들 때 정해져 코드에서는 확인할 길이 없다. 그래서 인증 없는 요청을 실제로
  한 번 던져 본다. 점검이 쓴 객체는 같은 실행에서 지운다
- **`pull.ts` — SDK 로 내려받는다.** `pull.sh` 를 대체한다. CLI 는 플래그 이름과 표
  출력 형식에 기대야 하고, 무엇보다 **객체가 1000개를 넘는 순간 목록 한 번으로는 조용히
  잘린다.** 조용히 잘리는 것이 최악이다 — 보고서는 그대로 나오고 숫자만 틀린다.
  커서로 끝까지 돌고 `--fp` · `--since` · `--local` 을 받는다
- **`blob.ts` — 자격을 한곳에.** `BLOB_READ_WRITE_TOKEN` 또는 `VERCEL_OIDC_TOKEN` +
  `BLOB_STORE_ID`. `vercel env pull` 이 떨어뜨린 `.env` 를 읽되 **이미 설정된 값은
  덮어쓰지 않는다** — 셸에서 준 토큰이 파일에 밀리면 디버깅이 불가능해진다
- 엔드포인트의 개발 폴백 판정에 OIDC 를 넣었다. `vercel dev` 처럼 토큰 대신 OIDC 가
  오는 환경에서 파일로 새면 "로컬에서는 되는데 배포하면 다르다"가 생긴다
- 표 A 의 편중 판정에서 **"편중이 없다"와 "판정을 못 했다"를 갈랐다.** 섞어서 말하면
  표본이 얇을 뿐인데 "괜찮다"로 읽힌다

검증: 브라우저 13회 사망 → 엔드포인트 → 저장 → `pull.ts` → `report.ts` 까지 실제로
한 줄로 통과시켰다(이벤트 24건, 지문 `3ef3592e`). 자격이 없는 상태의 `doctor.ts` 는
무엇을 해야 하는지 네 줄로 안내한다.

**아직 사람 데이터는 0건이고, 스토어도 없다.** Vercel 대시보드에서 비공개 Blob 스토어를
만들어 프로젝트에 연결하는 것은 코드가 할 수 없는 일이라 남아 있다 —
`tools/death-report/README.md` 의 "스토어 준비" 네 단계.

## 0.7.0 — 2026-09-17

**첫 실행 안내.** 손끝 핸드셰이크 한 번이고, 그 끝이 곧 첫 판의 출발이다.

brief 의 대상 조항이 "설치도 계정도 **튜토리얼도** 없다" 였으므로 이 작업은 조항과 정면
으로 부딪혔다. 조항을 지우지 않고 **좁혔다** — 0.5.0 이 HUD 조항을 좁힌 것과 같은 방식
이다. 조항이 지키려던 것은 화면 수가 아니라 **지시를 읽게 만들지 않는 것**이다.

- **진행 단계도, 넘기기 버튼도, 읽고 확인할 문단도 없다.** 한 번 누르고 한 번 놓으면
  끝난다(실측 1초 남짓). "브라우저에서 30초 안에 시작한다" 도 그대로다
- **끄는 스위치가 없다.** 두 동작을 익히면 마지막 한 번의 누름이 곧 티어 1-1 의 출발
  이고 그때 `meta.taught` 가 켜진다. "다시 보지 않기" 를 두면 그것 자체가 읽고 판단해야
  하는 일이 되어, 안내가 스스로 조항을 어긴다
- **첫 실행은 홈보다 안내가 먼저다.** 홈을 먼저 보여주고 안내를 덮으면 결정이 둘이 되고
  (무엇을 고를까 → 안내를 닫을까), 그 첫 결정이 아무것도 모르는 상태에서 내려진다
- **그림은 실제 계수로 그린다.** `resolve()` 가 준 상승·하강·전진 속도를 그대로 적분하고
  아바타는 `silhouetteOf` 를 히트박스 반지름으로 키운다. x·y 배율이 같아 **여기 보이는
  각도가 플레이 중 각도**다(표준 45°). 다른 그림을 보여주면 안내가 첫 거짓말이 된다
- **가두지 않고 세로로 따라간다.** 처음엔 창 안에 clamp 했는데 꾹 누르면 천장에 붙어
  **수평 구간**이 그려졌다 — "가만히 있는 선택지는 없다" 고 써 놓고 멈춘 그림을 보여주는
  셈이다. 여유 밖으로 나가려 할 때만 카메라가 따라가면 궤적은 언제나 기울어져 있다
- **안내 안에서는 죽지 않는다.** 첫 경험이 사망이면 배운 것이 "이 게임은 나를 죽인다"
  하나뿐이다. 즉사·자동 재시작·게이트 교환은 세 줄로만 알리고, 읽지 않아도 통과된다
- **입력 규칙이 플레이와 같다.** 활성 포인터를 세고 `Space`/`W`/`↑` 도 받는다 — 여기서만
  다르게 굴면 익힌 것이 본 게임에서 한 번 배신당한다
- **이미 해 본 저장본에는 뜨지 않는다.** `taught` 를 모르던 판본은 클리어·시도·최고 거리
  중 하나라도 있으면 배운 것으로 읽는다. 아는 사람에게 안내를 띄우는 것 자체가 실패다
- 다시 보기는 홈 푸터의 **조작 안내**. 한 번 끄면 영영 못 보는 것은 다른 종류의 실패다

검증 — 타입체크·빌드 통과, 실제 크로뮴에서 안내 16항목(첫 실행 진입 · 누름→놓음 단계
전이 · 마지막 누름이 곧 1-1 · `taught` 저장 · 두 번째 실행에는 없음 · 다시 보기는 홈으로
복귀 · 해 본 저장본은 건너뜀 · 빈 저장본은 안내). 0.6.1 의 27항목도 회귀 없음.

## 0.6.1 — 2026-09-17

진입에서 이탈까지의 흐름을 코드에서 읽어 적다가(`notes/user-flow.md`) 주 동선 위의
마찰 셋이 드러났고, 셋 다 고쳤다.

**클리어에서 누르면 목록으로 간다.**

- 클리어 오버레이가 **그 티어 세 칸의 최고 기록을 나란히 보여주면서** 누르면 홈으로
  떨어뜨리고 있었다. 다음 칸을 하려면 홈 → Stage → 셀로 세 번을 더 눌러야 했다 —
  "다음은 2번이다" 라고 말해 놓고 길을 끊는 셈이고, 깨고 또 깨는 것이 이 게임의 주
  동선인데 그 동선에만 마찰이 세 번 있었다
- **콜백을 갈라 고쳤다.** `onExit` 은 그만두기(좌상단 버튼·`Escape`)라 목적지가 홈이고,
  새로 뚫은 `onAdvance` 는 클리어에서 누른 것이라 목적지가 목록이다. 하나로 합쳐 두면
  같은 입력이 두 뜻을 갖게 되어 어느 한쪽이 반드시 틀린다
- 그래서 오버레이 문구도 `누르면 계속` → **`누르면 목록으로`** 다. 이 줄은 원래 결과와
  어긋나 있었는데, **문구가 먼저 틀린 게 아니라 동선이 틀렸고 문구가 그것을 정직하게
  못 적었을 뿐이다** — 문구만 고쳤으면 구멍이 그대로 남았다

**연습 모드가 켜져 있음이 런 내내 보인다.** 표시가 `ready` 오버레이 한 줄뿐이라 켠
채로 달리게 두었고, 그 사실은 클리어하고 나서야("기록에 남지 않는다") 드러났다.

- **상태를 `meta.practice` 로 저장한다.** 두 방향의 위험이 반대라 둘 다 막아야 했다 —
  꺼진 줄 아는 것(새로고침에 조용히 꺼지면 "죽었는데 왜 처음으로 가지" 가 되고 그건
  조작 실수처럼 보인다)은 저장으로, 켠 줄 모르는 것은 아래 표시로 막는다. 모르는
  저장본만은 **꺼진 것으로** 읽는다(`hud`·`telemetry` 와 반대다) — 연습으로 열면 그
  사람의 기록이 조용히 사라진다
- **주행 내내 화면 가장자리에 파선 테두리**를 둔다. 통로 위에 글자를 얹으면 시선 예산
  조항과 부딪히지만, 시선은 아바타(좌측 0.18)에서 오른쪽으로 훑으므로 가장자리는 그
  경로 밖이다. **주행 표시(`H`)와 독립이다** — 잊는 것을 막으려는 표시가 설정에 따라
  사라지면 그게 정확히 실패하는 자리다
- **색이 아니라 파선인 것은 색으로 했다가 부딪혔기 때문이다.** `--accent` 를 썼는데 그
  값(`#ffe66d`)이 렌더의 `player` 와 **같은 색**이었다 — `wallEdge` 가 각도 축과 같은
  청록이던 사고와 같은 종류다. 남은 색을 찾는 게 아니라 애초에 색이 아니어야 한다
- 목록 토글의 경고를 "기록에는 남지 않는다" → **"기록·코어·티어 해금에 남지 않는다"**
  로 넓혔다. 실제로 안 열리는 것은 티어인데 그 말이 없었다

문서

- `notes/user-flow.md` 추가 — 신 전환(셸 화면 4종)과 런 페이즈 4종을 두 층으로 갈라
  도식화하고, 두 층을 잇는 것이 `onPhase` 콜백 하나뿐임을 적는다. `press()` 가
  페이즈마다 출발·상승·목록·재시작 넷으로 갈리는 것도 표로 세웠다

검증 — 타입체크 통과, `lookahead` 스모크 통과, 실제 크로뮴에서 흐름 27항목 확인.
복귀지 11항목(클리어 → 목록 · 목록의 클리어 표시 갱신 · `Esc`/`←` 는 홈 유지 ·
Endless 해금)과 연습 16항목(저장·새로고침 생존·`H` 를 꺼도 남는 테두리·연습 통과는
기록도 코어도 클리어 표시도 남기지 않음·끄면 테두리도 사라짐).

## 0.6.0 — 2026-09-16

**두 줄기를 합쳤다.** 기체 베리에이션(기체 4종)과 코스 큐레이션·수집·분석기가 0.5.0
이전에 갈라져 서로 모른 채 각자 0.5.x 를 썼다. 어느 쪽도 버리지 않는다.

무엇이 얽혔나

- 코드 7개가 양쪽에서 바뀌었다 — `App.tsx` · `GameCanvas.tsx` · `engine.ts` · `meta.ts` ·
  `render.ts` · `storage.ts` · `styles.css`. 충돌 파일은 18개였다
- 가장 깊은 곳은 **셸의 상태 모델**이다. 기체 줄기는 `report` + `reward`, 코스 줄기는
  0.5.0 이 만든 `RunResult` 기록 화면이었다. 충돌을 기워 붙이는 대신 **기체 판을 바탕으로
  기록 화면을 다시 얹었다** — 기록은 이제 기체별이다(`runner:tier:no`)

어떻게 합쳤나

- **메타**는 기체 줄기의 v3 형태(기체별 키, 프리셋 흡수)를 취하고 `hud` · `telemetry` 를
  얹었다. v2 → v3 마이그레이션이 두 설정을 실어 나른다 — 해금이 아니라 취향이라 잃으면
  조용히 기본값으로 되돌아가고, 그게 더 나쁘다
- **결과 화면**은 기체별 최고 기록을 읽는다. 클리어 화면의 티어 목록도 "이 기체 기준"임을
  제목에 적는다
- **출발 형태**는 프리셋이 아니라 기체다. 텔레메트리도 그것을 싣는다

드러난 것

- `tests/` 가 타입체크 밖이라 **기체 줄기의 `runner-probe.ts` 가 `Runner` 를 없는 모듈에서
  가져오고 있었다.** tsx 는 타입 임포트를 지워 런타임에는 보이지 않던 오류다
- 큐레이션이 `meta.stageKey` 를 시드표 키로 쓰고 있었다. 그 함수에 기체가 붙으면서 형식이
  갈라졌다 — 시드표는 기체와 무관하므로 `tier:no` 를 직접 쓴다

검증 — verify PASS(lint · build · playtest 8 shots · 오류 0), 검증기 10종 전부 통과.
코스 줄기 6종(솔버·섹터 공정성·전 경로·수집 재구성·게이트 교환·축 양날)과 기체 줄기
4종(각도·등급·경로·기체 프로브)이 **서로의 변경 위에서** 돈다.

알려진 부채

**큐레이션이 기체 하나를 전제로 계산됐다.** 기체는 축 눈금을 계수로 옮기는 곡선이
다르므로, 0.5.1~0.5.5 가 적어 둔 티어 곡선(200/180/150/130ms 목표, ±2 실측
185/181/158/135)은 **4종 중 1종에 대해서만** 참이다. 같은 이유로 수집의 코스 지문과
분석기의 표도 기체를 축으로 갈라야 한다. 기체별 재큐레이션은 그 자체로 프로젝트 하나
크기라 여기에 넣지 않는다.
